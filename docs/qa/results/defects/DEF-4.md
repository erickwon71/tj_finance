# DEF-4 — calendar_financials 인접연도 CQ1(1분기) 수치 중복 — 광범위 데이터 결함(데이터결함, 앱버그 아님)

- **심각도**: S1 격상 검토 대상(범위가 매우 넓고 조용히 잘못된 값을 노출)
- **분류**: 데이터결함 (앱버그 아님 — 앱은 DB에 저장된 값을 있는 그대로 정확히 표시하고 있음)
- **상태**: ✅ 종결(2026-07-10) — 근본원인 확정(07-08)·메인 수정+전수 재처리(07-09)·잔여 위조중복 예외처리(07-10) 전부 완료. 앱 노출 위조 CQ1 = 0. 상세는 하단 "잔여 예외처리 완료" 절 참고
- **발견 체크리스트 ID**: DS-2/DS-3 조사 중 부수적으로 발견 — UI가 DB값을 그대로 보여주므로 자동 스윕의 UI↔DB 대조로는 잡히지 않고, DB 자체를 직접 대조하다 발견됨
- **발견일 / 담당자**: 2026-07-08 / shard5(DEF-S5-1, MDS테크)와 shard2(DEF-S2-1, 삼영이엔씨)가 각자 독립적으로 관찰, 감리(Opus)가 두 관찰을 연결하고 전수 규모를 정량화

## 위치
- **테이블**: `calendar_financials` (앱의 분기 재무제표 뷰가 조회하는 원천 테이블)
- **패턴**: 같은 기업·같은 재무제표 기준(연결 또는 별도)에서, **인접한 두 해의 CQ1(1~3월, 달력 1분기) 매출액과 영업이익이 소수점까지 완전히 동일한 값**으로 저장됨.

## 발견 경위
1. shard5가 MDS테크(00445841)의 분기x연결에서 UI↔DB 값불일치를 발견(DEF-S5-1) — 조사 중 DB 자체에 2012 CQ1과 2013 CQ1의 revenue/operating_income이 완전히 동일하게 저장된 것을 발견.
2. shard2가 삼영이엔씨(00288343)의 분기x별도에서 "동일 UI값이 다른 연도에 반복"되는 패턴을 발견(DEF-S2-1).
3. 감리가 DEF-3 조사 중 삼영이엔씨의 DB 원값을 직접 대조하다가, 2017 CQ1과 2018 CQ1의 연결 기준 매출액·영업이익이 완전히 동일함을 재확인 — MDS테크와 동일한 유형의 이상현상임을 확인.
4. 감리가 `calendar_financials` 전체에 대해 "같은 기업·같은 statement_type에서 인접연도 CQ1 매출액·영업이익이 완전히 동일한 경우"를 SQL로 전수 검사.

## 실제결과 — 규모(전수 검사 결과)
- 전체 `calendar_financials`의 CQ1(값 존재) 행 **69,485건** 중,
- **3,996건**(corp_code·statement_type·연도쌍)이 인접연도와 매출액·영업이익 모두 완전 동일(약 **5.75%**).
- 영향받는 **고유 (기업, 연결/별도) 조합 수: 3,626건**.
- **2012→2013년 전환 구간에 뚜렷하게 집중**되는 클러스터가 관찰됨(다수의 서로 무관한 기업에서 반복 확인). 그 외 연도에도 산발적으로 발생.

## 증거
- 감리가 실행한 전수 SQL(인접연도 CQ1 완전동일값 검사) 결과, 상위 30건 샘플 확인(00100601, 00100939, 00101044, 00101220, 00101433, 00101488, 00101549, 00101628, 00101664, 00101752, 00102113, 00102140, 00102353, 00102432, 00102618, 00102751, 00102858 등 다수 기업의 corp_code·statement_type·연도쌍).
- 상세 사례: MDS테크(2012/2013 CQ1, DEF-S5-1 원문), 삼영이엔씨(2017/2018 CQ1, 연결).
- **UI는 이 값을 있는 그대로 정확히 표시하고 있어 자동 스윕의 UI↔DB 대조로는 발견되지 않음** — 화면 검증만으로는 잡을 수 없고 DB 자체의 정합성 점검(인접기간 완전동일값 검사 등)이 별도로 필요함.

## 영향 범위
- 범위가 넓어(3,626개 기업×기준 조합) 단일 기업 이슈가 아니라 데이터 전반의 문제로 판단됨.
- **DEF-3(별도/연결 표시 문제)와는 무관한 별개 결함**임 — 두 문제가 우연히 같은 기업(삼영이엔씨)에서 겹쳐 관찰되어 최초 보고서(DEF-S2-1)에서는 하나의 현상처럼 보였으나, 실제로는 서로 다른 두 층의 문제(앱의 별도/연결 표시 문제, DB의 CQ1 인접연도 중복값)가 중첩된 것임.

## 근본원인 확정 (2026-07-08 개발 조사)

**결론: `fact_v2` 추출 단계의 분기 손익계산서 비교컬럼(전기 3개월) 값 중복 버그.** 앱·calendarize·comparative_fallback 은 모두 이 잘못된 셀을 충실히 전파할 뿐.

### 역추적 증거 (MDS테크 00445841, 2013 Q1 보고서 rcept=20130529000237)
`fact_v2` 의 `is.revenue` 셀을 직접 조회한 결과:

| basis | col_index | context_fiscal_year | amount_won |
|---|---|---|---|
| consolidated | 0 (당분기) | 2013 | 15,826,382,291 |
| consolidated | 1 (전기) | 2012 | **15,826,382,291** ← col0 과 동일(오류) |
| consolidated | 2 (전전기) | 2011 | 15,111,992,486 (정상, 상이) |
| separate | 0 | 2013 | 13,226,425,885 |
| separate | 1 | 2012 | **13,226,425,885** ← col0 과 동일(오류) |
| separate | 2 | 2011 | 12,639,538,117 (정상, 상이) |

→ **당분기(col_index=0) 값이 전기 3개월(col_index=1) 슬롯에 그대로 복제**됨. 전전기(col_index=2)는 정상. 분기보고서 IS 의 duration 컨텍스트(당분기 3개월 vs 전년동기 3개월)를 구분하지 못하고 같은 값을 col1 에 채우는 XBRL/표 추출 오류로 판단.

### 전파 경로
1. `fact_v2` col1 == col0 (버그 발원지).
2. `fin2/standardize/build.py::standardize_comparative_corp` → `_collect_comparative` 가 나중 보고서의 col1(전기)에서 (fy-1) 행을 합성 → 잘못된 col1 값을 std_financials_v2 (fy-1, Q1)에 기록. 이 때문에 인접연도 N·N-1 의 Q1 이 동일해짐.
3. `calendarize` 가 std 값을 CQ1 으로 그대로 배치 → `calendar_financials` 인접연도 CQ1 동일값. (calendar derivation 이 `native` 로 표기되는 라벨링 갭도 함께 확인 — 실제로는 comparative 합성행)

### 규모 (재현: `scripts/diag_calendar_cq1_dup.py`)
- calendar_financials 인접연도 CQ1 완전동일 페어 **4,046** / 고유 (기업,기준) 조합 **3,652** (CQ1 값존재 행의 5.82%).
- std_financials_v2 인접연도 Q1 동일값 페어 **3,476** 중 **3,445(99.1%)가 동일 `is_rcept` 공유** → 단일 후기보고서 백필 시그니처 확정.
- 연도쌍 분포: 2012→2013(777) 최다, 2007→2008(408), 2003→2004(218) 등 IFRS/분기 도입기에 집중되나 전 연도에 산발.

### 차기 라운드 수정 방향 (이번 라운드 미적용 — 사용자 결정: 조사만)
1. **발원지 수정**: `fact_v2` 분기 IS 추출에서 당분기 3개월 duration 을 전기(col_index=1) 컬럼에 중복 매핑하는 로직 교정. 대상은 fact_v2 를 채우는 추출기(분기 IS duration 컨텍스트 → col_index 매핑).
2. 수정 후 영향 corp 에 대해 `fin2_reextract_*` → `fin2_comparative_financial.py` → `fin2_quarterly_all.py` → `fin2_calendar_all.py` 재실행(범위·순서는 별도 계획).
3. **DQ 어서션 추가**(`scripts/dq_assertions.py`): (a) 분기보고서 fact_v2 에서 `is.revenue`/`is.operating_income` 의 col_index=0 == col_index=1, (b) calendar_financials 인접연도 CQ1 완전동일값 — 재발 감지.
4. calendar `derivation='native'` 라벨링이 comparative 합성행에도 붙는 문제 별도 점검.

### 조사 산출물
- 진단 스크립트: `scripts/diag_calendar_cq1_dup.py` (재현·정량화, `--dump-list` 로 전체목록 CSV).
- 전체 목록: `docs/qa/results/def4_cq1_dup_list.csv` (4,046행).

## 수정 완료 (2026-07-09)

**코드 수정**: `fin2/extract/text.py:219` — `interim_flow` 게이트에 `"Q1"` 추가(기존 H1/Q3만).
Q1 분기보고서 IS/CF 표 `[당기3개월,당기누적,전기3개월,전기누적]` 4열을 `_interim_cumulative_cols()`
누적컬럼 인식으로 정확히 처리 → 당기값이 전기(col1)에 복제되고 전기값이 전전기(col2)로
오라벨되던 문제 해소. 드라이런 5개사(2008~2022) 검증: col0(당기) 불변, col1(전기)이 실제
전기값으로 교정, phantom col2 소멸.

**데이터 재처리**(영향 = Q1 Track B 필링 보유 corp **2,394개**):
- **pass 1** (`scripts/def4_reprocess.py`): fact_v2 purge → E→R→S→분기→달력 재생성. 2,394사 완료, 오류 0.
- **pass 2** (`scripts/def4_reprocess_pass2.py`): pass 1 이 stale 파생행을 남기는 함정 발견 —
  standardize_comparative_corp 은 `기존 std_v2 키 불가침`(build.py:279,303)이라 예전 버그값
  comparative 행을 덮지 못하고, 그로부터 파생된 `quarterly_derived` 이산행이 `own` 으로 간주돼
  재합성을 영구 차단. → comparative_fallback 누적행 + is_discrete=True 이산행을 삭제 후
  comparative→분기→달력 재빌드. 2,394사 완료(삭제 298,432 / 재합성 34,916 / 오류 0).

**검증 결과**(`scripts/diag_calendar_cq1_dup.py` 재실행):
- 인접연도 CQ1 완전동일 페어 **4,046 → 60 (98.5% 감소)**. **2012→2013 클러스터(764) 완전 소멸.**
- 조작된 phantom CQ1 행 ~4,400개 제거(69,631→65,199) — 없는 데이터를 지어내던 행이 정상적으로 공백 처리("없는 데이터 > 틀린 데이터").
- 잔여 60쌍 분류: rev=0 휴면사 19 · 독립보고서 우연일치 15 · 같은-rcept 잔재 26(그중 23이 2003~2007 구 K-GAAP era — `3개월`/`누적` 헤더 토큰이 달라 fix 미적용, 별도 소규모 후속). **타깃 결함(3,626조합/2012→2013)은 완전 해소.**

**재발 방지**(`scripts/dq_assertions.py`에 WARN 어서션 2건 추가):
- `fact_v2_q1_duration_col0_eq_col1`: Q1 IS fact_v2 에서 col0==col1 동일값 탐지.
- `calendar_adjacent_year_cq1_identical`: 인접연도 CQ1 매출+영업이익 완전동일 탐지.

**대표 사례 확인**: MDS테크(00445841) 연결 CQ1 2012=15,111,992,486 ≠ 2013=15,826,382,291(이전 동일).
삼영이엔씨(00288343) 2017=8,212,415,332 ≠ 2018=7,278,180,580(이전 동일). 앱 라이브 렌더 정상.

## 잔여 60쌍 원인 규명 (2026-07-10, 읽기전용 조사)

같은-rcept 잔재 26건을 원문 XML까지 역추적한 결과, 메인 버그(interim_flow 게이트 누락)와는
**다른 표 구조 문제 2종**으로 확인됨(메인 수정으로는 커버 불가, 각각 별도 소규모 트랙):

1. **구 K-GAAP era(2002~2007, separate, 23쌍)** — 데이터행에 colspan 아티팩트로 빈 셀이
   교차 삽입됨(예 광동제약 2007Q1 매출액행 `['1.매출액','','49.5B','','49.5B','','46.5B','','46.5B']`).
   헤더행 `['3개월','누적','3개월','누적']`의 누적 위치(1,3)와, extract_rows 의 선두 None 절삭 후
   데이터 금액 위치(0,2)가 **어긋나** cum_map 이 None 셀을 가리킴 → 폴백이 present 값 순서대로
   집어 당기3개월·당기누적(둘 다 동일값)을 col0·col1 로 → 중복. 즉 헤더↔데이터 위치 정합이
   빈 셀 때문에 깨지는 구조. 수정하려면 헤더·데이터 양쪽의 빈 셀을 동일하게 압축해야 하는데,
   현대 IFRS 보고서에서 잘 도는 헤더 인식/셀 처리에 회귀 위험이 있어 별도 트랙으로 보류.

2. **현대 소수 변종(2017/2019/2020, 3쌍)** — fact_v2 에 여전히 col0==col1+col2 버그 시그니처.
   interim_flow=Q1 은 적용됐으나 `_interim_cumulative_cols` 가 이 보고서들의 IS 표 헤더를
   인식 못 해(요약재무 하이라이트표 등 다른 레이아웃 공존) 위치 폴백을 탐 → 버그 잔존.
   3건뿐이라 개별 케이스로 별도 확인 권장.

- 나머지 잔여: rev=0 휴면사 19(진짜 동일, 정상) · 독립보고서 우연일치 15(다른 rcept).
- **두 종 모두 `dq_assertions.py` 의 신규 WARN 어서션(fact_v2_q1_duration_col0_eq_col1=326,
  calendar_adjacent_year_cq1_identical=41)이 상시 추적** → 향후 타깃 수정 시 진척 계측 가능.

## 잔여 예외처리 완료 (2026-07-10, 구조무관 안전제거)

사용자 결정: 메인 파이프라인은 그대로 두고, 예외는 **격리된 1회성 스크립트**로 DB 보정
(`scripts/fin2_kgaap_gap.py`와 동일 철학). 방식=**안전 제거**(원값 복구 아님 — 위조 중복을
걷어내 공백으로, "없는데이터>틀린데이터").

**구조무관 규칙**: 한 보고서(rcept)·한 basis 안에서 `is.revenue col0(당기)==col1(전기)`(≠0)인
Q1 보고서 = 추출 버그(분기 당기==전년동기 정확일치는 사실상 항상 버그). 해당 보고서의
flow 계정(`is.%/cf.%`) `col_index>=1` 삭제(당기 col0만 보존). 구 K-GAAP·현대 변종·미지 변종
모두 동일 규칙으로 커버.

**실행**(`scripts/def4_exception_remove_dup.py`, C5 종료 후):
- 대상 172 (rcept,basis) · 영향 81 corp · fact_v2 삭제 15,271행(당기 보존, col≥1 flow만).
  삭제 전 `docs/qa/results/def4_exception_deleted_facts.csv`로 전체 덤프(가역).
- 재파생=기존 `def4_reprocess_pass2.py --corps-file`(81사, comparative 삭제 12,612/재합성 1,466/오류 0).
- 사전 백업 `~/tj_finance_db_backups/tj_finance_core_20260710_0938.dump`.

**검증 결과**:
- 인접연도 CQ1 완전동일 페어 **60→34**, 그중 **same-rcept 위조잔재 26→0**(완전 제거).
  잔여 34 = rev=0 휴면 19 + 독립rcept 우연 15(둘 다 진짜 동일, 버그 아님).
- DQ: `fact_v2_q1_duration_col0_eq_col1` 326→**23**, `calendar_adjacent_year_cq1_identical` 41→**15**. ERROR 위반 0.
- 대표: 광동제약(00103592) separate 2006 CQ1 = 공백(위조 제거), 2007 = 실제값 유지.
- **잔여 fact 23건은 의도적 미제거** — op_income 만 col0==col1(revenue 상이)인 케이스라,
  revenue 정상 comparative 를 과잉삭제할 위험이 있어 revenue-센티넬 보수적 유지. calendar
  중복은 유발 안 함(same-rcept=0). WARN 어서션이 계속 추적.

## 비고
- 근본원인 확정(2026-07-08), 수정·재처리·재검증(2026-07-09), 잔여 규명+예외처리 완료(2026-07-10).
- **DEF-4 전체 종결**: 메인(3,626조합/2012→2013, 98.5%↓) + 잔여 위조중복(same-rcept 26→0). 앱 노출 위조 CQ1 = 0.
