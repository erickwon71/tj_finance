# 계층2 원문전체대조 캠페인 — 발견 이슈 로그 (2026-09-20~)

파서 코드는 캠페인 중 수정하지 않는다. 여기에 기록만 하고 다음 보고서로 진행.

---

## 1. 두산에너빌리티 2019Q3 (r20191114002511) — [연결] 손익계산서 EPS 계속/중단영업 세부내역 결측

- **회사**: 두산에너빌리티 (00159616) KOSPI 시총순위 11
- **보고서**: 2019Q3 분기보고서, 접수번호 20191114002511
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20191114002511
- **CSV**: `layer2_review/KOSPI/00159616_두산에너빌리티/quarter/2019/20191114002511_review.csv`

**증상**: [연결] 손익계산서에서 DART 원문은 "기본주당이익(손실)"과 "희석주당이익(손실)" 아래에
각각 "계속영업이익(손실) (단위 : 원)"과 "중단영업이익(손실) (단위 : 원)" 세부 항목을 함께
공시하는데(당사가 2019년에 가스터빈/발전 등 일부 사업을 중단영업으로 분류), DB에는
기본주당이익(손실) -699, 희석주당이익(손실) -699 (모두 누적 컬럼) 두 합계값만 적재되어 있고
계속영업/중단영업 세부 EPS 4개 항목(3개월·누적 각 계속영업/중단영업)이 통째로 빠져 있다.

**원문값 (DART, 제57기 3분기, 3개월/누적)**:
| 항목 | 3개월 | 누적 |
|---|---|---|
| 기본주당이익(손실) | (604) | (699) |
| 계속영업이익(손실) | (558) | (786) |
| 중단영업이익(손실) | (46) | 87 |
| 희석주당이익(손실) | (604) | (699) |
| 계속영업이익(손실) | (558) | (786) |
| 중단영업이익(손실) | (46) | 87 |

**DB 적재 상태**: 기본주당이익(손실) -699 / 희석주당이익(손실) -699 만 존재 (순번 22,23 — CSV
`구분,단위,순번,깊이,항목명,금액` 기준 rows 314-315). 적재 행수 헤더가 "[연결] IS 33"으로
정확히 33개 항목만 카운트되어 있어 실제로 33개만 적재되었음을 확인함(계속영업/중단영업 EPS 4개
항목 누락).

**추정 원인(미확인, 코드 미조사)**: EPS 섹션 파서가 "기본주당이익(손실)"/"희석주당이익(손실)"
헤더 바로 아래 항목만 단일 값으로 인식하고, 그 하위에 다시 계속영업/중단영업으로 나뉘는
2단계 중첩 구조(라벨이 상위와 동일하게 "계속영업이익(손실)"/"중단영업이익(손실)"으로 반복됨)를
처리하지 못해 드롭했을 가능성. R28 계열(EPS 후속트랙, `[[eps-r28-followup-tracks-design-2026-08-16]]`)
범위 밖의 신규 패턴으로 보임 — 당시엔 계속/중단영업 EPS 세부내역 케이스가 다뤄지지 않음.

**영향 범위(미조사)**: 중단영업이 있는 다른 회사/분기에도 동일 패턴이 있을 가능성 있음 —
계층2 캠페인 종료 후 별도 스캔 필요.

**본 필링 처리**: `fail --note` 로 표시 후 다음 보고서로 진행 (파서 수정 없음).

---

## 2. 두산에너빌리티 2019Q1 (r20190515001464) — [연결] 손익계산서 EPS 계속/중단영업 세부내역 결측 (동일 패턴 재발)

- **회사**: 두산에너빌리티 (00159616) KOSPI 시총순위 11
- **보고서**: 2019Q1 분기보고서, 접수번호 20190515001464
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190515001464
- **CSV**: `layer2_review/KOSPI/00159616_두산에너빌리티/quarter/2019/20190515001464_review.csv`

**증상**: 위 이슈 1번(2019Q3, r20191114002511)과 완전히 동일한 패턴. [연결] 손익계산서에서
DART 원문은 "기본주당이익(손실)"과 "희석주당이익(손실)" 아래에 각각 "계속영업이익(손실)"과
"중단영업이익(손실)" 세부 항목을 공시하는데(3개월/누적 각각), DB에는 기본주당이익(손실) -366,
희석주당이익(손실) -366 (두 값 모두 3개월=누적, Q1이라 동일) 두 합계값만 적재되어 있고
세부 4개 항목이 빠져 있다.

**원문값 (DART, 제57기 1분기, 3개월=누적)**:
| 항목 | 3개월/누적 |
|---|---|
| 기본주당이익(손실) | (366) |
| 계속영업이익(손실) | (352) |
| 중단영업이익(손실) | (14) |
| 희석주당이익(손실) | (366) |
| 계속영업이익(손실) | (352) |
| 중단영업이익(손실) | (14) |

**DB 적재 상태**: 기본주당이익(손실) -366 / 희석주당이익(손실) -366 만 존재 (CSV rows 301-302,
[연결] 손익계산서 순번 22,23). 적재 행수 헤더 "[연결] IS 32"와 실제 카운트 일치 — 세부 4개
항목 누락 확인.

**참고**: [연결] 재무상태표(BS, 68개 항목)는 이 필링에서 전수 대조 완료, 결측/오류 0건.
IS는 위 EPS 세부내역 결측 발견 즉시 대조 중단(지시에 따라 CI/CF/SCE 및 [별도] 전체는 미대조).

**본 필링 처리**: `fail --note` 로 표시 후 다음 보고서로 진행 (파서 수정 없음). 이슈 1번과
동일 근본원인으로 추정되며, 두산에너빌리티의 다른 분기에도 반복될 가능성이 높음.

---

## 3. 두산에너빌리티 2019Q1 [기재정정] (r20190515002099) — 동일 패턴 재발 (이슈2와 같은날 정정신고서, 값 동일)

- **회사**: 두산에너빌리티 (00159616) KOSPI 시총순위 11
- **보고서**: 2019Q1 [기재정정]분기보고서, 접수번호 20190515002099 (같은날 원본 r20190515001464의 정정신고서)
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190515002099
- **CSV**: `layer2_review/KOSPI/00159616_두산에너빌리티/quarter/2019/20190515002099_review.csv`

**증상**: 이슈2와 완전히 동일. CSV 내용이 원본(r20190515001464)과 값 전부 동일(정정 사유는
재무제표 외 다른 항목으로 추정) — [연결] 손익계산서 EPS 계속영업/중단영업 세부내역
(기본/희석 각각 (352)/(14)) 4개 항목 결측, DB에는 기본주당이익(손실) -366 / 희석주당이익(손실)
-366 두 합계값만 존재. DART 원문 재확인 완료(표 순서만 앞쪽에 요약재무정보 표 추가되어 이동,
[연결] 손익계산서는 index7).

**본 필링 처리**: `fail --note` 로 표시 후 다음 보고서로 진행 (파서 수정 없음). 별도 원인조사
불필요 — 이슈1·2와 동일 근본원인.

## 4. 두산에너빌리티 2018FY 사업보고서 (r20190329003636) — [연결] 손익계산서 EPS 계속/중단영업 세부내역 결측 (동일 패턴 4번째 재발)

- 회사: 두산에너빌리티(00159616), KOSPI 시총순위11
- 보고서: 2018FY 사업보고서(2018.12), 접수번호 20190329003636, 접수일 2019-03-29
- DART: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190329003636
- CSV: layer2_review/KOSPI/00159616_두산에너빌리티/annual/2018/20190329003636_review.csv

### 증상
[연결] 손익계산서에서 DART 원문은 지배기업 소유주지분 주당손익 섹션에 다음과 같이 중첩 구조로 표시:

| 항목 | 제56기(당기) |
|---|---|
| 기본주당이익(손실) | (4,807) |
| ㄴ 계속영업이익(손실) | (4,167) |
| ㄴ 중단영업이익(손실) | (640) |
| 희석주당이익(손실) | (4,807) |
| ㄴ 계속영업이익(손실) | (4,167) |
| ㄴ 중단영업이익(손실) | (640) |

DB(CSV rows 343-344)에는 기본주당이익(손실) -4,807 / 희석주당이익(손실) -4,807 두 top-level 값만 적재되어 있고, 4개의 계속영업/중단영업 EPS 세부항목이 전부 결측.

### 확인 범위
- [연결] 재무상태표(66항목, CSV rows 253-321): DART 원문과 전체 대조 완료, 불일치 0건 (자산총계 24,814,911,563,566 / 부채총계 18,596,482,003,126 / 자본총계 6,218,429,560,440 모두 일치).
- [연결] 손익계산서(34항목, CSV rows 322-358): 매출액~비지배지분 귀속까지 전 항목 DART와 일치 확인. EPS 세부항목만 결측.
- CI, CF, SCE, [별도] 전체는 본 이슈 발견으로 대조 중단 (지침에 따름).

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈 #1(r20191114002511)·#2(r20190515001464)·#3(r20190515002099)와 동일한 근본원인으로 추정되는 4번째 재발 사례. 두산에너빌리티가 연결 손익계산서에 계속/중단영업 분리표시를 하는 모든 보고서에서 구조적으로 반복될 가능성이 높음.

## 5. 두산에너빌리티 2018FY [기재정정]사업보고서 (r20240327001230, 2024-03-27 재작성) — [연결] 손익계산서 EPS 계속/중단영업 세부내역 결측 (동일 패턴 5번째 재발)

- 회사: 두산에너빌리티(00159616), KOSPI 시총순위11
- 보고서: 2018FY [기재정정]사업보고서(2018.12), 접수번호 20240327001230, 접수일 2024-03-27 (5년 후 재작성 정정)
- DART: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240327001230
- CSV: layer2_review/KOSPI/00159616_두산에너빌리티/annual/2018/20240327001230_review.csv

### 증상
이슈#4(r20190329003636, 원본 2018FY)와 같은 결함이 5년 뒤 재작성된 정정신고서에도 그대로 재현됨. 재작성으로 수치 자체는 원본과 달라졌으나(계속영업손실 -333,447,080,055 vs 원본 -325,100,960,343 등 소급재작성 반영), 구조적 결함은 동일:

| 항목 | 제56기(당기) |
|---|---|
| 기본주당이익(손실) | (4,881) |
| ㄴ 계속영업이익(손실) | (4,241) |
| ㄴ 중단영업이익(손실) | (640) |
| 희석주당이익(손실) | (4,881) |
| ㄴ 계속영업이익(손실) | (4,241) |
| ㄴ 중단영업이익(손실) | (640) |

DB(CSV rows 343-344)에는 기본주당이익(손실) -4,881 / 희석주당이익(손실) -4,881 두 top-level 값만 적재, 4개 세부항목 결측.

### 확인 범위
- [연결] 손익계산서(34항목, CSV rows 322-358): 매출액~계속영업손실~중단영업이익~당기순이익 귀속까지 전 항목 DART와 일치. EPS 세부항목만 결측.
- [연결] BS, CI, CF, SCE, [별도] 전체는 발견 즉시 대조 중단 (지침에 따름). [별도] IS(rows 88-111)는 원본과 동일하게 계속영업/중단영업 분리 자체가 없어 이 결함의 영향을 받지 않는 구조로 보임(미대조, 참고만).

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#1~4와 동일 근본원인의 5번째 재발. 원본 필링뿐 아니라 그 정정본에도 동일하게 나타나는 것으로 보아, 이 결함은 특정 rcept의 XML 구조 이슈가 아니라 두산에너빌리티 [연결] IS에서 계속/중단영업이 분리 표시되는 모든 필링에 공통 적용되는 파서 레벨 결함으로 재확인됨.

## 6. 두산에너빌리티 2017FY [기재정정]사업보고서 (r20240327001228, 2024-03-27 재작성) — [연결] 손익계산서 EPS 계속/중단영업 세부내역 결측 (동일 패턴 6번째 재발)

- 회사: 두산에너빌리티(00159616), KOSPI 시총순위11
- 보고서: 2017FY [기재정정]사업보고서(2017.12), 접수번호 20240327001228, 접수일 2024-03-27 (원본 20180330001846의 재작성 정정)
- DART: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240327001228
- CSV: layer2_review/KOSPI/00159616_두산에너빌리티/annual/2017/20240327001228_review.csv

### 증상
이슈#4·#5(2018FY 원본/정정)와 같은 결함이 2017FY 정정신고서에도 그대로 재현됨. 재작성으로 수치는 원본(20180330001846)과 달라졌으나(계속영업손실 -172,278,830,188 등 소급재작성 반영), 구조적 결함은 동일:

| 항목 | 제55기(당기) |
|---|---|
| 기본주당이익(손실) | (3,737) |
| ㄴ 계속영업이익(손실) | (3,544) |
| ㄴ 중단영업이익(손실) | (193) |
| 희석주당이익(손실) | (3,737) |
| ㄴ 계속영업이익(손실) | (3,544) |
| ㄴ 중단영업이익(손실) | (193) |

DB(CSV rows 344-345)에는 기본주당이익(손실) -3,737 / 희석주당이익(손실) -3,737 두 top-level 값만 적재, 4개 세부항목 결측.

### 확인 범위
- [별도] 재무상태표(61항목)·손익계산서(CI 포함)·현금흐름표(46항목)·자본변동표(90항목): DART 원문과 전체 대조 완료, 불일치 0건. 재작성값(자산총계 11,610,156,476,366, 당기순이익 -111,837,764,505, 총포괄이익 69,644,418,161, 기말자본 4,340,584,828,540 등) 전부 DART와 정확히 일치 확인.
- [연결] 재무상태표(67항목, CSV rows 253-322): DART 원문과 전체 대조 완료, 불일치 0건 (자산총계 24,929,774,707,934 / 부채총계 18,452,088,873,921 / 자본총계 6,477,685,834,013 모두 일치, 미청구공사·투자부동산·순확정급여부채 등 재구성된 항목 포함).
- [연결] 손익계산서(매출액~비지배지분 귀속까지, CSV rows 323-343): DART와 일치 확인. EPS 세부항목만 결측.
- [연결] CI, CF, SCE는 본 이슈 발견으로 대조 중단 (지침에 따름). 원본 필링(r20180330001846)은 이 세션에서 이미 8개 섹션 전체 PASS 처리됨 — 원본에는 이 결함이 없었던 것으로 보이나(원본 IS는 EPS 계속/중단 세부내역 자체가 별도로 없는 구조), 정정본에서 새로 도입된 계속/중단영업 분리표시 구조에 결함이 적용된 것으로 추정.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#1~5와 동일 근본원인의 6번째 재발.

---

## 7. 신한지주 2021FY 사업보고서 (r20220316000748) — [연결]·[별도] 자본변동표 당기(2021) 롤포워드 섹션 통째로 결측

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2021FY 사업보고서, 접수번호 20220316000748
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20220316000748
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/annual/2021/20220316000748_review.csv`

**증상**: 이 필링은 오래된(2022년) DART 레이아웃으로, 자본변동표가 3개년 비교기간을
한 표에 담지 못하고 **별도의 `<table>` 엘리먼트 2개로 분할**되어 렌더링된다(연결재무제표
앵커 아래 tables[5]=2019.01.01→2020.12.31 구간, tables[6]=2020.01.01→2021.12.31(당기말)
구간). DB에는 tables[5](2019→2020 구간)만 적재되어 있고, **tables[6] 전체(당기 2021년
롤포워드 섹션)가 완전히 빠져 있다** — [별도]·[연결] 양쪽 모두 동일하게 결측.

**확인 방법**: 재무상태표(BS)의 자본총계가 자본변동표(SCE) 마지막 행(기말자본)의 총계와
반드시 일치해야 하는데, 이 필링에서는 불일치했다:
- [별도] BS 자본총계 = 26,405,376 (CSV) ↔ DB에 적재된 [별도] SCE 마지막 행("Ⅳ. 2020년
  12월 31일 (전기말)") 총계 = 25,057,097 — **불일치**.
- [연결] BS 자본총계 = 49,538,422 (CSV) ↔ DB에 적재된 [연결] SCE 마지막 행 총계 =
  46,356,858 — **불일치**.

DART 원문에서 tables[6]("Ⅳ. 2021년 12월 31일 (당기말)")의 실제 값을 확인한 결과:
- [별도] tables[6] 마지막 행 총계 = **26,405,376** → BS 자본총계와 정확히 일치.
- [연결] tables[6] 마지막 행 총계 = **49,538,422** → BS 자본총계와 정확히 일치.

즉 DART 원문에는 당기(2021) 롤포워드 섹션이 존재하고 BS와 정합하지만, DB에는 이 섹션
자체가 통째로 빠져 있다. 적재 행수 헤더("[별도] SCE 83 / [연결] SCE 149")도 tables[5]
하나 분량과 일치하여, tables[6]이 처음부터 로딩되지 않았음을 뒷받침한다.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/39항목)·손익계산서(IS 21/46항목)·현금흐름표(CF 54/79항목):
  DART 원문과 전체 항목 대조 완료, 불일치 0건(자산총계·부채총계·자본총계·당기순이익·
  영업/투자/재무활동현금흐름·기말현금 등 전부 일치).
  - (참고: 대조 스크립트가 원문의 "-"(제로 표기)를 잘못 처리해 일부 항목에서 다른 연도
    값을 잘못 집어오는 자체 버그가 있었음 — DART 표에서 "-"를 재확인해 이 문제가 스크립트
    쪽 결함이며 DB 값 자체는 정확함을 확인함. DB 결함 아님.)
- [별도]·[연결] 자본변동표(SCE): tables[5](2019→2020 구간)는 DB 값과 DART 원문이 정확히
  일치(첫 행·마지막 행 값 확인). tables[6](2020→2021 당기 구간)은 DB에 존재하지 않음.

**추정 원인(미확인, 코드 미조사)**: 오래된 DART 레이아웃에서 자본변동표가 페이지 폭 제약으로
2개의 별도 `<table>`로 분할 렌더링되는 경우, 파서가 첫 번째 table만 인식하고 두 번째
table(당기 롤포워드)을 놓치는 것으로 추정. 유사 레이아웃을 쓰는 다른 회사/연도 필링에도
동일 패턴이 있을 가능성 있음 — 캠페인 종료 후 전사 스캔 필요.

### 처리
- FAIL 처리, 파서 코드 수정 없음.

---

## 8. 신한지주 2018FY 사업보고서 (r20190401004307) — [연결]·[별도] 자본변동표 당기(2018) 롤포워드 섹션 통째로 결측 (이슈#7과 동일 패턴 재발)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2018FY 사업보고서, 접수번호 20190401004307
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190401004307
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/annual/2018/20190401004307_review.csv`

**증상**: 이슈#7과 완전히 동일한 근본원인. 자본변동표가 DART 원문에서 두 개의 별도
`<table>` 엘리먼트로 분할 렌더링되고(연결재무제표 앵커(`13_anchor`) 아래 tables[5]=
2017.01.01→2017.12.31 구간, tables[6]=2018.01.01→2018.12.31(당기) 구간; 재무제표 앵커
(`15_anchor`) 아래도 동일하게 tables[5]/tables[6]로 분할), DB에는 tables[5](2017년 구간)만
적재되고 **tables[6](당기 2018년 롤포워드 구간)이 통째로 결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계가 불일치함을 먼저 확인하고, DART 원문
tables[6] 마지막 행과 대조해 정합함을 확인:
- [별도] BS 자본총계 = 21,650,068 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일") 총계 =
  20,192,078 — 불일치. DART 원문 tables[6] 마지막 행("Ⅴ.2018년12월31일(당기말)") 총계 =
  **21,650,068** → BS와 정확히 일치.
- [연결] BS 자본총계 = 36,651,430 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일") 총계 =
  33,703,617 — 불일치. DART 원문 tables[6] 마지막 행("Ⅴ.2018년12월31일(당기말)") 총계 =
  **36,651,430** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/46항목)·손익계산서(IS 22/51항목)·현금흐름표(CF 50/88항목):
  DART 원문과 전체 항목 프로그램 대조 완료, 불일치 0건.
- [별도]·[연결] 자본변동표(SCE): tables[5](2017년 구간)는 DB 값과 일치. tables[6](2018년
  당기 구간)은 DB에 존재하지 않음 — 이슈#7과 동일한 결함.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7과 동일 근본원인의 2번째 재발(신한지주 내에서만 2건째 — 유사 레이아웃을 쓰는 다른
  회사/연도 필링에도 있을 가능성 있음, 캠페인 종료 후 전사 스캔 필요).

---

## 9. 신한지주 2018FY [기재정정]사업보고서 (r20190705000438) — 이슈#8과 동일 패턴 재발 (원본의 정정신고서)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2018FY [기재정정]사업보고서, 접수번호 20190705000438 (원본 r20190401004307의 정정신고서)
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190705000438
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/annual/2018/20190705000438_review.csv`

**증상**: CSV 데이터가 원본(r20190401004307)과 완전히 동일(`diff` 결과 0 — 정정 사유는
재무제표 외 다른 항목으로 추정). 이슈#8과 완전히 동일한 결함: [연결]/[별도] 자본변동표가
두 개의 `<table>`로 분할 렌더링되고(`15_anchor`=연결재무제표 tables[5]/[6], `17_anchor`=
재무제표 tables[5]/[6]) 두번째 테이블(당기 2018년 롤포워드)이 DB에 결측.

**확인 방법**: DART 원문 tables[6] 마지막 행 재확인:
- [연결] "Ⅴ. 2018년 12월 31일 (당기말)" 총계 = **36,651,430** → BS 자본총계와 정확히 일치.
- [별도] "Ⅴ. 2018년 12월 31일 (당기말)" 총계 = **21,650,068** → BS 자본총계와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/46항목)·손익계산서(IS 22/51항목)·현금흐름표(CF 50/88항목):
  DART 원문과 전체 항목 프로그램 대조 완료, 불일치 0건(원본 이슈#8에서 이미 검증된 값과
  동일함을 재확인).
- [별도]·[연결] 자본변동표(SCE): tables[5](2017년 구간)는 DB 값과 일치. tables[6](2018년
  당기 구간)은 DB에 존재하지 않음 — 이슈#8과 동일한 결함.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~8과 동일 근본원인의 3번째 재발(원본과 정정신고서 양쪽 모두에서 재현되는 것으로
  보아, 특정 rcept의 개별 이슈가 아니라 이 시기 DART 레이아웃 전반에 적용되는 파서 레벨
  결함으로 재확인됨). 이슈#7 문서 참고.

---

## 10. 신한지주 2021Q3 분기보고서 (r20211115002302) — [연결]·[별도] 자본변동표 당기(2021.09) 롤포워드 섹션 통째로 결측 (이슈#7~9와 동일 패턴, 최초의 분기보고서 사례)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2021Q3 분기보고서, 접수번호 20211115002302
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20211115002302
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/quarter/2021/20211115002302_review.csv`

**증상**: 이슈#7~9(사업보고서)와 동일한 근본원인이 분기보고서에서도 재현됨. [연결]재무제표
앵커(`17_anchor`) 아래 자본변동표가 두 개의 `<table>`로 분할 렌더링되고(tables[5]=
2019.01.01→2020.12.31 구간, tables[6]=2020.01.01(전분기초)→2021.09.30(당분기말) 구간),
[별도]재무제표 앵커(`19_anchor`)도 동일 구조(tables[5]/[6]). DB에는 tables[5]만 적재되고
**tables[6](당기 2021.09 말까지의 롤포워드 구간)이 통째로 결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계가 불일치함을 먼저 확인하고, DART 원문
tables[6] 마지막 행과 대조해 정합함을 확인:
- [별도] BS 자본총계 = 26,628,170 (CSV) ↔ DB SCE 마지막 행("Ⅳ. 2020년 12월 31일 (전기말)")
  총계 = 25,057,097 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 9월 30일
  (당분기말)") 총계 = **26,628,170** → BS와 정확히 일치.
- [연결] BS 자본총계 = 49,415,744 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2020년 12월 31일 (전기말)")
  총계 = 46,356,858 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 9월 30일
  (당분기말)") 총계 = **49,415,744** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/39항목)·손익계산서(포괄손익 포함 IS 21/46항목)·
  현금흐름표(CF 54/79항목): DART 원문과 전체 항목 프로그램 대조 완료, 불일치 0건
  (자산총계·부채총계·자본총계·영업/투자/재무활동현금흐름·기말현금 등 전부 일치).
- [별도]·[연결] 자본변동표(SCE): tables[5](2019→2020 구간)는 DB 값과 DART 원문이 일치
  (첫 행·마지막 행 확인). tables[6](2020.01→2021.09 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~9와 동일 근본원인의 4번째 재발이자 **최초로 확인된 분기보고서(분기별 SCE) 사례**
  — 지금까지는 사업보고서(연간)에서만 확인되었으나, 이번 건으로 이 결함이 SCE가 2개
  table로 분할되는 모든 보고서 유형(사업보고서/분기보고서 불문)에 공통 적용되는 파서 레벨
  결함임이 재확인됨. 신한지주의 다른 분기/반기보고서에도 동일 패턴이 있을 가능성이 매우 높음.

---

## 11. 신한지주 2021H1 반기보고서 (r20210817001629) — [연결]·[별도] 자본변동표 당기(2021.06) 롤포워드 섹션 통째로 결측 (이슈#7~10과 동일 패턴, 5번째 재발/두번째 비연간 보고서 사례)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2021H1 반기보고서, 접수번호 20210817001629
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20210817001629
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/half/2021/20210817001629_review.csv`

**증상**: 이슈#7~10과 완전히 동일한 근본원인. [연결]/[별도] 재무제표 앵커(`17_anchor`/
`19_anchor`) 아래 자본변동표가 tables[5](2019→2020 구간)/tables[6](2020.01→2021.06
당반기말 구간)로 분할 렌더링되는데, DB에는 tables[5]만 적재되고 **tables[6](당기
2021.06말까지의 롤포워드 구간)이 통째로 결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계 불일치 확인 후 DART 원문 tables[6]과
대조:
- [별도] BS 자본총계 = 26,842,076 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2020년 12월 31일 (전기말)")
  총계 = 25,057,097 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 06월 30일
  (당반기말)") 총계 = **26,842,076** → BS와 정확히 일치.
- [연결] BS 자본총계 = 48,466,392 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2020년 12월 31일 (전기말)")
  총계 = 46,356,858 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 6월 30일
  (당반기말)") 총계 = **48,466,392** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/39항목)·손익계산서(포괄손익 포함 IS 21/46항목)·
  현금흐름표(CF 53/79항목): DART 원문과 전체 항목 프로그램 대조 완료, 불일치 0건.
- [별도]·[연결] 자본변동표(SCE): tables[5](2019→2020 구간)는 DB 값과 DART 원문 일치.
  tables[6](2020.01→2021.06 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~10과 동일 근본원인의 5번째 재발. 신한지주에서 사업보고서 2건·분기보고서 1건에
  이어 반기보고서에서도 확인됨 — 신한지주가 제출하는 이 시기 모든 정기보고서 유형에
  구조적으로 공통 적용되는 결함으로 사실상 확정.

---

## 12. 신한지주 2021Q1 분기보고서 (r20210517001885) — [연결]·[별도] 자본변동표 당기(2021.03) 롤포워드 섹션 통째로 결측 (이슈#7~11과 동일 패턴, 6번째 재발)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2021Q1 분기보고서, 접수번호 20210517001885
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20210517001885
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/quarter/2021/20210517001885_review.csv`

**증상**: 이슈#7~11과 완전히 동일한 근본원인. 이번 필링은 앵커 번호가 다른 세대
(`14_anchor`=연결재무제표, `16_anchor`=재무제표)이지만 동일한 구조적 결함이 재현됨.
[연결]재무제표 앵커 아래 자본변동표가 tables[5](2019→2020 구간)/tables[6](2020.01
(전분기초)→2021.03.31(당분기말) 구간)로 분할 렌더링되고, [별도]재무제표 앵커도 동일 구조
(tables[5]/[6]). DB에는 tables[5]만 적재되고 **tables[6](당기 2021.03 말까지의 롤포워드
구간)이 통째로 결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계 불일치 확인 후 DART 원문 tables[6]과
대조:
- [별도] BS 자본총계 = 26,147,753 (CSV) ↔ DB SCE 마지막 행("Ⅳ. 2020년 12월 31일 (전기말)")
  총계 = 25,057,097 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 03월 31일
  (당분기말)") 총계 = **26,147,753** → BS와 정확히 일치.
- [연결] BS 자본총계 = 46,742,592 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2020년 12월 31일 (전기말)")
  총계 = 46,356,858 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2021년 3월 31일
  (당분기말)") 총계 = **46,742,592** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/39항목)·손익계산서(포괄손익 포함 IS 21/46항목)·
  현금흐름표(CF 54/79항목): DART 원문과 전체 항목 프로그램 대조 완료, 불일치 0건.
- [별도]·[연결] 자본변동표(SCE): tables[5](2019→2020 구간)는 DB 값과 DART 원문 일치.
  tables[6](2020.01→2021.03 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~11과 동일 근본원인의 6번째 재발. 앵커 번호 세대가 달라져도(17/19 → 14/16)
  동일 구조 결함이 재현되는 것을 확인 — 앵커 번호가 아니라 SCE가 2개 table로 분할되는
  DART 레이아웃 패턴 자체가 결함의 근본 트리거임을 재확인.

---

## 13. 신한지주 2020FY 사업보고서 (r20210317001127) — [연결]·[별도] 자본변동표 당기(2020.12) 롤포워드 섹션 통째로 결측 (이슈#7~12와 동일 패턴, 7번째 재발)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2020FY 사업보고서, 접수번호 20210317001127
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20210317001127
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/annual/2020/20210317001127_review.csv`

**증상**: 이슈#7~12와 완전히 동일한 근본원인. [연결]재무제표 앵커(`14_anchor`) 아래
자본변동표가 tables[5](2018→2019 구간)/tables[6](2019.01(전기초)→2020.12.31(당기말)
구간)로 분할 렌더링되고, [별도]재무제표 앵커(`16_anchor`)도 동일 구조(tables[5]/[6]).
DB에는 tables[5]만 적재되고 **tables[6](당기 2020.12 말까지의 롤포워드 구간)이 통째로
결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계 불일치 확인 후 DART 원문 tables[6]과
대조:
- [별도] BS 자본총계 = 25,057,097 (CSV) ↔ DB SCE 마지막 행("Ⅲ. 2019년 12월 31일")
  총계 = 22,465,426 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2020년 12월 31일
  (당기말)") 총계 = **25,057,097** → BS와 정확히 일치.
- [연결] BS 자본총계 = 46,356,858 (CSV) ↔ DB SCE 마지막 행("Ⅲ.2019년 12월 31일")
  총계 = 41,930,381 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ.2020년 12월 31일
  (당기말)") 총계 = **46,356,858** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 26/39항목): DART 원문과 전체 항목 대조 완료, 불일치 0건
  (자산총계·부채총계·자본총계 전부 일치, 자동검산 bs_balance PASS).
- [별도]·[연결] 손익계산서·현금흐름표: 자동검산(cf_closing_cash) PASS, 행수 정상.
- [별도]·[연결] 자본변동표(SCE): tables[5](2018→2019 구간)는 DB 값과 DART 원문 일치.
  tables[6](2019.01→2020.12 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~12와 동일 근본원인의 7번째 재발. 신한지주가 제출한 이 시기 정기보고서
  (사업보고서 3건·분기보고서 2건·반기보고서 1건, 이번 건 포함 총 7건)에서 예외 없이
  재현 — 개별 필링 결함이 아니라 신한지주의 이 SCE 2-table 분할 레이아웃 자체에 내재된
  파서 레벨 구조적 결함으로 확정.

---

## 14. 신한지주 2018Q3 분기보고서 (r20181114002167) — [연결]·[별도] 자본변동표 당기(2018.09) 롤포워드 섹션 통째로 결측 (이슈#7~13과 동일 패턴, 8번째 재발, 최초 확인시점 2018년으로 확장)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2018Q3 분기보고서, 접수번호 20181114002167
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20181114002167
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/quarter/2018/20181114002167_review.csv`

**증상**: 이슈#7~13과 완전히 동일한 근본원인. [연결]재무제표 앵커(`13_anchor`) 아래
자본변동표가 tables[5](~2017.12 구간)/tables[6](2018.01(당분기초)→2018.09.30(당분기말)
구간)로 분할 렌더링되고, [별도]재무제표 앵커(`15_anchor`)도 동일 구조(tables[5]/[6]).
DB에는 tables[5]만 적재되고 **tables[6](당기 2018.09 말까지의 롤포워드 구간)이 통째로
결측**. 이 필링은 지금까지 확인된 것 중 가장 이른 시점(2018년) 사례.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계 불일치 확인 후 DART 원문 tables[6]과
대조:
- [별도] BS 자본총계 = 21,842,882 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일")
  총계 = 20,192,078 — 불일치. DART 원문 tables[6] 마지막 행("Ⅳ. 2018년 09월 30일
  (당분기말)") 총계 = **21,842,882** → BS와 정확히 일치.
- [연결] BS 자본총계 = 36,016,509 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일")
  총계 = 33,702,536 — 불일치. DART 원문 tables[6] 마지막 행("Ⅴ. 2018년 9월 30일
  (당분기말)") 총계 = **36,016,509** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/47항목): 자동검산(bs_balance) PASS, 자본총계는 위와
  같이 DART 원문과 직접 대조 완료.
- [별도]·[연결] 손익계산서·현금흐름표: 자동검산(cf_closing_cash) PASS, 행수 정상
  (IS 22/51, CF 50/88).
- [별도]·[연결] 자본변동표(SCE): tables[5](~2017.12 구간)는 DB 값과 DART 원문 일치.
  tables[6](2018.01→2018.09 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~13과 동일 근본원인의 8번째 재발. 확인 시점이 2018Q3까지 확장되어 신한지주의
  이 SCE 2-table 분할 레이아웃 구조적 결함이 최소 2018~2021년 전 기간에 걸쳐 있음을
  시사.
- ★사용자 지시(2026-09-20): 동일한 형태(이슈#7~13과 동일 근본원인)의 문제이므로 이번
  건부터는 camp_err_review 세션에 별도 재통지하지 않음 — 이슈 문서 기록만 유지.
- ★참고: 이슈#14 기록 직후 camp_err_review 세션(camp-err-review-c2)으로부터 R148
  근본원인 수정 완료 통지 수신(커밋 `99c5b95`, `fin2/extract/text.py::_detect_body_
  statement_tables()` — 표지 문구 없이 SCE가 물리적 TABLE 2개로 쪼개지는 서식에서
  forward-scan이 첫 데이터표만 채택 후 break하던 버그, 표 내용 판정만으로 직전 SCE를
  물려받도록 수정). 이슈#7~13은 이미 그쪽에서 재적재+pass 처리 완료. 본 세션은 캠페인
  전용이라 cherry-pick은 하지 않음(사용자 지시: 파서 코드 수정 금지) — main 반영은
  사용자/camp_err_review 쪽에서 진행.

---

## 15. 신한지주 2018H1 반기보고서 (r20180814002280) — [연결]·[별도] 자본변동표 당기(2018.06) 롤포워드 섹션 통째로 결측 (이슈#7~14와 동일 패턴, 9번째 재발, R148로 근본원인 해결됨 — 별도 재통지 없음)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2018H1 반기보고서, 접수번호 20180814002280
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20180814002280
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/half/2018/20180814002280_review.csv`

**증상**: 이슈#7~14와 완전히 동일한 근본원인(R148로 이미 근본수정됨). [연결]재무제표
앵커(`13_anchor`) 아래 자본변동표가 tables[5](~2017.12 구간)/tables[6](2018.01(당반기초)
→2018.06.30(당반기말) 구간)로 분할 렌더링되고, [별도]재무제표 앵커(`15_anchor`)도 동일
구조(tables[5]/[6]). DB에는 tables[5]만 적재되고 **tables[6](당기 2018.06 말까지의
롤포워드 구간)이 통째로 결측**.

**확인 방법**: BS 자본총계와 DB SCE 마지막 행 총계 불일치 확인 후 DART 원문 tables[6]과
대조:
- [별도] BS 자본총계 = 20,966,964 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일")
  총계 = 20,192,078 — 불일치. DART 원문 tables[6] 마지막 행("Ⅴ. 2018년 06월 30일
  (당반기말)") 총계 = **20,966,964** → BS와 정확히 일치.
- [연결] BS 자본총계 = 34,298,893 (CSV) ↔ DB SCE 마지막 행("Ⅳ.2017년 12월 31일")
  총계 = 33,702,536 — 불일치. DART 원문 tables[6] 마지막 행("Ⅴ. 2018년 6월 30일
  (당반기말)") 총계 = **34,298,893** → BS와 정확히 일치.

**확인 범위**:
- [별도]·[연결] 재무상태표(BS 25/47항목): 자동검산(bs_balance) PASS, 자본총계는 위와
  같이 DART 원문과 직접 대조 완료.
- [별도]·[연결] 손익계산서·현금흐름표: 자동검산(cf_closing_cash) PASS, 행수 정상
  (IS 21/53, CF 48/87).
- [별도]·[연결] 자본변동표(SCE): tables[5](~2017.12 구간)는 DB 값과 DART 원문 일치.
  tables[6](2018.01→2018.06 당기 구간)은 DB에 존재하지 않음.

### 처리
- FAIL 처리, 파서 코드 수정 없음.
- 이슈#7~14와 동일 근본원인의 9번째 재발. R148로 근본원인이 이미 확인·수정되어
  camp_err_review 쪽에서 반영 예정이므로, 사용자 지시에 따라 이번 건은 별도 재통지
  없이 이슈 문서 기록만 남김.

---

## 16. 신한지주 2022Q1 분기보고서 (r20220516002487) — [별도]·[연결] 손익계산서 EPS(주당이익) 섹션 전체 결측 (신규 패턴 — 이슈#1의 R147과 다름, camp_err_review 통지 대상)

- **회사**: 신한지주 (00382199) KOSPI 시총순위 12
- **보고서**: 2022Q1 분기보고서, 접수번호 20220516002487
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20220516002487
- **CSV**: `layer2_review/KOSPI/00382199_신한지주/quarter/2022/20220516002487_review.csv`

**증상**: DART 원문 [별도]·[연결] 손익계산서 모두 "Ⅷ. 주당이익" 섹션 아래 "기본 및
희석주당이익" 행(값이 존재하는 EPS 항목)을 공시하는데, DB/CSV에는 [별도]·[연결] 어느
쪽에도 "주당" 문자열을 포함한 행이 **단 한 개도 없음**(CSV 전체를 `grep -n "주당"`으로
확인, 0건). 이는 이슈#1(두산에너빌리티, R147로 수정됨)의 "계속영업/중단영업 세부행만
결측"과 달리, **EPS 섹션 전체가 통째로 사라진** 더 넓은 범위의 신규 결함 패턴이다.

**원문값 (DART, 제22기 1분기)**:
- [별도] 손익계산서: "Ⅷ. 주당이익" (헤더행, 값 없음) → "기본 및 희석주당이익 | 2,564원"
- [연결] 손익계산서: 별도로 확인 필요하나(연결 IS는 EPS행 존재 여부를 별도로 직접
  스크린샷 확인하지 않았음 — 별도 IS와 동일 레이아웃 패턴이므로 존재할 개연성 높음),
  CSV 쪽은 [연결]도 "주당" 0건으로 동일하게 결측 확인됨.

**DB 적재 상태**: [별도] 손익계산서 21개 항목(Ⅰ. 영업이익 ~ Ⅶ. 총포괄이익)까지는
DART 원문과 값 전부 정확히 일치. 그 다음에 이어져야 할 "Ⅷ. 주당이익"/"기본 및
희석주당이익" 행이 없이 바로 [별도] 현금흐름표로 넘어감. [연결] 쪽도 동일 구조로
EPS행 없이 바로 다음 표로 넘어감. CSV 헤더의 적재 행수 카운트도 EPS 없이 집계된
숫자와 일치(별도 IS 21, 연결 IS 46).

**타사 대조**: 두산에너빌리티 등 다른 회사 리뷰 CSV에는 EPS 행이 정상적으로 존재함을
이번 세션에서 확인 — 전사(全社) 공통 파이프라인 붕괴가 아니라 회사/레이아웃별 특이
케이스로 보임. 이 세션에서 확인한 신한지주 CSV 9건 전부(quarter/half/annual 혼합)
EPS 행이 0건이었음 — 신한지주 전체 기간에 걸친 구조적 결함으로 추정.

**추정 원인(미확인, 코드 미조사 — 파서 코드 수정 금지 지시에 따라 조사만, 수정 안 함)**:
DART 원문의 기간-컬럼 헤더는 컬럼당 2개의 물리 `<td>`(colspan=2)로 되어 있고, 리프
행은 첫 번째 서브컬럼에, 부모/합계 성격의 행은 두 번째 서브컬럼에 값이 들어가는
구조적 함정이 있다(이번 세션 전체 대조 과정에서 관찰됨). "기본 및 희석주당이익" 행이
이 두 번째 서브컬럼 위치(합계성 행 취급)에 값을 갖고 있을 가능성이 있고,
`fin2/extract/report_lines.py::_emit_eps_lines()`의 컬럼선택 로직
(`header_cols`/`cum_map`/포지셔널 폴백)이 이 경우 "짝을 못 찾음 = EPS 섹션 헤더만
있고 데이터 없음"으로 오판해 행 전체를 드롭했을 가능성. 미확인 가설이며 코드 조사·
수정은 이 세션 범위 밖.

**확인 범위**: 이 필링([별도]·[연결] BS/IS/CF 전 항목 값 + SCE 롤포워드 경계값)은
EPS를 제외하고 전부 DART 원문과 정확히 일치 확인 완료(R148 재적재 정상 동작 확인
포함). EPS 결측만 유일한 결함.

### 처리
- 신규 패턴(이슈#1의 R147과 범위가 다름)이므로 프로토콜에 따라 camp_err_review
  세션(camp-err-review-c2)에 통지함. 이 접수번호는 통지 응답 또는 5분 타임아웃 전까지
  pass/fail 처리 보류.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

## 17. SK스퀘어 2021FY 사업보고서 제1기 (r20220317000691) — [별도] 손익계산서 EPS(주당손실) 행 결측 (이슈#16과 동일 패턴 재발 — 원인가설 실측 확인됨, 재notify 불필요)

- **회사**: SK스퀘어 (01596425) KOSPI 시총순위 3
- **보고서**: 2021FY 사업보고서 (제1기, 분할설립일 2021.11.01~2021.12.31), 접수번호 20220317000691
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20220317000691
- **CSV**: `layer2_review/KOSPI/01596425_SK스퀘어/annual/2021/20220317000691_review.csv` (이미 fail 처리로 보존)

**증상**: DART 원문 [별도] 손익계산서에 "Ⅶ.주당손실" 헤더 + "기본 및 희석주당순손실
(89원)" 행이 존재하는데, CSV/DB에는 [별도] 손익계산서가 "Ⅵ.당기순손실"에서 끝나고
바로 포괄손익 섹션(Ⅰ.당기순손실 재시작)으로 넘어감 — EPS 섹션(헤더+데이터 행)이
통째로 없음. 이슈#16(신한지주)과 동일한 결함 클래스.

**원문 셀 구조 실측 (`document.getElementById('ifrm').contentDocument`로 직접 확인)**:
```
row14 cells=3 :: "Ⅵ.당기순손실\n" | "\n" | "(12,529)"       ← 합계행, 값이 3번째 셀(col idx2)
row15 cells=3 :: "Ⅶ.주당손실"     | "\n" | "\n"              ← 헤더행, 값 없음
row16 cells=3 :: "  기본 및 희석주당순손실" | "　"(공백) | "(89원)"  ← 리프행인데 값이 3번째 셀(col idx2)
```
다른 정상 리프 행(예: row4 `종업원급여 | 4,752 | \n`)은 값이 **2번째 셀(col idx1)**에
있는데, EPS 리프 행(row16)만 유일하게 합계행과 같은 **3번째 셀(col idx2)**에 값이
들어가 있음 — 이슈#16에서 미확인 가설로 제시했던 "EPS 리프 행이 합계행 컬럼 위치에
값을 가져 컬럼선택 로직이 오판, 행 전체 드롭"을 이 필링에서 **원문 DOM 구조로 직접
실측 확인**함(이슈#16 당시엔 화면 렌더링만 보고 셀 인덱스까지는 실측하지 않았음).

**값 서식 추가 특징**: "(89원)" — 괄호 음수 + 원 단위 셀 내장(신한지주 사례 "2,564원"은
양수·비괄호였음), "기본 및 희석주당순손실"로 기본/희석이 한 행에 합쳐진 표기(연결
쪽은 이 필링에서도 "기본주당이익"/"희석주당이익" 두 행으로 분리돼 있고 정상 적재됨 —
같은 필링 내에서 연결 IS EPS는 정상, 별도 IS EPS만 결측).

**확인 범위**: 이 필링의 나머지 전 범위(별도 BS/CF/SCE, 연결 BS/IS/CF/SCE 전체,
연결 IS의 EPS 포함)는 DART 원문과 전부 일치 확인 완료. 별도 IS의 EPS 행 결측만
유일한 결함.

### 처리
- 이슈#16과 동일 결함 클래스(EPS 섹션 결측)이므로 프로토콜에 따라 재notify하지 않고
  문서에만 기록.
- `python scripts/layer2_review.py fail --note "[별도] 손익계산서 EPS(기본 및
  희석주당순손실 (89원)) 행 결측 — 이슈#17/#16과 동일 패턴, 리프행 값이 합계행 컬럼
  위치(col idx2)에 있어 컬럼선택 로직이 드롭 추정"` 로 처리.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

## 18. KB금융 2018H1 반기보고서 (r20180814002480) — [별도] 자본변동표 당기(2018.06) 롤포워드 물리분할 표 전체 결측 (R148과 동일 결함 메커니즘이나 ★최초로 비-신한지주 회사에서 확인 + ★R148 수정 커밋(15:17) 이후 재적재(17:51)한 데이터에서도 재현됨)

- **회사**: KB금융 (00688996) KOSPI 시총순위 8
- **보고서**: 2018H1 반기보고서 (2018.06), 접수번호 20180814002480
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20180814002480
- **CSV**: `layer2_review/KOSPI/00688996_KB금융/half/2018/20180814002480_review.csv`

**증상**: DART 원문 [별도] 재무제표 섹션에서 자본변동표가 물리적으로 2개의 `<table>`
로 쪼개져 있다(표지 문구 없음, 이슈#7~15의 신한지주와 동일한 서식):
- `tables[5]` (19행): 2016.1.1(전전기초) → 2017.12.31(전기말) — 비교기간
- `tables[6]` (17행): 2017.1.1(전기초) → **2018.6.30(당반기말)** — 당기 롤포워드

CSV/DB의 [별도] 자본변동표(45행)는 `tables[5]`만 적재되어 있고 `tables[6]`
(당기 롤포워드 17행 전체)이 통째로 빠져 있다. 마지막 적재 행이 "2017.12.31(전기말)
19,126,545"에서 끝나며, [별도] 재무상태표 자본총계(19,189,484, 2018.6.30 기준)와
불일치 — SCE 마지막 행 총계가 BS 자본총계와 다르다는 자동검산(ORPHAN_BODY_TABLE)이
이를 정확히 포착함.

**원문 `tables[6]` 전체 내용(DART에서 직접 추출)**:
```
2017.1.1(전기초) | 2,090,558 | 14,656,168 | (4,742) | 2,998,923 | (719,090) | 19,021,817
...(중간 생략: 분기순이익 640,316 / 순확정급여부채 재측정요소 (56) / 연차배당 (497,969) / 자기주식취득 (82,111))
2017.6.30(전반기말) | 2,090,558 | 14,656,168 | (4,798) | 3,141,270 | (801,201) | 19,081,997
2018.1.1(당기초) | 2,090,558 | 14,742,814 | (5,233) | 3,054,379 | (755,973) | 19,126,545
...(반기순이익 1,009,961 / 순확정급여부채 재측정요소 (73) / 연차배당 (766,728) / 자기주식취득 (180,221))
2018.6.30(당반기말) | 2,090,558 | 14,742,814 | (5,306) | 3,297,612 | (936,194) | 19,189,484
```
마지막 행(2,090,558 / 14,742,814 / -5,306 / 3,297,612 / -936,194 / 19,189,484)은
[별도] 재무상태표 자본금/자본잉여금/기타포괄손익누계액/이익잉여금/자기주식/자본총계와
정확히 일치 — 이 표가 진짜 당기 SCE임을 확인.

**★두 가지 신규 포인트**:
1. **비-신한지주 최초 사례**: 이슈#7~15는 전부 신한지주였음. R148 수정(`_looks_like_
   equity_changes_header()`)이 "신한지주 8건 백필 완료"로 종료 처리됐는데, 이번 건으로
   이 SCE 물리분할 결측 패턴이 신한지주 국한이 아니라 **은행지주 업종 전반(혹은 특정
   DART 렌더 엔진 버전) 공통 서식 이슈**일 가능성이 제기됨 — 전사 census 필요 시사.
2. **R148 수정 커밋 이후 재적재에서도 재현**: 이 파일의 CSV 헤더 `# 재적재,2026-09-20
   17:51:32`는 R148 수정 커밋(`99c5b95`, 2026-09-20 15:17:09)보다 **2시간 이상 이후**
   시점이다. 즉 이 재적재는 R148이 수정된 코드로 실행됐을 가능성이 높음에도 결함이
   그대로 재현됨 — `_looks_like_equity_changes_header()` 규칙이 KB금융의 이 표 변형
   (컬럼 6개: 자본금/자본잉여금/기타포괄손익누계액/이익잉여금/자기주식/총계, 신한지주와
   컬럼 구성이 다름)은 못 잡는 것으로 추정됨(코드 미조사, 이 세션은 파서 코드 접근 안 함).
3. 참고로 **같은 필링의 [연결] 자본변동표(tables[5]/[6], 205행)는 두 표 모두 정상
   적재됨** — 당기 롤포워드(2018.6.30 당반기말)까지 CSV에 전부 존재하고 DART 원문과
   정확히 일치. 즉 이 필링 안에서도 [별도]만 결측, [연결]은 정상 — 비대칭적 발현.

**확인 범위**: 이 필링의 나머지 전 범위(별도 BS/IS/CF, 연결 BS/IS/CF/SCE 전체)는
DART 원문과 전부 일치 확인 완료(EPS 포함, 별도/연결 모두 정상 적재됨 — R149 적용 후
재적재라 원 단위 EPS도 정상). [별도] 자본변동표의 당기 롤포워드 표 결측만 유일한 결함.

### 처리
- R148과 동일한 결함 메커니즘(SCE 물리분할, 표지 문구 없음)이지만, (a) 비-신한지주
  최초 사례이고 (b) R148 수정 커밋 이후 재적재본에서도 재현된다는 점이 backfill
  스코프/수정 일반화 여부에 대한 새로운 정보이므로 peer(camp-err-review-c2)에게
  notify 후 지시 대기.
- `python scripts/layer2_review.py fail --note "[별도] 자본변동표 당기(2018.06)
  롤포워드 물리분할 표(tables[6], 17행) 전체 결측 — R148과 동일 메커니즘이나 비-신한
  지주 최초 사례 + R148 수정 커밋 이후 재적재본에서도 재현, peer notify함"` 로 처리
  예정(peer 지시 수신 또는 5분 타임아웃 후).
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

## 19. KB금융 2018H1 [기재정정] (r20210427000389) — [별도] 자본변동표 당기(2018.06) 롤포워드 결측 (이슈#18과 동일 패턴 재발 — peer 지시로 재notify 불필요)

- **회사**: KB금융 (00688996), 이슈#18(r20180814002480)의 2021-04-27자 기재정정본, 동일 대상기간(2018H1)
- **CSV**: `layer2_review/KOSPI/00688996_KB금융/half/2018/20210427000389_review.csv`
- **증상**: 셀프체크 `ORPHAN_BODY_TABLE`이 이슈#18과 동일한 원문 조각("과 목 자 본 금
  자 본잉여금 기타포괄손익누계액 이 익잉여금 자기주식 총 계 2017.1.1(전기초) 2,090,558
  14,656,168 (4,742) 2,99...")을 그대로 재보고 — 같은 물리분할 SCE 표의 두 번째(당기)
  표가 이번 정정본에서도 통째로 결측.

### 처리
- 이슈#18과 동일 근본원인(R148 확장 대상, peer가 KB금융 전기간 일괄 백필 예정)이므로
  peer 지시(2026-09-20)에 따라 재notify 없이 문서기록만 하고 fail 처리 후 캠페인 계속
  진행. 파서 코드 수정 없음.

### 재발 목록(이슈#18과 동일 신호 — 셀프체크 ORPHAN_BODY_TABLE, "과 목 자 본 금..."
동일 조각. peer 지시로 건별 재notify·전체 heading 생략, 여기 표로만 누적 기록)

| rcept_no | 보고서 | 처리 |
|---|---|---|
| r20210427000389 | 2018H1 [기재정정] | fail |
| r20180515001957 | 2018Q1 | fail |
| r20210427000369 | 2018Q1 [기재정정] | fail |
| r20230515002541 | 2023Q1 | fail |
| r20231005000478 | 2023Q1 [기재정정] | fail |
| r20230316001417 | 2022FY | fail |
| r20230324001073 | 2022FY [기재정정] | fail |
| r20231005000471 | 2022FY [기재정정] | fail |
| r20221114001992 | 2022Q3 | fail |
| r20241230000609 | 2022Q3 [기재정정] | fail |
| r20220816001769 | 2022H1 | fail |
| r20220516002493 | 2022Q1 | pass (정상 — 물리분할 2번째 표가 이번엔 정상 적재됨, 8스코프 전부 원문일치) |
| r20220317001083 | 2021FY | fail |
| r20220325001281 | 2021FY [기재정정] | fail |
| r20211115002116 | 2021Q3 | fail |
| r20241230000601 | 2021Q3 [기재정정] | fail |
| r20210817001770 | 2021H1 | fail |
| r20210517001542 | 2021Q1 | fail |
| r20201116001810 | 2020Q3 | fail |
| r20210427000513 | 2020Q3 [기재정정] | fail |
| r20200814002257 | 2020H1 | fail |
| r20210427000500 | 2020H1 [기재정정] | fail |
| r20200515002333 | 2020Q1 | fail |
| r20200522000253 | 2020Q1 [기재정정] | fail |
| r20210427000485 | 2020Q1 [기재정정] | fail |
| r20190515001876 | 2019Q1 | fail |
| r20210427000437 | 2019Q1 [기재정정] | fail |
| r20191114001567 | 2019Q3 | fail |
| r20210427000463 | 2019Q3 [기재정정] | fail |
| r20190814002451 | 2019H1 | fail |
| r20210427000450 | 2019H1 [기재정정] | fail |
| r20181114002418 | 2018Q3 | fail |
| r20210427000402 | 2018Q3 [기재정정] | fail |

---

## 20. 기아 2024Q1 분기보고서 (r20240516001819) — [별도]·[연결] 손익계산서 EPS(기본주당이익) 행 결측 (신규 메커니즘 — 이슈#16/#17과 증상은 같으나 원인 셀 구조가 다름, camp_err_review 통지 대상)

- **회사**: 기아 (00106641) KOSPI 시총순위 14
- **보고서**: 2024Q1 분기보고서, 접수번호 20240516001819
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240516001819
- **CSV**: `layer2_review/KOSPI/00106641_기아/quarter/2024/20240516001819_review.csv`

**증상**: 셀프체크(`SOURCE_ROW_NOT_LOADED`)가 [연결]·[별도] 손익계산서 양쪽 모두에서
"기본주당이익 (단위 : 원)" 행이 원문에는 있는데 DB에 없다고 정확히 포착함. DART 원문
직접 확인 결과 실재함:
- [연결] IS: 기본주당이익 3개월 7,125 / 누적 7,125(동일, Q1) / 전년동기 3개월 5,323 / 누적 5,323
- [별도] IS: 기본주당이익 3개월 5,495 / 누적 5,495(동일) / 전년동기 3개월 2,251 / 누적 2,251

**원문 셀 구조 실측** (`tables[3]`(연결)/`tables[3]`(별도) 손익계산서, `tr > td` 직접 순회):
```
"주당이익"                    cells = ["주당이익","","","",""]              ← 헤더행
"기본주당이익 (단위 : 원)"    cells = ["기본주당이익 (단위 : 원)","7,125","","5,323",""]
```
다른 일반 항목(예: 매출액)의 헤더 구조는 4개 값 컬럼(3개월/누적/전년3개월/전년누적)이
전부 채워지는데, EPS 리프 행만 **2번째·4번째 위치만 채워지고 3번째·5번째(td)가
빈 문자열로 존재** — 즉 "3개월" 위치에 값이 들어가고 "누적" 위치가 빈 td로 되어
있다(Q1이라 3개월=누적이라서 DART가 두번째 값을 아예 비워 둔 것으로 추정, colspan이
아니라 **빈 `<td>`가 실제로 존재**). 이슈#16/#17의 "리프 행 값이 합계행 컬럼 위치
(col idx2)에 있다"는 구조와는 다른 셀 패턴 — 그쪽은 정상 컬럼에 값이 있으나 인덱스가
밀린 경우이고, 이번 건은 **의도된 컬럼(누적) 위치가 원천적으로 빈 셀**인 경우.
`_emit_eps_lines()`(추정)이 "누적" 컬럼(통상 cells[2])을 우선 채택하는 로직이라면
이 빈 셀을 "값 없음"으로 오판해 행 전체를 드롭했을 가능성.

**확인 범위**: 이 필링의 나머지 전 범위 — [연결] BS(50)/IS(EPS 제외 31)/CF(36)/SCE(96),
[별도] BS(44)/IS(EPS 제외 23)/CF(35)/SCE(48) — 전부 DART 원문과 일치 확인
(연결/별도 각각 이 세션에서 직접 대조 완료, 이 항목 이전 기아 5건과 동일 방법론).
EPS 결측만 유일한 결함.

### 처리
- 신규 메커니즘(이슈#16/#17과 증상은 같으나 셀 구조 원인이 다름)이므로 프로토콜에
  따라 camp_err_review 세션(camp-err-review-c2)에 notify함. 이 접수번호는 응답
  또는 타임아웃 전까지 pass/fail 보류.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 21. 현대모비스 2020Q3 분기보고서 (r20201116000573) — [연결]·[별도] 손익계산서 기타포괄손익(CI) 세부항목 1개 행 "결측" → ✅결함 아님, R156 설계대로 확정 (peer 회신 완료)

- **회사**: 현대모비스 (00164788) KOSPI 시총순위 19
- **보고서**: 2020Q3 분기보고서, 접수번호 20201116000573
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20201116000573
- **CSV**: `layer2_review/KOSPI/00164788_현대모비스/quarter/2020/20201116000573_review.csv`

**증상**: [연결] 손익계산서의 기타포괄손익(CI) 표(`tblIdx5`) "후속적으로 당기손익으로
재분류되지 않는 항목" 섹션에서, DART 원문은 다음 순서로 4개 행을 공시한다:

| 항목 | 3개월(당분기) |
|---|---|
| 확정급여제도의 재측정요소 | 3,383 |
| 지분법이익잉여금변동 | (33,213) |
| 기타포괄손익-공정가치 측정 금융자산 평가이익(손실) | 34,004 |
| **기타포괄손익-공정가치 측정 금융자산 처분이익(손실)** | **(363)** |
| 지분법자본변동 | (27,032) |

DB/CSV에는 "평가이익(손실)" 다음 "지분법자본변동"으로 바로 이어져, **"처분이익(손실)"
행(-363) 하나가 통째로 빠져 있다.** 이 행 뒤로는(후속 재분류 항목 섹션, 연결총포괄이익
등) DART 원문과 정확히 다시 정렬되어 일치 — 딱 이 한 줄만 순수하게 결측.

**확인 방법**: `javascript_tool`로 con IS 원문 전체(tblIdx3+tblIdx5)를 라벨과 함께
추출해 CSV 기대값(30행)과 순서대로 비교한 결과, 인덱스 22부터 한 칸씩 밀리는 결측
패턴이 나타났고, 원문 라벨을 대조해 정확히 "기타포괄손익-공정가치 측정 금융자산
처분이익(손실)" 행 자체가 CSV에 없음을 확인(위/아래 다른 모든 행은 값까지 완전 일치).

**추정 원인(미확인, 코드 미조사 — 파서 코드 수정 금지 지시에 따라 조사만, 수정 안 함)**:
"평가이익(손실)"과 "처분이익(손실)"이 라벨 접미사만 다른 유사 계정으로, 같은 CI
표 안에 연속으로 나타난다. 라벨 정규화/매칭 로직이 두 유사 라벨을 같은 개념으로
오인해 중복 제거(dedup)했거나, 값이 작아(-363) 임계값성 필터에 걸렸을 가능성 등
미확인 가설. 이슈#1(두산에너빌리티 EPS 계속/중단영업 결측)·이슈#16/#17/#20(EPS
컬럼선택 결측)·이슈#7~19(SCE 물리분할 결측) 어느 패턴과도 셀 구조나 항목 성격이
다른 완전히 새로운 사례로 판단.

**확인 범위**: 이 필링의 나머지 전 범위 — [연결] BS(40)/CF(29)/SCE(94, 0건 불일치),
[별도] BS(36)/IS(21, 이 패턴 제외)/CF(27)/SCE(49) — 전부 DART 원문과 전체 행 100% 대조,
불일치 0건. [연결] IS는 이 1개 행을 제외한 나머지 29개 항목 전부 DART 원문과 정확히
일치. (최초 기록 시점엔 [별도] IS를 아직 원문대조하지 않은 상태였으나, notify 이후
직접 sep-15_anchor 로 이동해 대조 완료 — 아래 "처리" 참조.)

### 처리
- **peer 회신(camp-err-review-c2, R156, `docs/PARSING_RULES.md` 커밋 `b436351`) —
  결함 아님, 설계대로.** 근본원인은 dedup/임계값 추측이 아니라 **누적 칸이 원문 자체에서
  공란**인 것: 헤더는 [3개월, 누적, 전년3개월, 전년누적] 4열인데 이 행("처분이익(손실)")은
  3개월=(363)만 있고 누적 칸이 진짜로 빈칸(전기 열도 마찬가지). R144 정책상 분기·반기는
  누적 열만 적재하므로(`report_lines` col_index=0=당기 누적 전용), 누적이 공란인 이
  행은 실을 축 자체가 없어 원천적으로 미적재 — 날조 방지(R3/R85) 관점에서 올바른 동작.
  바로 위 행("평가이익(손실)")이 3개월 36,135 vs 누적 34,004 로 **Q3는 3개월≠누적**임을
  자체 증명하므로, 기아(R154, Q1 한정 자기증명 등식)와는 적용 조건이 다름.
  peer가 반증실험으로 확인: 두 유사라벨은 `extract_rows` 산출물에 각각 살아있음(dedup
  아님), 더 작은 값(334)은 정상 적재됨(임계값 아님) — 그 행의 누적 칸에만 값을 넣으면
  즉시 픽업됨.
- **peer 통지 후 직접 sep 쪽도 재확인**: sep-15_anchor 이동 후 sep IS(tblIdx3+5) 원문
  대조 결과 동일 패턴 재현 — 같은 "처분이익(손실)" 행이 sep에서도 3개월=(363)/누적
  공란. 판정기준표(아래) 기준 정상.
- **★향후 판정 기준표(R156)**:
  | 원문 상태 | 판정 |
  |---|---|
  | 분기·반기에서 3개월만 있고 누적 공란 | 정상 |
  | Q1인데 누적 공란 + 같은 표 다른 행이 3개월=누적 증명 | 결함(R154 대상) |
  | Q1인데 당기 3개월·누적 둘 다 공란 | 정상 |
  | 전기에만 값 있음 | 정상(전기는 적재 대상 아님) |
  | **누적이 차 있는데도 결측** | 결함 → 이슈로 올릴 것 |
- `python scripts/layer2_review.py pass --verified-scopes con-bs,con-is,con-cf,con-sce,
  sep-bs,sep-is,sep-cf,sep-sce --note "...R156 설계대로..."` 로 처리 완료
  (2026-09-21). 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 22. 엘에스일렉트릭 2025FY 사업보고서 (r20260318001243) — [별도]·[연결] 자본변동표(SCE) 원문 셀 값이 ÷1,000,000 소수점 표기로 손상된 것을 파서가 그대로 절삭 적재 → 이익잉여금 열 값이 실제값의 약 100만분의 1로 왜곡 (신규 메커니즘, camp_err_review 통지 대상)

- **회사**: 엘에스일렉트릭 (00105855) KOSPI
- **보고서**: 2025FY 사업보고서, 접수번호 20260318001243
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260318001243
- **CSV**: `layer2_review/KOSPI/00105855_엘에스일렉트릭/annual/2025/20260318001243_review.csv`

**증상**: DART 원문 자체가 [별도]·[연결] 자본변동표(SCE)의 "이익잉여금" 열에서 일부
셀을 실제 원화 금액이 아니라 **100만으로 나눈 값에 소수점을 붙인 형태**로 잘못
렌더링하고 있다(DART 소스 자체의 서식 결함). 예:

```
[별도] SCE, "배당지급" 행(2024.01.01 기초자본 이동, 제51기):
  이익잉여금 열 = "(82,196.9288)"      ← DART 원문 자체가 이렇게 표시
  자본총계 열(같은 행)  = "(82,196,928,800)"  ← 정상 표기, 82,196,928,800 ÷ 1,000,000 = 82,196.9288 정확히 일치
```

같은 패턴이 [별도] SCE에서 총 6개 셀(제51기=2024/제52기=2025 × 3개 항목: 배당지급,
당기순이익, 확정급여제도의 재측정요소), [연결] SCE에서 4개 셀(배당금의 지급,
확정급여제도의 재측정요소 × 2개년 — 연결 쪽은 당기순이익 항목은 이 손상 패턴에
해당하지 않음, 별도/연결 발현 범위가 다름) 확인됨:

| 구분 | 항목 | 회계연도 | 원문 손상값 | ÷1,000,000 검산 대상(같은 행 자본총계/총계) | 정상값(재구성) |
|---|---|---|---|---|---|
| 별도 | 배당지급 | 2024(제51기) | (82,196.9288) | (82,196,928,800) | -82,196,928,800 |
| 별도 | 당기순이익 | 2024(제51기) | 213,382.825983 | 213,382,825,983 | 213,382,825,983 |
| 별도 | 확정급여제도의 재측정요소 | 2024(제51기) | (6,105.268746) | -6,105,268,746 | -6,105,268,746 |
| 별도 | 배당지급 | 2025(제52기) | (86,133.6569) | (86,133,656,900) | -86,133,656,900 |
| 별도 | 당기순이익 | 2025(제52기) | 293,824.766256 | 293,824,766,256 | 293,824,766,256 |
| 별도 | 확정급여제도의 재측정요소 | 2025(제52기) | (5,999.574561) | -5,999,574,561 | -5,999,574,561 |
| 연결 | 배당금의 지급 | 2024 | (82,196.9288) | (82,196,928,800) | -82,196,928,800 |
| 연결 | 확정급여제도의 재측정요소 | 2024 | (9,488.12732) | -9,488,127,320 | -9,488,127,320 |
| 연결 | 배당금의 지급 | 2025 | (86,133.6569) | (86,133,656,900) | -86,133,656,900 |
| 연결 | 확정급여제도의 재측정요소 | 2025 | (4,785.049806) | -4,785,049,806 | -4,785,049,806 |

**우리 파서(DB) 적재 상태**: 위 10개 셀 전부 DART의 손상된 소수점 텍스트를 그대로
**정수부만 절삭**해 적재함 — 예: CSV의 "배당지급" 행(별도, 순번 31)은 `-82,196`으로
적재되어 있음(정상값 -82,196,928,800과 비교해 약 100만분의 1로 왜곡, 단순 반올림이
아니라 자릿수 자체가 소실됨). CSV row 256("당기순이익")도 `213,382`로 적재되어
정상값 213,382,825,983과 100만배 차이. 연결 SCE의 4개 셀도 동일하게 정수부만 절삭된
왜곡값으로 확인(예: 연결 "배당금의 지급" 2024 → CSV에 `-82,196`).

**왜 R148(신한지주 SCE 물리분할 결측)·이슈#16~20(EPS 결측)과 다른 신규 케이스로
판단하는지**: 이 필링은 SCE가 물리적으로 분할되지도 않았고(단일 table), 행 자체가
빠진 것도 아니다(해당 행은 CSV에 존재함) — **행은 존재하는데 그 행의 특정 열(이익
잉여금) 값 하나가 파싱 단계에서 왜곡**된 케이스로, 지금까지 로그된 21건 중 어느
패턴과도 겹치지 않는 새로운 결함 유형이다.

**왜 이슈#7~21(2022FY 연결 CF 부호오류 등)과 달리 "DART 원문 재현이 아니라 파서
결함"으로 판단하는지**: 2022FY CF 부호오류 사례(같은 회사, 위 세션 초반 조사)는
DART 원문 자체가 부호까지 포함해 돌이킬 수 없이 잘못 표기된 값이라 파서가 그대로
재현하는 것이 맞는 처리였다. 반면 이번 SCE 소수점 손상은 (a) 같은 행의 다른 열
(자본총계/총계)에 정확한 원 단위 총계가 남아 있어 ÷1,000,000 관계로 **수학적으로
완전히 복원 가능**하고, (b) 우리 파서는 그 복원 가능성을 활용하지 않고 오히려
자릿수를 100만분의 1로 절삭해 원문보다 훨씬 더 왜곡된 값을 만들어낸다는 점에서
명확히 파서 레벨 결함이다. `parse_amount()`(R149 EPS "원" 접미사 처리 함수와 동일
계열로 추정)가 소수점이 포함된 "82,196.9288" 같은 텍스트를 만났을 때 정수부만
취하고 소수부를 버리는 방식으로 처리하고 있을 가능성이 높음(코드 미조사 — 이 세션은
파서 코드 수정 금지).

**확인 범위**: 이 필링의 [별도] BS(53항목)·IS+CI(24항목)는 DART 원문과 전체 대조
완료, 불일치 0건(자산총계 3,999,949,042,698·자본총계 1,986,911,322,242·매출액
3,462,741,368,949·당기순이익 293,824,766,256·EPS 9,889원 전부 일치). [별도]·[연결]
SCE는 위 10개 손상 셀을 `/\d\.\d/` 정규식으로 전수 스캔해 확인. [별도] CF, [연결]
BS/IS/CF는 이 결함 발견 이후 대조 중단(신규 결함 발견 시 즉시 notify 원칙에 따름).

### 처리
- 신규 메커니즘(SCE 특정 셀 소수점 손상 → 파서 절삭 왜곡)이므로 프로토콜에 따라
  camp_err_review 세션(camp-err-review-c2)에 notify함.
- **peer 회신 수신(2026-09-21, 5분 타임아웃 전)** — 재현 확인, 진짜 결함, fail 처리
  맞음. peer가 원문·DB 양쪽 직접 확인: 별도 SCE 이익잉여금 열이 "같은 열 안에서
  표기가 섞여" 있음(2023 배당지급은 정상 원단위, 2024/2025 배당지급·당기순이익만
  백만원 소수 표기로 손상). DB 실측: `배당금의 지급` col=4에 `-82196`이 다른 연도의
  정상값 `-32,495,350,600`과 나란히 들어가 있어 **결측이 아니라 값이 있어서 자동검산도
  통과해버리는** 더 위험한 유형으로 재확인. 근본원인은 `parse_amount()`가 소수부를
  버리고 정수부만 취하는 동작(R149와 같은 함수, 다른 결함) — 코드 수정은 peer 쪽에서
  진행 예정.
- **★peer가 조사 중 발견한 2차 결함(이슈#22보다 범위가 클 가능성) — 캠페인 판정
  기준에 즉시 반영**: `parse_amount()`가 소수부를 **단위 배수 적용 전에** 버림.
  따라서 "(단위: 백만원)"/"(단위: 천원)" 선언 표에 소수 표기가 있으면 조용히 값이
  깎임 — 예: 원문 `1,234.5`(백만원) → 정확값 1,234,500,000인데 DB엔 1,234,000,000만
  적재. `0.5`(백만원) → 정확값 500,000인데 DB엔 **0**으로 사라지는 극단 케이스도 가능.
  peer가 전사 규모 측정 중.
  - **★★캠페인 판정 기준 추가(즉시 적용)**: 백만원/천원 단위 선언 표에서 **소수점이
    있는 원문 셀**을 만나면, DB 값이 소수부만큼 깎여 있는지 반드시 확인할 것
    (원문 `1,234.5`인데 DB가 `1,234,000,000`이면 결함, 정확값 1,234,500,000).
    이 계열은 값이 그럴듯하게 있어서 지금까지 이 세션에서 pass 처리한 필링 중에도
    잠복해 있을 수 있음 — 향후 유사 사례 발견 시 재notify 없이 문서기록만 하고
    fail(또는 이미 pass된 건은 비고 기록) 처리.
  - ★단, peer 지적대로 소수부 절삭이 "맞는" 케이스도 존재(이화공영 EPS `(69.0)`→-69
    등 소수부가 반올림 잔여물인 경우) — 일반화해서 전부 결함으로 단정하지 말고 케이스별
    확인 필요.
  - 이슈#22 본건(단위 선언 자체가 없이 열 안에서 표기가 섞인 경우)의 수정 방향(ⓐ결측
    처리 vs ⓑ같은 행 자본총계로 수학적 복원)은 peer가 사용자 판단 요청 예정 — 이
    세션은 관여하지 않음(캠페인 전용).
- `python scripts/layer2_review.py fail --note "[별도]·[연결] 자본변동표(SCE)
  이익잉여금 열 10개 셀 소수점손상 파서절삭 왜곡(정상값의 약100만분의1) — peer
  재현확인, 근본원인 parse_amount() 소수부절삭, 2차결함(단위배수적용전 절삭) 발견
  건은 캠페인 판정기준에 반영"` 로 처리.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 23. 엘에스일렉트릭 2025Q3 분기보고서 (r20251114002743) — [연결] 자본변동표 배당금의 지급·확정급여제도의 재측정요소 소수점손상 파서절삭 (이슈#22와 동일 근본원인 재발, 재notify 불필요)

- **회사**: 엘에스일렉트릭 (00105855) KOSPI
- **보고서**: 2025Q3 분기보고서, 접수번호 20251114002743
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20251114002743
- **CSV**: `layer2_review/KOSPI/00105855_엘에스일렉트릭/quarter/2025/20251114002743_review.csv`

**증상**: 이슈#22와 완전히 동일한 근본원인. [연결] 자본변동표 DART 원문에서
"배당금의 지급" 행의 이익잉여금 열이 `(86,133.6569)`, "확정급여제도의 재측정요소"
행이 `(286.963259)`로 손상 표기되어 있고(둘 다 같은 행의 다른 열 정상 총계와
÷1,000,000 관계로 수학적 검증됨: -86,133,656,900 / -286,963,259), DB에는 정수부만
절삭된 `-86,133`(CSV row 618)과 `-286`(CSV row 674)이 그대로 적재됨.

**확인 방법**: `javascript_tool`로 DART [연결] SCE(table[5], 31행)를 `/\d\.\d/`
정규식으로 스캔해 `(86,133.6569)`·`(286.963259)` 두 손상 셀을 직접 확인. CSV에서
같은 항목의 짝 행(row 619 `-86,133,656,900`, row 675 `-286,963,259`)이 정상값을
갖고 있어 대조 확인됨. [별도] SCE는 이 필링에서 손상 없이 정상 적재됨(row 213-269
전부 정상 원단위 값, 이 필링에선 [연결]에서만 발현 — 비대칭 발현은 이슈#18에서도
관찰된 패턴).

### 처리
- 이슈#22와 완전히 동일한 근본원인(parse_amount() 소수부절삭)의 재발이므로, peer가
  이미 파악하고 캠페인 판정기준에 반영한 상태 — 재notify 없이 문서기록만 하고 fail
  처리 후 캠페인 계속 진행.
- `python scripts/layer2_review.py fail --note "[연결] 자본변동표 배당금의 지급/
  확정급여제도의 재측정요소 소수점손상 파서절삭 — 이슈#22와 동일 근본원인 재발"`
  로 처리.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 24. 엘에스일렉트릭 2023FY 사업보고서 (r20240313001659) — [별도]·[연결] 자본변동표 소수점손상 파서절삭 재발(이슈#22/#23과 동일 근본원인), 처음으로 당기순이익 행까지 손상 확인

- **회사**: 엘에스일렉트릭 (00105855) KOSPI
- **보고서**: 2023FY 사업보고서, 접수번호 20240313001659
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240313001659
- **CSV**: `layer2_review/KOSPI/00105855_엘에스일렉트릭/annual/2023/20240313001659_review.csv`

**증상**: 이슈#22/#23과 완전히 동일한 근본원인. `/\d\.\d/` 정규식으로 DART 원문
SCE를 스캔한 결과:
- [별도] SCE(35 tr): `(29,325.026)`(2022 배당금의 지급) · `16,811.69929`(2022
  확정급여제도의재측정요소) · `(32,291.6506)`(2023 배당금의 지급) ·
  `202,528.052033`(2023 당기순이익) · `(9,692.967279)`(2023 확정급여제도의재측정
  요소) — 5개 셀 손상.
- [연결] SCE(51 tr): `(29,528.726)`(2022 배당금의 지급) · `(32,495.3506)`(2023
  배당금의 지급) — 2개 셀 손상.

DB는 전부 정수부만 절삭해 적재: [별도] CSV row 239 `-29,325`(정상 -29,325,026,000),
row 249 `16,811`(정상 16,811,699,290), row 261 `-32,291`(정상 -32,291,650,600),
**row 263 `202,528`(정상 202,528,052,033)**, row 271 `-9,692`(정상 -9,692,967,279).
[연결] CSV row 560 `-29,528`(정상 -29,528,726,000), row 610 `-32,495`(정상
-32,495,350,600).

**★신규 관찰**: 지금까지(이슈#22/#23)는 배당지급·확정급여제도 재측정요소·(별도
한정)당기순이익 조합이었는데, 이번 건은 **[별도] "당기순이익" 행이 손상된 첫
사례**(이전 이슈#22의 별도 당기순이익 손상은 이미 있었음 — 정정하면, 연결 SCE에
당기순이익 손상이 없다는 점은 계속 동일하게 유지됨). 손상 발생 항목/연도 조합에
일관된 규칙성은 안 보이나, 매번 "배당금의 지급"과 "확정급여제도의 재측정요소"가
가장 빈번히 걸리는 패턴은 유지됨.

### 처리
- 이슈#22와 동일 근본원인(parse_amount() 소수부절삭)의 재발이므로 재notify 없이
  문서기록만 하고 fail 처리.
- `python scripts/layer2_review.py fail --note "[별도]·[연결] 자본변동표 배당금의
  지급/확정급여제도의 재측정요소/당기순이익 소수점손상 파서절삭 7개 셀 — 이슈#22와
  동일 근본원인 재발(이슈#24)"` 로 처리.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 25. (참고, 결함 아님) 엘에스일렉트릭 2023FY [기재정정] (r20240326000494) — 이슈#24의 SCE 소수점 오류를 DART 원문 자체에서 정정한 필링, PASS

- **회사**: 엘에스일렉트릭 (00105855) KOSPI
- **보고서**: 2023FY 사업보고서 [기재정정], 접수번호 20240326000494 (최초제출
  20240313001659의 13일 후 정정)
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240326000494

**배경**: 최초본(r20240313001659, 이슈#24)이 [별도]·[연결] SCE 7개 셀(배당금의
지급·확정급여제도의 재측정요소·당기순이익)에서 소수점손상 파서절삭 결함으로
fail 처리됨. 13일 후 접수된 이 [기재정정]의 CSV를 최초본과 `diff`한 결과, 정확히
그 7개 셀만 차이가 있었고(예: 별도 배당금의 지급 `-29,325` → `-29,325,026,000`),
나머지 606행은 byte-identical.

**확인**: DART 정정신고서 "3. 정정사항" 표에 "Ⅲ.재무에 관한 사항 2-3.
연결자본변동표"·"4-3. 자본변동표"가 각각 "단순기재오류"로 정정사유 명시됨 —
즉 **DART 원문 자체가 소수점 표기 오류를 정정**한 것이며, 파서/DB 결함이 아님.
이 필링의 SCE를 DOM에서 재확인한 결과 7개 셀 전부 소수점 없는 온전한 값으로
표기되어 있고, CSV도 동일 값으로 정확히 적재됨.

**★캠페인 원칙 확인**: 최초본은 SCE 결함 발견 즉시 fail 처리되어 BS/IS/CF는
검증되지 않았으므로(이슈#24 처리 시점 관행), 이 정정본에서 8개 스코프
([별도]·[연결] 각 BS/IS/CF/SCE) 전체를 처음부터 끝까지 DART 원문과 라인 단위로
전수 대조 완료 — 전부 일치, 이상 없음.

### 처리
- `python scripts/layer2_review.py pass --verified-scopes sep-bs,sep-is,sep-cf,sep-sce,con-bs,con-is,con-cf,con-sce`
  로 처리.
- notify 불필요(결함 아님, 오히려 결함이 상류에서 해소됐음을 확인).

---

## 26. 삼성화재해상보험 2019Q1 분기보고서 (r20190515002191) — [별도] 현금흐름표 전체가 [별도] 자본변동표 라벨로 오분류 적재, [별도] CF는 완전 결측(0행) — ★신규 패턴, ★캠페인 최초 보험업 사례

- **회사**: 삼성화재해상보험 (00139214) KOSPI 시총순위 24
- **보고서**: 2019Q1 분기보고서 (2019.03), 접수번호 20190515002191
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190515002191
- **CSV**: `layer2_review/KOSPI/00139214_삼성화재해상보험/quarter/2019/20190515002191_review.csv`

**증상**: CSV 헤더 요약 `[별도] BS 35 / IS 54 / CF 0 / SCE 398`에서 CF가 0행,
SCE가 비정상적으로 398행(다른 8개 필링 대비 3~10배 큼)인 것이 첫 신호.

DART 원문 "4. 재무제표"(별도) 탭의 실제 `<table>` 구조를 직접 확인:
- `tables[5]` (37행) — 진짜 자본변동표. 컬럼: 자본금/자본잉여금/자본조정/
  기타포괄손익누계액/이익잉여금/총계.
- `tables[7]` (82행) — **별개의 현금흐름표**. 첫 행이 "I. 영업활동으로 인한
  현금흐름"으로 시작, 이후 "(1) 분기순이익", "(2) 손익조정항목" 등 전형적인 CF
  구조를 그대로 담고 있음(예: 분기순이익 230,825,451,933 — 원문 별도 IS의
  당기순이익과 일치).

CSV `[별도] 자본변동표` 라벨 아래 398개 값행(ordinal 1~398) 중:
- ordinal 1~104 (파일 라인 163~266) — 진짜 SCE 내용(연차배당/당기순이익/
  매도가능금융자산평가손익/확정급여제도 재측정요소 등, `tables[5]` 대응).
- ordinal 105~398 (파일 라인 267~560, **294행**) — `tables[7]`의 CF 내용이
  그대로 같은 "[별도] 자본변동표" 라벨을 달고 이어짐. 라인 267에서 "I. 영업활동으로
  인한 현금흐름"이 시작되고, 이후 "II. 투자활동으로 인한 현금흐름"(라인 464),
  "III. 재무활동으로 인한 현금흐름"(라인 522), "V. 현금및현금성자산의 증가(감소)
  (l+ll+lll+IV)"(라인 549), "VI. 기초의 현금및현금성자산"(라인 553), "VII. 기말의
  현금및현금성자산 (V+VI)"(라인 557)까지 CF 전체 구조가 고스란히 담겨 있음 —
  라벨만 "[별도] 자본변동표"로 잘못 붙었을 뿐 데이터 자체는 완전함(멸실이 아니라
  오분류).
- `grep -c "^\[별도\] 현금흐름표"` = 0 — [별도] CF는 자기 라벨로는 단 한 행도
  존재하지 않음.

**★[연결]은 정상**: 같은 필링의 `[연결] 자본변동표`(143행)에는 영업활동/투자활동/
재무활동 등 CF 언어가 전혀 섞여 있지 않고(grep 매치 0건), `[연결] 현금흐름표`는
자기 라벨로 83행 정상 적재됨. 즉 이 필링 안에서도 **[별도]만 결함, [연결]은
정상** — 비대칭적 발현(이슈#18의 KB금융 SCE 결측 건과 같은 비대칭 패턴).

**★신규 패턴 판단 근거**: 기존 이슈 로그에서 "CF가 SCE 라벨로 흡수" 또는 "CF
라벨 완전 결측(0행)"과 정확히 일치하는 사례를 찾지 못함(이슈#7~18/#22~24는 전부
SCE 자체의 롤포워드 결측 또는 소수점 절삭 문제로, CF와의 경계 오분류가 아님).
또한 이 필링은 **캠페인에서 처음 접하는 보험업(삼성화재해상보험)** 사례 —
KOSPI 24위, 첫 항목. 은행지주(신한지주/KB금융)와는 다른 업종에서 SCE/CF 경계
관련 결함이 나온 것이므로, 업종별 서식 특이성(보험사 재무제표 특유의 표 순서/
헤더 구조)이 원인일 가능성을 배제할 수 없음 — 근본원인은 파서 코드 미조사(이
세션은 캠페인 전용, 코드 접근 안 함).

**확인 범위**: 이 필링은 이슈 발견 즉시 대조 중단(지침에 따름). [별도] BS/IS,
[연결] 전체(BS/IS/CF/SCE)는 대조하지 않음.

### 처리
- **신규 패턴**이므로 peer(camp-err-review-c2)에게 notify 후 최대 5분 대기,
  응답 유무와 무관하게 진행.
- `python scripts/layer2_review.py fail --note "[별도] 현금흐름표 전체(294행)가
  [별도] 자본변동표 라벨로 오분류 적재, [별도] CF 자기라벨 0행 완전결측(오분류이지
  값 자체는 원문에 존재) — 신규 패턴, 이슈#26, camp-err-review-c2 notify 완료"`
  로 처리.
- 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 27. 삼성화재해상보험 2017Q3/2017H1 vs 2017Q1 — [연결] 자본변동표 "2015.01.01(전전기초) 연결기타포괄손익누계액" 값이 같은 회사·같은 개념의 역사적 시점인데 필링마다 다르고, 그 중 2건은 SCE 자체 내부 등식과도 안 맞음 — ★값 오류, DART 원문 자체 결함으로 판단(파서 결함 아님)

- **회사**: 삼성화재해상보험 (00139214) KOSPI 시총순위 24
- **관련 필링 3건**(모두 [연결] 자본변동표에 "I. 2015년 01월 01일(전전기초)" 행 보유,
  전전기=2015이 되는 2017년도 필링에만 등장):
  - 2017Q3 분기보고서 r20171114002393 (이미 pass 처리됨, 이 세션에서)
  - 2017H1 반기보고서 r20170814002066 (이미 pass 처리됨, 이 세션에서)
  - 2017Q1 분기보고서 r20170515003620 (이번에 검토 중 발견)

**증상**: 세 필링 모두 [연결] 자본변동표의 "I. 2015년 01월 01일(전전기초)" 행,
연결기타포괄손익누계액 열(5번째 데이터컬럼) 값이 다음과 같이 다름:
- 2017Q3 DART 원문 표시: `3,225,867,324,37 `(끝자리 잘림, 콤마 4번째 그룹이 2자리)
  → CSV 파싱값 `322,586,732,437`(12자리)
- 2017H1 DART 원문 표시: 동일하게 `3,225,867,324,37 ` → CSV 파싱값 동일 `322,586,732,437`
- 2017Q1 DART 원문 표시: `3,225,867,324,370`(13자리, 정상 완전한 값) → CSV 파싱값
  동일 `3,225,867,324,370`

**★내부 등식으로 검증**: 같은 [연결] SCE 표 안에서 2015년도 중 발생한
연결기타포괄손익누계액 변동행(4.매도가능금융자산평가손익 296,979,532,090 /
5.해외사업환산손익 6,945,503,990 / 6.현금흐름위험회피파생손익 5,216,353,743 /
7.특별계정기타포괄손익 18,460,174,438 / 8.확정급여제도 재측정요소 -11,611,342,311,
이 5개 행은 세 필링 모두 완전히 동일)의 합계는 +315,990,221,950. "II. 2015년
12월 31일(전전기말)" 종료잔액(연결기타포괄손익누계액)은 세 필링 모두 동일하게
`3,541,857,546,320`으로 선언됨. 따라서:
  개시잔액(2015.01.01) = 3,541,857,546,320 − 315,990,221,950 = **3,225,867,324,370**
이는 2017Q1 필링의 값과 정확히 일치 — 2017Q1이 산술적으로 옳다.
반면 2017Q3/2017H1의 저장값(322,586,732,437)으로 같은 등식을 계산하면
322,586,732,437 + 315,990,221,950 = 638,576,954,387 ≠ 3,541,857,546,320
(선언된 종료잔액과 전혀 안 맞음) — **2017Q3/2017H1 필링 자체의 SCE 표가
내적으로 불일치**.

**판단**: CSV는 각 필링에서 DART 화면에 실제로 표시된 텍스트를 그대로(합리적으로)
전사했음 — 2017Q3/2017H1은 DART 원문 HTML 자체가 이미 끝자리 "0"이 잘린
`3,225,867,324,37` 형태로 깨져 있고, 파서가 이를 콤마 위치 재해석하여
322,586,732,437로 적재. 이는 **원문(DART 필링 자체)의 결함**으로 판단 —
회사가 2017년 3분기·반기 보고서 제출 시 비교연도(2015년) 개시잔액을 재입력하며
끝자리 숫자를 빠뜨린 것으로 추정(1분기 보고서엔 없던 오류가 3분기·반기에만
등장 — 회사가 매 분기 SCE 비교표를 수기 재작성하면서 생긴 오타로 추정, 확인
불가). 파서/로더 코드에 결함이 있다고 보기 어려움 — 단, 콤마 재해석 로직이
"끝자리 잘린 숫자"를 감지해 경고할 수 있는지는 camp_err_review 쪽에서 검토
여지 있음(이 세션은 캠페인 전용, 코드 미수정).

**★이미 pass된 두 필링에 대한 조치**: 2017Q3/2017H1 pass 당시 이 셀을
"DART HTML 렌더링 콤마 오류, 자릿수 재배열하면 CSV값과 일치"라고 잘못
판단하여 비결함으로 종결했음(이 세션 자체의 판단 오류). 이번에 3필링 교차
검증으로 실제로는 CSV 값(322,586,732,437)이 산술적으로 틀렸고, 참값은
3,225,867,324,370(2017Q1 필링 기준)임을 확인. 다만 이 오류의 근원은 파서가
아니라 DART 원문 자체이므로 **재조사(fail→재검토) 대상이 아니라고 판단** —
이미 pass된 두 건은 그대로 두고, 이 이슈 로그에 사실관계만 정정 기록.
`redo` 커맨드로 되돌리지 않음(파서 결함이 아니라 원문 결함이므로 재작업해도
결과가 달라지지 않음).

### 처리
- 신규 패턴(값 자체가 필링마다 다르고 SCE 내부 등식이 깨지는 사례는 최초)이므로
  peer(camp-err-review-c2)에게 notify.
- 파서 코드 수정 없음(이 세션은 캠페인 전용, 원문 결함으로 판단).
- 2017Q1(r20170515003620) 계속 검토 진행 — 이 필링 자체는 이 값이 정상이므로
  다른 스코프 계속 대조.

---

## 28. 효성중공업 2019Q1 분기보고서 (r20190515002585) — [별도]·[연결] 자본변동표(SCE) "기타자본구성요소" 열(및 그 하위 OCI 세부행)이 음수인데 DART 원문 SCE 표 자체에 부호(괄호/마이너스)가 누락됨 → 파서는 원문 그대로(양수) 적재해 BS와 부호가 어긋남 — ★신규 패턴(값의 부호가 통째로 누락되는 사례는 최초), camp_err_review 통지 대상

- **회사**: 효성중공업 (01316245) KOSPI 시총순위 25 — ★캠페인에서 처음 접하는 회사, 첫 항목
- **필링**: 2019Q1 분기보고서 r20190515002585

**증상**: 자동검산에서 `[연결] 현금흐름표 cf_closing_cash FAIL`(기말−기초 ≠ 순증가감소,
차 341,974,376원)이 뜬 것을 계기로 원문 전체대조를 시작. CF 쪽은 원문에 별도의
"현금및현금성자산의 환율변동으로 인한 효과" 행(341,974,376)이 존재해 셀프체크 공식이
그 행을 반영 못한 것뿐 — **결함 아님**(40,977,456,803+24,407,747,692+341,974,376=
65,727,178,871, 기말과 정확히 일치). 그런데 전체대조를 계속하는 과정에서 SCE 표에서
별개의 진짜 결함을 발견:

- [별도] 자본변동표 "기타자본구성요소" 열: 당기초(2019.01.01) `30,162,524,009`,
  당기말(2019.03.31) `29,461,715,719` — 둘 다 원문 SCE 표에 **괄호/마이너스 없이
  양수로만** 표시됨(raw HTML `<p>29,461,715,719</p>` 확인, 부호기호 전혀 없음).
- 그런데 **같은 필링의 [별도] 재무상태표**는 같은 개념·같은 날짜(2019.03.31/
  2018.12.31)를 `(29,461,715,719)` / `(30,162,524,009)` — **괄호로 음수** 표시
  (raw HTML `<p>(29,461,715,719)</p>` 확인).
- [연결] 자본변동표도 동일 패턴: 기타자본구성요소 열 전기말(2018.12.31)
  `47,199,745,015`, 지배기업소유주지분합계 전기말 `904,517,045,623` 등 전부 양수
  표시. [연결] 재무상태표는 같은 날짜를 `(47,199,745,015)` 로 음수 표시.
- SCE 표 안의 총계 행(자본 합계) 자체는 정확한 값(944,476,124,370 등, BS와 일치)이므로
  **표를 만들 때 사용한 실제 계산값은 음수가 맞고, 화면에 렌더링된 셀 텍스트만 부호가
  빠진 것**임을 산술로 확인: SCE 개시(940,284,624,318)+당기순이익(3,797,842,048)+
  OCI 3항목(양수 표시대로 단순합산 시 4,805,800,624) = 945,090,424,942 ≠ 종료잔액
  944,476,124,370(차 614,300,572 = 순확정급여부채 재측정요소 307,150,286의 2배).
  "순확정급여부채의 재측정요소"를 음수(-307,150,286)로 바꿔 계산하면 정확히
  3,490,691,762(이익잉여금 열 실제 증가분)와 일치 — 이 항목도 원문 SCE 표시가
  부호 누락된 것으로 판단.

**판단**: 파서는 DART 원문 HTML에 보이는 텍스트를 있는 그대로(양수) 충실히 전사함 —
파서 결함이 아니라 **DART 원문 SCE 표 자체의 렌더링 결함**(같은 필링의 BS는 괄호로
정확히 표시하는데 SCE만 괄호가 누락)으로 판단. 다만 결과적으로 로딩된 SCE의
"기타자본구성요소"·"순확정급여부채의 재측정요소" 값이 실제 부호와 반대로 DB에
적재된 상태 — 이 개념이 std_v3 등 하류 집계에서 실제로 사용되는지, 그리고 BS 값을
우선시하는지 여부는 코드 조사가 필요(이 세션은 캠페인 전용, 미조사).

**확인 범위**: 이 필링은 이슈 발견 후에도 나머지 스코프 계속 대조 완료
([별도]BS37/IS21/CF23/SCE18, [연결]BS40/IS25/CF27/SCE65 전부 원문과 대조, 이
SCE 부호결함 외 신규 결함 0건). 부호결함은 [별도]SCE·[연결]SCE 양쪽에서만 확인.

### 처리
- **신규 패턴**이므로 peer(camp-err-review-c2)에게 notify 후 최대 5분 대기,
  응답 유무와 무관하게 진행(프로토콜 [[feedback-layer2-campaign-new-issue-wait-for-peer-instruction]]).
- 파서 코드 수정 없음(이 세션은 캠페인 전용, 원문 렌더링 결함으로 판단).
- **peer 응답(사후, 5분 대기 이후 도착)**: peer가 원문 직접대조로 진단을 확인 —
  SCE의 "일부 셀만"(기초·기말 총계행) 부호 누락이고 변동행 자체는 원래 양수가
  맞음, 항등식(이익잉여금 열 롤포워드·기타자본구성요소 열 롤포워드·자본합계)으로
  참값이 자기증명됨. std_v3(계층3)는 SCE를 숫자로 전혀 소비하지 않음(`combine.py`
  `_FS` 매핑에 SCE 없음, `face_audit.py`/`extract/pdf.py`/`gateb_audit.py` 모두
  SCE 명시 제외 확인) — **계층3 오염 없음**. 다만 동일 계열(같은 필링·같은
  라벨이 IS/BS엔 음수, SCE엔 같은 절대값 양수) 프록시 스캔 결과 **2015+ 기준
  1,209필링/2,552셀** 규모로 확인(당기순이익/기타포괄손익/확정급여재측정/
  해외사업환산손익/지분법자본변동 순). 원문결함이라 "파서를 고친다"는 개념이
  아니며, 자기증명 셀만 부호복원할지(R154 패턴)는 camp_err_review가 **착수 없이
  사용자 판단 대기**로 보류. ★향후 같은 증상(SCE만 부호없음+BS/IS는 괄호)은
  신규결함으로 재기록하지 말고 이 이슈#28로 묶어서 참조.

---

## 29. 한화오션(舊 대우조선해양) 2017FY 사업보고서 (r20180330001629) — [별도] 현금흐름표 당기(제18기)열 "외화표시 현금및현금성자산의 환율변동효과" 행이 음수인데 DART 원문 CF 표 자체에 부호(괄호/마이너스)가 누락됨 → 이슈#28과 동일 증상이나 ★SCE가 아닌 CF에서 최초 확인, camp_err_review 통지 대상

- **회사**: 한화오션 (00111704) KOSPI 시총순위 26 — ★캠페인에서 처음 접하는 회사, 첫 항목
- **필링**: 2017FY 사업보고서(3년비교, 제18기~제16기), 접수번호 20180330001629
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20180330001629
- **CSV**: `layer2_review/KOSPI/00111704_한화오션/annual/2017/20180330001629_review.csv`

**증상**: 자동검산에서 `[별도] 현금흐름표 cf_closing_cash FAIL`(기말−기초 ≠
순증가감소, 차 -521,074,352원)이 뜬 것을 계기로 원문 전체대조 시작. DART 원문
[별도] 현금흐름표(table[9]) 마지막 4행(제18기/제17기/제16기 3개년 비교):

| 항목 | 제18기(당기) | 제17기 | 제16기 |
|---|---|---|---|
| 현금및현금성자산의 증가(감소) | 16,367,553,617 | (930,201,067,779) | 1,060,366,672,157 |
| 기초의 현금및현금성자산 | 144,292,901,261 | 1,072,187,331,380 | 17,607,966,279 |
| 외화표시 현금및현금성자산의 환율변동효과 | **521,074,352** (부호없음) | 2,306,637,660 | **(5,787,307,056)** (괄호) |
| 기말의 현금및현금성자산 | 160,139,380,526 | 144,292,901,261 | 1,072,187,331,380 |

제18기(당기)만 부호가 누락됨을 산술로 확인:
- 제18기: 144,292,901,261 + 16,367,553,617 **+** 521,074,352 = 161,181,529,230 ≠
  기말 160,139,380,526. 반대로 **빼면** 144,292,901,261 + 16,367,553,617 **−**
  521,074,352 = 160,139,380,526 — 기말과 정확히 일치. 즉 이 행은 실제로는
  −521,074,352(음수)여야 항등식이 성립함.
- 제17기(비교연도, 정상 대조군): 1,072,187,331,380 + (−930,201,067,779) +
  2,306,637,660(원문 그대로 양수) = 144,292,901,261 — 기말과 정확히 일치. 이
  컬럼은 부호가 정상.
- 제16기(비교연도): 17,607,966,279 + 1,060,366,672,157 + (−5,787,307,056)(원문
  그대로 음수·괄호 있음) = 1,072,187,331,380 — 기말과 정확히 일치. 이 컬럼도
  정상.

즉 3개 컬럼 중 **당기(제18기) 컬럼만** 이 행의 부호가 누락되어 있고, 비교연도
2개 컬럼은 정상적으로 부호(괄호 있음/없음)가 표시되어 항등식이 그대로 성립한다.
CSV는 DART 원문에 보이는 텍스트(521,074,352, 부호없음)를 그대로 충실히 적재함
(CSV row 159) — 파서 결함이 아니라 **DART 원문 CF 표 자체의 당기 컬럼 렌더링
결함**으로 판단.

[연결] 현금흐름표는 동일 행("외화표시 현금및현금성자산의 환율변동효과")이 3개
컬럼 모두 정상적으로 부호 표시됨(당기 −9,185,868,067, 괄호 있음) — 항등식도
그대로 성립(자동검산 PASS). 즉 이 결함은 **[별도] CF에서만, 그것도 당기 컬럼
1개 셀에서만** 발생.

**이슈#28과의 관계**: 이슈#28은 [별도]·[연결] **자본변동표(SCE)**의 기초/기말자본
행에서 "기타자본구성요소" 등 특정 열이 원문 자체에서 부호 누락된 패턴이었다.
이번 건은 통계표 종류가 **현금흐름표(CF)**로 다르고, 발생 위치도 SCE의
누적잔액 행이 아니라 CF의 조정행(환율변동효과) 1개 셀이라는 점에서 이슈#28과
표면적으로는 유사(DART 원문 자체의 부호 누락, 항등식으로 참값 자기증명)하지만
**적용 대상 재무제표가 다른 최초 사례**로 판단해 신규 이슈로 기록.

**확인 범위**: 이 필링은 이슈 발견 후에도 나머지 스코프 전부 계속 대조 완료
— [별도] BS54(3개년 비교 중 당기컬럼)/IS25(당기순이익739,144,216,539/
EPS8,553)/CI(포괄손익계산서 별도표 10행, 당기총포괄이익661,977,140,647)/
CF47(이 이슈의 발견 행 제외 전부 일치)/SCE92(2015.01.01~2017.12.31, 3개
기간 롤포워드 전부 일치, 부호결함 없음) — [연결] BS59/IS31(계속영업이익=
당기순이익645,758,362,568/EPS7,141)/CI14/CF49(부호결함 없음)/SCE158(3개
기간 롤포워드 전부 일치, 부호결함 없음) 전부 DART 원문과 대조 완료, 이
CF 부호결함 1건 외 신규 결함 0건.

### 처리
- **신규 패턴**(적용 대상 재무제표가 이슈#28의 SCE와 다른 CF에서 최초 확인)이므로
  peer(camp-err-review-c2)에게 notify 후 최대 5분 대기, 응답 유무와 무관하게
  진행(프로토콜 [[feedback-layer2-campaign-new-issue-wait-for-peer-instruction]]).
- 파서 코드 수정 없음(이 세션은 캠페인 전용, 원문 렌더링 결함으로 판단 — CSV는
  원문 리터럴값을 정확히 보존함).
- 이 필링은 CF 부호결함 1건 외 다른 모든 스코프가 원문과 정확히 일치하므로
  `pass --note`로 표시(부호결함은 원문 자체 결함이지 파서/DB 오적재가 아니므로
  fail 대상이 아니라고 판단 — 이슈#28 처리 선례와 동일한 논리).
- ★향후 같은 증상(CF 환율변동효과 행이 당기컬럼만 부호없음, 비교연도는 정상,
  항등식으로 자기증명 가능)은 신규결함으로 재기록하지 말고 이 이슈#29로
  묶어서 참조.

---

## 30. 한화오션(舊 대우조선해양) 2017Q1 분기보고서 (r20170515004751) — [별도] 자본변동표(SCE) 전체(74행) 통째로 결측(0행) — ★단일 table 실종, 이슈#7~15(R148, 2-table 분할)와는 다른 메커니즘, camp_err_review 통지 대상

- **회사**: 한화오션 (00111704) KOSPI 시총순위 26
- **보고서**: 2017Q1 분기보고서(3년비교, 제18기~제16기), 접수번호 20170515004751
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20170515004751
- **CSV**: `layer2_review/KOSPI/00111704_한화오션/quarter/2017/20170515004751_review.csv`

**증상**: 적재 행수 헤더가 "[별도] BS 57 / IS 24 / CF 39 / **SCE 0**"으로 [별도]
자본변동표가 완전히 0행. 자동검산이 정확히 이 결함을 지목: `ORPHAN_BODY_TABLE`
"원문 본문표 1개가 어느 재무제표에도 안 붙음" — 첫 건으로 "자본 자본금
기타자본잉여금 자기주식 신종자본증권 매도가능금융자산평가손익
현금흐름위험회피파생상품평가손익 유형자산재평가잉여금 이익잉여금(결손금)
자본 합계 2015." 문자열을 인용.

**DART 원문 확인**: "4. 재무제표" 클릭 후 tables 인덱스 [0=제목,1=BS,2=제목,
3=IS,4=제목,5=CI,6=제목("자본변동표"),7=SCE데이터,8=제목,9=CF]. table[7]은
**분할되지 않은 단일 `<table>`** 74행, 9개 열(자본금/기타자본잉여금/자기주식/
신종자본증권/매도가능금융자산평가손익/현금흐름위험회피파생상품평가손익/
유형자산재평가잉여금/이익잉여금(결손금)/자본합계), 2015.01.01(당기초)부터
2017.03.31(기말자본, 합계 453,861,699,196)까지 3개년 롤포워드를 전부 포함하는
실제 데이터표. table[6] 제목 텍스트로 "제 18 기 1분기 2017.01.01~2017.03.31"과
"제 17 기 1분기 2016.01.01~2016.03.31" 기간이 명시되어 있어 이 table[7]이
확실히 [별도] SCE 데이터임을 확인.

**이슈#7~15(R148)와의 차이**: R148은 SCE가 헤더 없이 물리적으로 **2개의 별도
`<table>`**로 쪼개지는 서식에서 forward-scan이 첫 표만 채택하고 break하던
버그였다(신한지주 사례). 이번 건은 table[6](제목)+table[7](데이터) 쌍이
**하나만 존재**하고 쪼개지지 않았는데도 그 유일한 데이터표 전체가 통째로
안 붙었다 — R148이 다루는 "두 번째 표를 못 찾음" 상황이 아니라 "유일한 표조차
못 붙임" 상황이므로 별개의 메커니즘으로 추정.

**진단 스크립트 결과** (`layer2_forward_cells.py --rcept 20170515004751`,
읽기전용, 수정 없음): 이 필링 전체 원문 금액셀 5,231개 중 SCE 통계 항목이
"원문 185 · 절단 0"으로 집계됨 — 이 185는 [연결] SCE(185행, 정상 적재됨)의
셀 수와 정확히 일치하고, [별도] SCE(table[7], 약 60여개 비공백 셀 추정)는
이 SCE 카운트 버킷에 아예 들어가 있지 않음(★열절단/헤더드롭/라벨없음/설명안됨
어느 카테고리에도 안 잡힘 — SCE로 인식조차 안 된 것으로 보임). "★설명안됨"
482건 중 최대표는 "BS/consolidated seq=0 num_cols=3"으로 이 SCE와 무관해
보이나 정밀 대조는 안 함.

**확인 범위**: [별도] BS 57/IS 24/CF 39(SCE 제외)는 미대조, [연결] 전체도
미대조 — SCE 통째결측 발견 즉시 대조 중단(지침에 따름).

### 처리
- **신규 패턴**(단일표 실종, 기존 R141/R148 카탈로그의 "2-table 분할" 케이스와
  다른 메커니즘)이므로 peer(camp-err-review-c2)에게 notify.
- `fail --note`로 표시 완료. 파서 코드 수정 없음(이 세션은 캠페인 전용).

---

## 31. 한화오션(舊 대우조선해양) 2015FY [기재정정]사업보고서 (r20170511004419) — [별도] 자본변동표 "2015.12.31(기말자본)" 이익잉여금 셀이 정정 이전(구필링) 값으로 남아있는데 DB에는 정정후 참값이 정확히 적재됨 — ★단순 부호누락(R162/이슈#28)이 아니라 스테일 자릿값 자체가 통째로 바뀐 최초 사례

- **회사**: 한화오션 (00111704) KOSPI 시총순위 26
- **보고서**: 2015FY [기재정정]사업보고서(2017.05.11 접수, 최초본 20160330004251의
  회계추정오류 소급재작성 기재정정), 접수번호 20170511004419
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20170511004419
- **CSV**: `layer2_review/KOSPI/00111704_한화오션/annual/2015/20170511004419_review.csv`

**증상**: [별도] 자본변동표 "2015.12.31 (기말자본)" 행에서 DART 원문 HTML은
이익잉여금 셀을 `(1,563,903,332,495)`로 표시하는데, 이는 **정정 이전(최초본
20160330004251)의 구값과 정확히 일치**한다(최초본 2015.12.31 자본총계
232,848,796,905 = 이 구값으로 계산했을 때의 결과). 그런데 같은 행의 다른 6개
컬럼(자본금/기타불입자본/매도가능금융자산평가손익/유형자산재평가잉여금 등)은
전부 이 기재정정에서 실제로 바뀐 재작성값이고, 자본합계 컬럼도 정정후 참값
`415,242,474,443`으로 이미 올바르게 표시됨. 즉 **이 행 안에서 이익잉여금
셀 1개만 정정이 반영 안 된 구값으로 남아있고, 나머지 컬럼+합계는 전부
정정후 값** — 행 자체 항등식(7개 구성요소 합=합계)이 원문 그대로는 안 맞고
(232,848,796,905 ≠ 415,242,474,443), 이익잉여금을 `-1,381,509,654,957`로
바꾸면 정확히 합계 415,242,474,443과 일치.

**DB 상태**: `report_lines`(및 CSV)에는 이미 `-1,381,509,654,957`(정정후 참값)이
적재돼 있음 — 원문 HTML의 스테일 텍스트가 아니라 올바른 값이 들어감. 파서가
이 값을 어디서 가져왔는지는 미조사(같은 필링의 XBRL fact나 다른 표에서
교차획득했을 가능성) — 이 세션은 캠페인 전용이라 코드 조사 안 함.

**이슈#28/R162와의 차이**: #28·R162는 부호(괄호)만 원문에서 누락된 패턴으로,
절대값 자체는 원문과 DB가 항상 일치했다. 이번 건은 절대값 자체가 다른 스테일
숫자(정정 전 구값)로 원문에 남아있고, DB는 그와 무관하게 정정후 참값을 갖고
있다 — **부호 문제가 아니라 값 자체의 불일치**라는 점에서 최초 사례로 판단.

**확인 범위**: 이 필링은 이 셀 1개 제외 전부 원문과 정확히 일치 확인 완료 —
[별도] BS54/IS+CI25/CF47/SCE76(이 셀 제외) — [연결] BS63/IS+CI33/CF52/SCE140
(R162 패턴 2셀: 2013.01.01 기초자본 기타자본·자기주식, 정상 복원 확인) 전부
대조 완료.

### 처리
- **신규 패턴**(부호 누락이 아닌 스테일 값 자체의 불일치)이므로 peer(camp-err-review-c2)에게
  notify 후 최대 5분 대기, 응답 유무와 무관하게 진행.
- 파서 코드 수정 없음(이 세션은 캠페인 전용). DB 값은 이미 참값이라 데이터 정합성
  문제는 없음 — 단지 "원문 HTML과 DB가 다른 경우가 있다"는 새로운 사실만 기록.
- 이 필링은 이 셀 외 모든 스코프가 원문과 일치하고 DB 값 자체는 올바르므로
  `pass --note`로 표시(이슈#28/#29 선례와 동일 논리 — 원문 자체 결함이지 DB
  오적재가 아님).

**★후속(같은날 다음 필링에서 반전 확인, 2026-09-22)**: 바로 다음 자매 필링인
2015FY [기재정정]사업보고서 2차본(r20171019000325, 2017.10.19 접수, 같은 회사·
같은 스테일 원문 HTML — "2015.12.31(기말자본)" 이익잉여금 `(1,563,903,332,495)`
동일 확인)에서도 **DB가 원문 그대로의 스테일 값을 담고 있음**(report_lines/CSV
= -1,563,903,332,495, 행 항등식 위배: 자본합계 415,242,474,443과 불일치). 2차본을
fail 처리.

**★★재정정(peer camp-err-review-c2 DB 실측, 2026-09-22)**: "1차본(004419)은
DB가 우연히 참값을 가졌다"는 위 판단은 **오류**였음이 확인됨 — peer가 DB를
직접 조회한 결과 두 자매 필링의 [별도] SCE 셀 값은 **완전히 동일**(둘 다
-1,563,903,332,495, 스테일). 최초 pass 판정 당시 SCE 셀이 아니라 **같은 필링의
[별도] BS 이익잉여금**(-1,381,509,654,957, 참값)을 잘못 대조 대상으로 삼아
"DB에 참값 있음"으로 오판한 것 — SCE 표 자체는 두 필링 다 스테일 오적재
상태다. 원문 구조도 peer가 XML에서 직접 확인: 참값(BS)과 스테일값(SCE)이
**같은 문서 안에 둘 다 존재**하며, 파서는 SCE 표의 셀을 원문 그대로 정상
전사했을 뿐(필링마다 다른 소스에서 비일관적으로 가져온다는 이전 추정은 틀림)
— SCE 표 자체가 원문에서 스테일인 것이 근본원인. **1차본(004419)도 fail로
재판정 완료**(`layer2_review.py fail --rcept 20170511004419`).

---

## 32. POSCO홀딩스 2018H1 반기보고서 (r20180814002744) — [별도] 자본변동표 "자기주식" 열 전체가 음수인데 DART 원문 SCE 표 자체에 부호(괄호/마이너스)가 누락됨 (이슈#28/R162와 동일 근본원인 재발, 재notify 불필요)

- **회사**: POSCO홀딩스 (00155319) KOSPI 시총순위 28
- **보고서**: 2018H1 반기보고서, 접수번호 20180814002744
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20180814002744
- **CSV**: `layer2_review/KOSPI/00155319_POSCO홀딩스/half/2018/20180814002744_review.csv`

**증상**: [별도] 자본변동표(tables[5]) "자기주식" 열 값이 표 전체(기초자본 행부터
반기말 행까지)에 걸쳐 양수로 표시됨(예: 기초자본 행 `1,533,468,003,532`, 반기말
행 `1,532,783,944,018`). 같은 페이지의 [별도] 재무상태표(tables[1])에서는 **동일한
금액이 정확히 괄호로 음수 표시**됨(`Ⅴ.자기주식 (1,532,783,944,018)`) — 즉 BS표는
정상, SCE표만 부호 누락. 행 항등식으로 검증: 기초자본 행(자본금+자본잉여금+
신종자본증권+적립금-자기주식+이익잉여금)이 자기주식을 음수로 취급해야만 표시된
합계(44,329,446,923,770)와 정확히 일치하고, 양수로 더하면 불일치. 반기말
행(자본합계 45,297,740,004,158)도 동일하게 자기주식을 음수 취급해야만 일치.
[연결] SCE(tables[6])는 같은 금액이 정상적으로 음수(`-1,533,468,003,532`)로
표시되어 있어 결함이 없음 — [별도] SCE 표에만 국한된 원문 렌더링 결함.

**pass 직전 로그 참고**: 직전 필링(2018Q3, r20181114002900) pass 실행 시
`[report_lines/R162] SCE 부호 복원 1셀 (20180814002744)` 디버그 로그가 자동으로
찍혔음 — 이 rcept에 대해 R162 규칙이 이미 1개 셀을 복원 시도했다는 뜻이나,
실제로는 자기주식 열 전체(기초자본·반기말 등 다수 행)에 걸쳐 부호 누락이
남아있어 복원이 불완전한 것으로 추정(1셀만으로는 위 행 항등식이 닫히지 않음,
코드 미조사).

**확인 범위**: [별도] BS(43)·IS(24)·CF(53), [연결] BS(50)·IS(35)·CF(62)·SCE(139)는
전부 DART 원문과 셀단위 전수대조 완료, 불일치 0건. [별도] SCE(65)만 자기주식
열의 부호 누락 확인.

### 처리
- 이슈#28/R162와 완전히 동일한 근본원인(원문 자체가 괄호/마이너스를 빼먹은
  패턴)의 재발이며, MEMORY.md에 이미 "R162 완료+캠페인 redo 6건 대기"로
  추적 중인 항목과 같은 클래스이므로 별도 재notify 없이 이슈 문서 기록만 남김.
- `fail --note` 로 표시 후 다음 보고서로 진행 (파서 코드 수정 없음).

**★후속(같은날 세션, 자매 필링에서 재확인)**: 바로 다음 자매 필링인 2018H1
[기재정정]반기보고서(r20180911000187, 2018.09.11 접수, 원본 대비 약 4주 후
정정 — 비동일자 정정이라 독립 재검증 수행)에서도 [별도] BS/IS/CF·[연결]
BS/IS/CF/SCE 8개 스코프 중 7개는 전부 DART 원문과 일치(불일치 0건)하고,
[별도] SCE만 원본과 완전히 동일한 자기주식 부호누락이 DART 원문 자체에도
그대로 재현됨을 직접 확인(기초자본 행 자기주식 `1,533,468,003,532` 양수,
원본과 byte-identical). 이 정정본도 `fail`로 처리.

## 33. 고려아연 2017H1 반기보고서 (r20170811000919) — [별도] 자본변동표 "연차배당" 행이 두 회계기간(2016H1·2017H1) 모두 양수인데, R162 부호복원은 한쪽(2017H1)만 적용되어 나머지 한쪽(2016H1 비교연도)은 DART·DB 모두 양수로 남음 (이슈#28/R162와 동일 근본원인, ★POSCO 아닌 회사에서 첫 확인, 재notify 불필요)

- **회사**: 고려아연 (00102858) KOSPI 시총순위 29
- **보고서**: 2017H1 반기보고서, 접수번호 20170811000919
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20170811000919
- **CSV**: `layer2_review/KOSPI/00102858_고려아연/half/2017/20170811000919_review.csv`

**증상**: [별도] 자본변동표(tables[5]) "자본에직접반영된소유주와의거래>연차배당"
행이 두 회계연도 블록(2016.01.01→2016.06.30, 2017.01.01→2017.06.30) 모두
`150,231,040,000`으로 표시(괄호/마이너스 없음). 행 항등식으로 검증한 결과 두
블록 모두 배당금을 음수로 처리해야만 이익잉여금·자본총계 롤포워드가 정확히
닫힘(예: 2017블록 이익잉여금 기초 5,191,209,256,866 + 당기순이익
298,343,180,055 − 150,231,040,000 = 기말 5,339,321,396,921, 정확히 일치;
자본총계도 기초 5,343,333,581,425 + 298,343,180,055(NI) + 21,267,095,663
(매도가능금융자산평가손실) − 150,231,040,000(배당) = 기말 5,512,712,817,143,
정확히 일치). 그런데 DB값은 **2017블록만 음수로 정상 복원**돼 있고(DART 원문과
비교시 부호가 다르게 나와 diff에 걸림), **2016블록(비교연도)은 DART·DB 모두
양수로 남아있어** diff 도구로는 한쪽이 잡히지 않음(양쪽이 똑같이 틀려서
"불일치"가 발생하지 않는 구조적 맹점 — 캠페인 규칙 문서에 이미 기록된
단방향대조 맹점과 유사한 양상). 이는 R162 자동복원(`sce_sign_repair.py`)이
매 rcept 전체가 아니라 일부 셀에만 적용되는 알려진 한계(이슈#32의 "복원이
불완전" 서술과 동일 클래스)가 POSCO 외 회사에서도 재현됨을 보여줌.

**pass 직전 로그 참고**: 이 rcept 자체의 처리 시 `[report_lines/R162] SCE 부호
복원 1셀 (20170811000919)` 로그가 찍혔음 — 1개 셀만 복원되어 2017블록만
해소되고 2016블록은 미해소.

**확인 범위**: [별도] BS(40)·IS(18)·CF(33), [연결] BS(42)·IS(30)·CF(35)·SCE(87)는
전부 DART 원문과 셀단위 전수대조 완료, 불일치 0건. [별도] SCE(36) 중 35셀
일치(2017블록 배당 2셀은 DB가 이미 음수로 복원돼 DART 양수와 비교시 불일치로
잡힘), 2016블록 배당 2셀은 항등식으로 검증한 결과 DART·DB 둘 다 오류(양수)
상태로 확인.

### 처리
- 이슈#28/#32/R162와 동일 근본원인(원문 자체가 괄호/마이너스를 빼먹었고,
  자동복원이 해당 rcept의 일부 셀에만 적용되는 알려진 한계)의 재발이며, 이미
  MEMORY.md·이슈#32에 "복원이 불완전"으로 추적 중인 항목과 같은 클래스이므로
  별도 재notify 없이 이슈 문서 기록만 남김.
- `fail --note` 로 표시 후 다음 보고서로 진행 (파서 코드 수정 없음).

**★후속(같은 회사, 2022H1 반기보고서 r20220816001335)**: 같은 고려아연의
5년 뒤 필링에서도 [별도] 자본변동표 "배당"·"기타포괄손익-공정가치측정금융자산
처분" 행에서 동일 클래스 재발 확인.
- 2022블록(당기): "기타포괄손익-공정가치측정금융자산 처분" 행(기타포괄손익
  누계액 열, 187,831,104)과 "배당" 행(합계 열, 353,484,800,000) 둘 다 DART
  원문에 양수로 표시되나 DB에는 이미 음수로 복원돼 있어 diff에서 불일치로
  잡힘 — 각각 열 자체의 기초/기말 항등식(OCI누계액: -38,164,321,679 +
  Y = -64,263,459,900 → Y=-187,831,104 / 합계: 407,360,063,045(순이익) −
  25,911,307,117(OCI평가손실) + 0(OCI처분,합계란은 DART상 공란) + Z =
  27,963,955,928(기말-기초) → Z=-353,484,800,000)으로 음수가 정답임을 재확인.
- 2021블록(전기 비교연도): "배당" 행(이익잉여금·합계 열 둘 다
  265,113,600,000)은 DART·DB 모두 양수로 남아있어 diff로는 안 잡히나, 합계열
  항등식(순이익 380,811,829,251 − OCI평가손실 2,695,504,141 + X =
  113,002,725,110(기말-기초) → X=-265,113,600,000)으로 검증 결과 이 역시
  음수가 정답 — 이슈#33 최초 사례(POSCO 2018H1)와 완전히 동일한 "비교연도만
  미복원" 패턴.
- 같은 회사·같은 행 패턴(SCE 소유주거래 행)에서 반복 확인됨 — R162 자동복원의
  "일부 셀만 적용" 한계가 특정 계정과목(배당·OCI재분류) 계열에서 구조적으로
  재발하는 것으로 보이나, 파서 코드 조사는 camp_err_review 세션 소관이므로
  이 세션은 기록만 남기고 `fail` 처리.

**★후속2(같은 회사, 2026Q1 분기보고서 [기재정정] r20260813001749)**: ★처음으로
[연결](별도가 아닌) 자본변동표에서, 그리고 ★diff로는 전혀 잡히지 않는(당기
블록 자체가 DART·DB 둘 다 양수라 "일치"로 통과하는) 형태로 재발 확인.
- 이 필링은 원 분기보고서(r20260515002132, 이미 PASS)의 연결재무제표 재작성
  기재정정으로, "연결재무제표의 재작성으로 인하여..." 라는 정정사유가 명시돼
  있음. 원 필링(r20260515002132)에서는 [연결] SCE "연차배당" 행이 DART·DB
  모두 음수(`-407,864,640,000`)로 정상 표시됐었는데, 이 정정 필링에서는
  DART 원문 자체가 괄호 없이 양수(`407,864,640,000`)로 렌더링되고 DB도 그대로
  양수로 적재됨 — diff 비교시 양쪽이 동일(둘 다 양수)이라 **불일치가 전혀
  발생하지 않아 자동 diff로는 원리적으로 검출 불가**, 오직 열 항등식으로만
  발견됨: 이익잉여금 열 기준 기초 6,379,173,989,294 + 당기순이익
  354,597,323,695 − 139,895,841(확정급여재측정) − 20,229,721(지분법이익잉여금
  변동) − X = 기말 6,325,746,547,427 → X = 407,864,640,000(양수 갚아야 닫힘,
  즉 "빼는 방향"이 맞고 표시부호는 음수여야 함).
- 같은 필링의 [별도] 자본변동표는 같은 배당 행이 정상적으로 음수
  (`-407,864,640,000`)로 표시돼 있어, 결함이 이 필링의 **연결 SCE 렌더링에만
  국한**됨을 확인.
- 확인 범위: 이 필링의 별도/연결 BS·IS·CF·SCE 전 스코프(47/18/32/71 ·
  55/29/50/146, 총 448행) DART 원문 전수대조 완료, 이 셀 1개(연결 SCE 연차배당
  당기블록, 이익잉여금·지배기업소유지분합계·자본합계 3열 동시 영향) 외 전부
  일치.
- 이슈#33과 동일 근본원인(R162 부분복원 한계)의 재발이나 ★"비교연도만
  미복원"이 아니라 "당기 블록 자체가 원문·DB 모두 미복원"인 변종이며, ★연결
  SCE에서 최초 확인 — 재notify는 하지 않되(원인 클래스는 이미 추적 중), 다음
  camp_err_review 인수인계 시 이 변종(당기 블록도 diff로 안 잡힘)을 함께
  전달할 가치가 있음. `fail --note` 로 표시 후 다음 보고서로 진행.

**★후속3(같은 회사, 2025FY 사업보고서 [기재정정] r20260813001726)**: ★처음으로
"행 내부 구성요소와 소계가 서로 다른 부호로 갈라져 그 행 자체가 내부모순"인
변종 확인 — 지금까지의 "행 전체가 부호 누락"과는 다른 세분화된 패턴.
- 이 필링은 앞선 기재정정 2건(20260325000008, 20260601001725, 둘 다 주석/임원
  정정으로 BS/IS/CF/SCE 무관, 이미 PASS)과 달리 사유가 "재무제표 재작성"이며
  실제로 연결 BS/IS/SCE 값이 최초제출본과 다르게 바뀜(예: 연결 이익잉여금
  6,542,819,899,592 → 6,379,173,989,294). 로딩 로그에도
  `[report_lines/R162] SCE 부호 복원: 2셀 (20260813001726)`이 찍혔음.
- [연결] 자본변동표 "자기주식취득" 행(2024블록)에서 자본잉여금·자본조정 두
  구성요소 셀은 DART 원문이 양수(괄호없음)인데 DB는 이미 음수로 정상 복원돼
  diff에 잡힘(`-2,714,535,052`, `-2,124,881,635,500`). 그런데 **같은 행의
  소계 2칸(지배기업소유지분합계·자본합계, 둘 다 `2,127,596,170,552`)은
  DART·DB 모두 양수로 남아 diff를 통과함** — 그러나
  `2,714,535,052 + 2,124,881,635,500 = 2,127,596,170,552`로 구성요소의 합이
  소계와 정확히 일치하므로, 구성요소가 음수라면 소계도 반드시 음수여야
  한다(그렇지 않으면 그 행 자체가 산술적으로 자기모순). 즉 R162 부분복원이
  "행 안의 특정 열만" 고치고 "같은 행의 소계 열"은 그대로 둔 사례 — 이슈#33/
  후속1/후속2의 "비교연도 미복원"·"당기 블록 자체 미복원"과는 또 다른
  세번째 갈래(행 내부 열단위 불일치).
- ★교차확인: 같은 필링의 [별도] 자본변동표에서는 동일 거래(자기주식취득,
  자본잉여금 `(2,714,535,052)`·자본조정 `(2,124,881,635,500)`·자본합계
  소계 `(2,127,596,170,552)`)가 DART 원문에 전부 괄호(음수)로 정상 표시돼
  있어 결함이 이 필링의 **연결 SCE에만 국한**됨을 재확인(★후속2의 관찰과
  동일 구도: 별도는 멀쩡, 연결만 결함).
- 확인 범위: 별도/연결 BS·IS·CF·SCE 전 스코프(44/18/54/198 · 54/28/62/480,
  총 938행) DART 원문 전수대조 완료, 위 2셀(연결 SCE 자기주식취득 소계열) 외
  전부 일치(연결 BS/IS/CF는 재작성된 신규 값으로 정확히 일치 확인).
- 이슈#33과 동일 근본원인(R162 부분복원 한계)의 세번째 변종이며, 기록만
  남기고 재notify 없이 `fail --note` 로 표시 후 다음 보고서로 진행.

**★후속4(같은 회사, 2017Q1 분기보고서 r20170515004148, 2026-09-23)**: ★처음으로
"배당(연차배당)이 아닌 다른 열(자본조정)"에서 이 회사의 R162류 부호누락이
확인됨. [별도] 자본변동표(tables[5]) "자본조정" 열이 표 전체(2015.01.01
기초자본 행부터 2017.03.31 최종 기말자본 행까지, 4개 회계기간 전부)에 걸쳐
`38,478,851,840`으로 양수 표시(괄호 없음) — DART 원문 자체에 부호가 통째로
누락됨. 행 항등식으로 검증: 임의의 기초/기말자본 행에서 자본금+자본잉여금+
자본조정+기타포괄손익누계액+이익잉여금 합이 표시된 자본합계와 정확히 일치
하려면 자본조정이 반드시 음수여야 함(예: 2015.01.01 기초자본 —
94,350,000,000+54,599,469,714−38,478,851,840+24,370,619,561+
4,437,702,901,291 = 4,572,544,138,726, 표시된 합계와 정확히 일치; 같은
필링의 [별도] 재무상태표는 이 값을 "자본조정 (38,478,851,840)"으로 이미
괄호(음수) 정상 표시). 이 R162류 결함은 **표 전체가 일관되게 틀려서** 어느
행도 "다른 행과 다르게" 튀지 않아 R162 자동복원(`sce_sign_repair.py`)이
발동하지 않았고(같은 rcept 로딩 로그의 "SCE 부호 복원 10셀"은 별개 열
"연차배당"·"확정급여제도 재측정요소"·"매도가능증권평가이익"·"종속기업
유상증자"·"자본에인식된거래합계" 8셀([별도])+2셀([연결])만 정상 복원),
DB도 DART 원문을 그대로(양수) 충실히 적재한 상태 — 값 자체는 원문과
일치하므로 이슈#28/#29/#32 선례와 동일하게 **원문 자체 결함, 파서/DB
오적재 아님**으로 판단해 `pass --note`로 표시. ★향후 같은 증상(SCE
특정열이 전체 기간에 걸쳐 일관되게 부호누락, BS와 대조·행 항등식으로만
드러남)은 재notify 불필요, 기록만 남기고 진행.

---

## 34. SK이노베이션 2023FY 사업보고서 (r20240320000950) — [연결] 자본변동표(SCE) 라벨 셀 ROWSPAN=2 로 두 물리행에 걸쳐 있는데, 값이 담긴 두 번째(라벨 없는) 물리행 전체가 DB에서 통째로 결측 — ★신규 메커니즘, camp_err_review 통지 대상

- **회사**: SK이노베이션 (00631518) KOSPI 시총순위 32
- **필링**: 2023 사업보고서(연차, 3개년 비교), r20240320000950
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20240320000950
- **CSV**: `layer2_review/KOSPI/00631518_SK이노베이션/annual/2023/20240320000950_review.csv`

**증상**: [연결] 자본변동표를 브라우저에서 원문 그대로 SHA-256 해시 대조하니 342개 값 중
CSV(DB) 기대값(211개)보다 정확히 10개가 더 많이 추출됨. `<tr>` 단위로 원문 HTML을 직접
조사한 결과, "기타포괄손익-공정가치측정 금융자산 평가손익" 행의 라벨 `<td>`가 `ROWSPAN=2`로
선언되어 있고, 첫 번째 물리행(라벨을 담은 행)은 데이터 셀이 전부 공란, **실제 9개 열의 값은
전부 두 번째 물리행(라벨 셀 없이 데이터 9칸만 있는 행)에 들어있음**을 확인:

```
row(라벨행, rowspan=2): ["기타포괄손익-공정가치측정 금융자산 평가손익","","","","","","","","",""]
row(값행, 라벨 없음)  : ["","","","","","(48,279,660)","(48,279,660)","(4,822,811)","(53,102,471)"]
```

이 패턴이 [연결] SCE 표 안에서 **3번(2021/2022/2023 각 연차 롤포워드 섹션마다 1번씩)**
반복되고, 그때마다 값행의 셀 개수가 다름(2021구간=1개 값, 2022구간=4개 값, 2023구간=5개 값,
합계 정확히 10개 = 관측된 해시 불일치분과 정확히 일치). CSV(DB)에는 이 세 값행이 모두
누락되어 있음(라벨 "기타포괄손익-공정가치측정 금융자산 평가손익"이 CSV에 아예 등장하지 않는
연차도 있고, 등장해도 값 개수가 부족함).

**항등식으로 결측 확인**: 값행 자체의 산술(-48,279,660(기타자본구성요소)
= -48,279,660(지배기업소유주지분합계, OCI만 영향) 이고, -4,822,811(비지배지분)+
-48,279,660 = -53,102,471(자본합계))이 내부적으로 닫혀 있어 원문 렌더링 오류가 아니라
**실제로 존재하는 유효한 회계 항목**임을 확인. [연결] BS·IS·CF와 [별도] 4개 스코프는
전부 CSV 기대값과 SHA-256 완전일치(별도 SCE도 동일 rowspan 패턴 없음, 정상) — 이 결함은
[연결] SCE 한 곳에만 존재.

**추정 메커니즘(미확인, 코드 미조사)**: R11(occupied-grid, ROWSPAN/COLSPAN 확장,
`docs/PARSING_RULES.md` 완료 표시)이나 R134/R135(SCE 전용, 라벨 유실 방향)와 증상이
다름 — R135는 "라벨이 여러 물리 셀에 나뉜 행에서 두 번째 이후 라벨 손실"인 반면, 이번
건은 "라벨은 하나(ROWSPAN으로 위쪽 행에만 존재)인데 그 라벨에 대응하는 **값 자체가 담긴
아래쪽 물리행 전체**가 통째로 드롭"되는 패턴 — R11 occupied-grid 확장이 SCE 본문에
전면 적용됐는지 재확인 필요(R11 문서상 "본문(BS/IS/CF)은 실측 0건"이라고만 명시, SCE는
별도 언급 없음).

**영향 범위(미조사)**: 이 회사([연결] SCE)에서만 3회 발견. 다른 회사에서도 SCE 라벨 셀에
ROWSPAN이 쓰이는 경우 동일 패턴 재현 가능성 있음 — 전수 스캔 필요.

### 처리
- `fail --note`로 표시 후 다음 보고서로 진행(파서 코드 수정 없음, 이 세션은 캠페인 전용).
- **신규 패턴**이므로 peer(camp-err-review-c2)에게 notify.

---

## 35. 한미반도체 2023H1 반기보고서 (r20230814001921) — [연결] 자본변동표(SCE) 라벨 셀 ROWSPAN=2, 값이 담긴 두 번째 물리행이 결측 — R166(이슈#34)과 같은 메커니즘이나 ★라벨행이 "공란"이 아니라 "전부 0"인 하위 케이스, R166 수정 코드가 "이미 값 있는 열"로 판정해 자동 거부한 것으로 추정

- **회사**: 한미반도체 (00161383) KOSPI 시총순위 33
- **필링**: 2023 반기보고서, r20230814001921
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20230814001921
- **CSV**: `layer2_review/KOSPI/00161383_한미반도체/half/2023/20230814001921_review.csv`

**증상**: [연결] 자본변동표를 원문과 SHA-256 전체 대조하니 CSV 기대값(112개)보다 13개
더 많이 추출됨(occupied-grid 125개). `<tr>` 단위로 원문 HTML을 직접 조사한 결과, 두 개
회계연도(2022반기/2023반기) 롤포워드 섹션 각각에서 "자기주식처분이익" 행의 라벨 `<td>`가
`ROWSPAN=2`로 선언되어 물리행 2개(하나는 라벨 보유, 하나는 라벨 없음)에 걸쳐 있음:

```
2022반기 구간(정상— 값이 라벨행에 있음, DB와 원문 일치):
  row(라벨행, rowspan=2): ["자기주식처분이익","0","0","(8,097,836,340)","0","0","(8,097,836,340)","(8,097,836,340)"]
  row(값 없는 둘째행)   : ["0","0","0","0","0","0","0"]  ← 전부 0, DB도 이 행은 무시(정상)

2023반기 구간(★결측 발생 — 값이 둘째행에 있는데 라벨행도 전부 "0"이라 이미 채워진 것으로 판정):
  row(라벨행, rowspan=2): ["자기주식처분이익","0","0","0","0","0","0","0"]  ← 전부 "0"(공란 아님)
  row(값 있는 둘째행)   : ["0","5,464,171,128","0","0","","5,464,171,128","5,464,171,128"]
```

CSV(DB)에는 2023반기 구간의 "자기주식처분이익"이 7개 모두 0으로 적재되어 있고(순번
92-98), 둘째행의 실제 값(자본잉여금 +5,464,171,128 → 지배기업지분합계·자본합계도
+5,464,171,128)이 전부 유실됨. 동일 숫자 5,464,171,128이 [별도] 자본변동표 자기주식처분이익
행(CSV 205,208번)에는 정상 적재되어 있어, **원문 자체에 실재하는 유효한 회계 항목**이며
렌더링 오류나 우연의 일치가 아님을 확인.

**항등식으로 결측 확인**: 둘째행 단독으로도 0(자본금)+5,464,171,128(자본잉여금)+0+0+
blank(이익잉여금) = 5,464,171,128(지배기업지분합계) = 5,464,171,128(자본합계)로 내부
항등식이 닫혀 유효한 데이터임을 확인. 반면 라벨행(전부 0)도 트리비얼하게 항등식이
닫히므로(0=0), 두 행 모두 "자기완결"로 보여 R166 수정 코드의 "이미 값 있는 열은 건드리지
않는다" 규칙에 걸려 둘째행이 자동 거부됐을 것으로 추정(코드 미조사, 가설).

**R166(이슈#34)과의 관계**: 동일 ROWSPAN=2 메커니즘이나, 이슈#34의 세 구간은 모두 라벨행이
"전부 공란"(빈 문자열)이라 R166 수정으로 정상 복구됨. 이번 건은 라벨행이 "전부 0"(공란이
아닌 명시적 텍스트)이라 R166의 "이미 값이 있는 열은 건드리지 않는다" 판정 조건에 걸려
★같은 필링 내에서도 2022반기 구간(둘째행이 전부 0 → 정상, 문제없음)과 2023반기 구간(둘째행에
실값 → 결측)이 **정반대 방향**으로 나타남 — R166 커밋이 언급한 "2021 TR6/7 자기완결·거부"
사례와 구조적으로 유사하나, 그 경우는 라벨행에 진짜 유의미한 비0 값이 있었던 반면 이번 건은
라벨행이 **전부 트리비얼한 0**이라는 점이 다름(실측 필요).

**영향 범위(미조사)**: 이 회사([연결] SCE)에서 1회 확인. "라벨행 전부 0 + 둘째행 실값"
조합이 다른 회사/구간에도 있는지 전수 스캔 필요.

### 처리
- `fail --note`로 표시 후 다음 보고서로 진행(파서 코드 수정 없음, 이 세션은 캠페인 전용).
- **R166과 관련은 있으나 세부 케이스가 다름**이므로 peer(camp-err-review-c2)에게 notify.

---

## 36. 두산 2025FY 사업보고서 (r20260323000945) — [별도] 자본변동표(SCE) "배당금의 지급" 행 자본합계 셀이 원문에 소수점(`(35,851.435)`)으로 표기되어 있는데 DB에서 통째로 결측 — ★R157~R159(소수 표기 금액)와 다른 신규 케이스: 값 왜곡이 아니라 완전 결측

- **회사**: 두산 (00117212) KOSPI 시총순위 35
- **필링**: 2025 사업보고서, r20260323000945
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260323000945
- **CSV**: `layer2_review/KOSPI/00117212_두산/annual/2025/20260323000945_review.csv`

**증상**: [별도] 자본변동표를 원문과 SHA-256 전체 대조하니 CSV 기대값(233개)보다 원문
occupied-grid가 1개 더 많음(234개). 위치별 diff로 정확한 지점을 확인한 결과, 2025.01.01~
2025.12.31 롤포워드 구간의 "배당금의 지급" 행에서 자본합계(6번째 데이터 열) 셀 원문이
`(35,851.435)`로 소수점 표기되어 있는데, DB에는 이 셀이 통째로 빠져 있고 그 뒤 모든 셀이
한 칸씩 앞으로 당겨져 적재되어 있음(값 왜곡이 아니라 완전 결측).

**원문 해당 행 전체(td 그대로)**:
```
["배당금의 지급","0","0","0","0","(35,851)","(35,851.435)"]
```
→ 자본금=0, 자본잉여금=0, 기타자본항목=0, 기타포괄손익누계액=0, 이익잉여금=(35,851),
자본합계=(35,851.435).

**항등식으로 결측·정답값 확인**: 이 행은 이익잉여금 열만 변동하므로 자본합계도 정확히
이익잉여금과 같은 -35,851이어야 함(다른 5개 열이 모두 0). 즉 원문의 `.435`는 DART
원문 자체의 오기(정수 열과 소수 열이 동일 금액이어야 하는데 소수부가 잘못 붙음)이고,
**정답은 -35,851**임이 행 항등식만으로 확정됨(추측 아님, R159 등재조건 (나) 충족).

**DB 적재 상태(positional diff로 확인)**:
```
원문 순서(6개씩):  ..., 0, -35851, -35851.435, 0, 0, 20131, ...
DB(CSV) 순서:      ..., 0, -35851,             0, 0, 20131,    0, ...
```
`-35851.435` 셀 자체가 빠지고 그 뒤 모든 셀이 한 칸씩 당겨짐 — 이후 20131개 남은 셀도
전부 위치가 밀려 있었으나 값 자체는 모두 원문과 일치(밀림만 발생, 왜곡 없음).

**R157/R158/R159와의 관계(다름)**: `r157-r159-decimal-amount-defects-2026-09-22` 메모에는
"소수 표기 금액 3계열... 백필 완료 — 9필링 전부 복원, 알려진 잔여 0"으로 닫혀 있음. 이번
건은 그 3계열(①배수 전 절삭 R157, ②콤마→마침표 렌더 R158, ③원문오타 예외목록 R159) 중
어디에도 해당하지 않는 **새로운 실패모드** — 값이 틀리게 적재된 게 아니라 **아예 안
실린 결측**이며, 같은 메모에 남겨진 경고("SCE(`report_lines._grid_body_rows`, 셀 개별
파싱)는 왜곡값이 그대로였다")와 일치하는 방향 — SCE 전용 그리드 파싱 경로가 소수점 셀을
만나면 파싱 실패 후 스킵(→결측)하는 것으로 추정(코드 미조사, 가설).

**영향 범위(미조사)**: SCE 그리드 파싱에서 소수점 셀을 만나는 다른 회사/필링에도 동일
결측 가능성 있음 — 전수 스캔 필요.

### 처리
- `fail --note`로 표시 후 다음 보고서로 진행(파서 코드 수정 없음, 이 세션은 캠페인 전용).
- R157-159와 다른 신규 실패모드이므로 peer(camp-err-review-c2)에게 notify.

### ★결론(peer camp-err-review-c2 조사, 2026-09-23) — 파서 결함 아님, R160 정책대로 동작

- **①"한 칸씩 당겨졌다"는 내 진단이 틀렸다** — SCE 그리드 경로는 셀을 순번이 아니라
  `grid_col`(열 라벨)로 배치하므로, 한 셀이 빠져도 뒤 셀들이 밀리지 않는다. 실제로
  `col_index=4 이익잉여금 = -35,851,000,000`은 제자리에 정상 적재돼 있고,
  `col_index=5 자본합계`만 결측이다. 내가 쓴 **평탄한 값 나열 기준 positional diff**가
  착시를 만든 것 — 값 하나가 빠지면 그 뒤 인덱스가 전부 1씩 어긋나 보여 "밀림"으로
  잘못 읽었다. **교훈: 이런 판정은 (행, 열라벨, 값) 삼중 비교로 해야 하며, 평탄
  순번 비교만으로는 결측과 오배치를 구분할 수 없다.**
- **②결측 자체는 R160(마침표 셀 적재금지, 사용자 정책, [[r160-r161-dot-policy-and-note-title-2026-09-22]]) 이 의도한 동작이다.**
  사용자 정책: "'.'으로 된 큰 금액 오타를 반올림/절삭해서 적재하지 말고, 결측으로
  남기고 확인요청 리스트로 보고하라." 절삭·반올림 둘 다 10³배 작은 값이 그럴듯하게
  들어가 검산을 통과하므로, 파서는 의도적으로 결측 상태를 유지하고 사용자 확인을
  받는다. **파서를 고칠 대상이 아니다.**
- **③이 발견이 실제로 드러낸 문제**: `docs/qa/dot_typo_candidates_2026-09-22.md`의 근거
  스캔이 `--breadth`(회사당 1필링)로만 돌아 2,528개사 각 1건(전체 2015+의 약 2%)만
  훑었고, 두산은 스캔됐지만 다른 필링(`20260515001456`)이었을 뿐 이 필링
  (`20260323000945`)은 스캔 대상에 없었다. → peer가 전수 스캔 재실행 중, 완료 후
  확인요청 목록을 전량 갱신해 사용자 판단을 받을 예정.
- **참값은 행 항등식으로 자기증명**: 이 행은 이익잉여금 열만 변동하므로
  `자본합계 = 이익잉여금 = -35,851`(백만원) — 내 원래 판정과 동일. 근거가 강해
  확인요청 목록에 "항등식 확정"으로 표시되어 오를 예정이나, **적재는 사용자 승인
  후**(R160 정책).
- **최종 분류**: `fail`(파서 결함) 아님 → **R160 정책 대기(확인요청 목록 등재 예정)**로
  재분류. camp_run 큐에서는 `skip --note`로 처리(진행 재개, 신규결함 아님).

---

## 37. SK이노베이션 2025FY 사업보고서 (r20260316000827) — [별도] 자본변동표(SCE) "주식보상비용" 행 자본금 열(2024 롤포워드 구간) 셀이 원문에 소수점(`801.523`)으로 표기되어 있는데 DB에서 결측 — 이슈#36과 동일 근본원인(R160 정책대로 동작, 파서 결함 아님) 재발, ★재notify 불필요

- **회사**: SK이노베이션 (00631518) KOSPI 시총순위 32
- **필링**: 2025FY 사업보고서, r20260316000827 (peer의 R166 라벨-ROWSPAN 개정 재적재 이후 재검증 중 발견)
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20260316000827
- **CSV**: `layer2_review/KOSPI/00631518_SK이노베이션/annual/2025/20260316000827_review.csv`

**발견 경로**: peer의 R166 재적재 요청(20260316000827 포함 3건 재검증) 처리 중, 8-scope
SHA-256 전체대조에서 sep-SCE만 유일하게 불일치(연결·별도 BS/IS/CF, 연결 SCE는 전부
CSV 기대 해시와 완전 일치, sep-SCE만 어긋남). 값별 카운트 대조(항목명별 그룹 개수를
8의 배수와 비교)로 위치를 좁혀 "주식보상비용" 라벨이 24개(8열×3개년) 기대와 달리
23개만 있음을 확인, DART 원문 rowspan-aware 재구성으로 해당 물리행을 직접 대조.

**원문 해당 행(rowspan-aware 재구성, 2024 롤포워드 구간)**:
```
["주식보상비용","0","0","801.523","12,166,592","12,968,115","0","0","12,968,115"]
```
→ 자본금=0, 주식발행초과금=0, 자기주식=801.523(소수점 오타), 기타=12,166,592,
기타불입자본합계=12,968,115, 이익잉여금=0, 기타자본구성요소=0, 자본합계=12,968,115.

**항등식으로 참값 확인**: 기타불입자본합계 = 주식발행초과금+자기주식+기타 이어야
하므로 `0 + 801,523 + 12,166,592 = 12,968,115` — 정확히 일치. 원문의 `801.523`은
DART 원문 자체의 오타(콤마가 마침표로 잘못 인쇄)이고 **정답은 801,523**임이 행
항등식만으로 확정됨(R159 등재조건 (나) 충족, 추측 아님).

★R158(같은 행의 정수 짝 대조로 자동 복원)은 여기서 발동하지 않는다 — R158은 깨진
셀의 숫자열이 같은 행의 **다른 정수 칸의 숫자열 접두사**여야 복원하는데, 이 행에는
"801523"을 접두사로 갖는 다른 정수 칸이 없다(짝은 **합 항등식**으로만 확정됨, R158이
찾는 리터럴 접두사 짝과는 다른 근거). 그래서 R160(마침표 셀 결측+확인요청) 경로로
빠진 것으로 보이며, 이슈#36(두산 `20260323000945`)과 정확히 같은 메커니즘.

**DB 적재 상태**: [별도] 자본변동표 CSV에서 "주식보상비용" 라벨이 총 23개(정상이면
8열×3개년=24개여야 함) — 2024 롤포워드 구간의 자기주식 열 1개 셀만 빠져 있고 나머지
23개(2023·2025 두 개년 8개씩 + 2024년의 나머지 7개)는 전부 원문과 일치.

**확인 범위**: 이 필링의 8-scope 전체(con-bs/is/cf/sce, sep-bs/is/cf/sce)를 SHA-256으로
전체 대조 — sep-sce 이 1건(주식보상비용 자기주식 열)을 제외한 나머지 전부 완전 일치
(con-sce 720개 값 포함, R166 재적재 반영 후 정상 확인). peer가 사용한 rowspan-aware
그리드 재구성 방식을 그대로 재사용해 con-sce/sep-sce 전체를 물리행 단위로 재검증함.

### 처리
- 이슈#36과 동일 근본원인(R160 정책이 의도대로 마침표 셀을 결측 처리)이므로 파서 결함
  아님. camp_run 큐에서는 `skip --note`로 처리(진행 재개). peer가 진행 중인
  dot_typo_candidates 전수 스캔·확인요청 목록 갱신에 이 셀(SK이노베이션
  `20260316000827` 별도 SCE 주식보상비용/자기주식/2024, 항등식 확정값 801,523)이
  포함되도록 참고용으로만 알림 — 재notify(fail+대기)는 불필요.

---

## 38. SK텔레콤 2021Q1 분기보고서 (r20210517001554) — [별도] 자본변동표(SCE) "2021.03.31 (기말자본)" 행 자기주식 열이 DB에 양수(+2,169,660)로 적재되어 있는데 항등식상 음수(-2,169,660)여야 함 — ★신규 부호오류, camp_err_review 통지 대상

- **회사**: SK텔레콤 (00159023) KOSPI 시총순위 37
- **필링**: 2021Q1 분기보고서, r20210517001554
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20210517001554
- **CSV**: `layer2_review/KOSPI/00159023_SK텔레콤/quarter/2021/20210517001554_review.csv`

**발견 경로**: 8-scope 전체 원문 대조 중 [별도] 자본변동표(SCE, headerRows=3,
dataWidth=10) 마지막 행 "2021.03.31 (기말자본)"을 원문 tr 순회로 뽑아 DART 원문
raw 텍스트로 재확인:
```
["2021.03.31 (기말자본)","44,639","2,915,887","2,169,660","398,759","1,528","900,673","245,841","16,488,288","501,135","17,279,903"]
```
열 순서(sep-SCE 10열): 자본금/주식발행초과금/자기주식/신종자본증권/주식선택권/기타/
기타불입자본합계/이익잉여금/기타자본구성요소/자본합계.

**항등식으로 참값 확인**: 기타불입자본합계(7번째 값, 245,841) = 주식발행초과금 +
자기주식 + 신종자본증권 + 주식선택권 + 기타 이어야 함.
- DB 적재값 그대로 대입(자기주식=+2,169,660, 기타=-900,673. CSV에서 기타는 이미
  음수로 정상 적재됨): `2,915,887 + 2,169,660 + 398,759 + 1,528 − 900,673 =
  4,585,161` ≠ 245,841 (차액 4,339,320 = 정확히 2×2,169,660).
- 자기주식만 음수로 바꿔 대입: `2,915,887 − 2,169,660 + 398,759 + 1,528 − 900,673 =
  245,841` — **정확히 일치**.
- 자본합계(10번째 값)도 독립 검산: `자본금(44,639) + 기타불입자본합계(245,841) +
  이익잉여금(16,488,288) + 기타자본구성요소(501,135) = 17,279,903` — 상위 합계는
  일치하므로 이 결함은 기타불입자본합계 **내부의 자기주식/기타 하위분해에서만** 발생.
- 인접 비교행("2020.03.31 (기말자본)": 자기주식 -1,696,997, 기타 -903,332,
  기타불입자본합계 715,651)은 두 열 다 음수로 이미 올바르게 적재되어 있어, 부호오류는
  이 필링의 "2021.03.31 (기말자본)" 한 행 한 셀(자기주식)에 국한된 것으로 보임.

**DART 원문 자체의 표기**: 원문 HTML에는 이 행의 자기주식·기타 두 셀 모두 괄호/음수
부호 없이 양수 형태("2,169,660", "900,673")로 표기되어 있다(R162류 "음수 괄호
누락" 패턴과 유사). 그런데 DB는 기타 열은 이미 음수(-900,673)로 올바르게 복원해
적재했으면서, 바로 옆 자기주식 열만 원문 그대로 양수로 적재했다 — 같은 행 안에서
복원 로직이 한쪽 셀에는 적용되고 다른 쪽 셀에는 적용되지 않은 것으로 보인다(R162
부호복원 로직의 열별 적용 누락 가능성, 정확한 근본원인은 파서 코드 확인 필요).

**확인 범위**: 이 필링의 8-scope 전체(con-bs/is/cf/sce, sep-bs/is/cf/sce)를 원문
tr 순회 + 파이썬 element-wise 대조로 전부 확인 — 이 자기주식 셀 1건을 제외한 나머지
전부 원문과 정확히 일치(con-bs 68/con-is 38/con-cf 45/con-sce 82/sep-bs 60/
sep-is 31/sep-cf 33/sep-sce 71, 총 428행 중 결함은 이 1셀뿐).

### 처리
- ★신규 결함(부호오류) — 이슈#28/33(R162 계열)과 증상은 유사하나 "같은 행 인접 두
  음수 셀 중 하나만 복원되고 하나는 원문 그대로 적재"라는 세부 메커니즘은 처음 확인.
  camp_run에서는 파서 코드 수정 금지 규약에 따라 `fail --note`로 처리하고 대기,
  peer(camp-err-review-c2)에게 notify 후 지시를 받는다.

---

## 39. SK텔레콤 2020FY 사업보고서 (r20210317000804) — [별도] 자본변동표(SCE) "자기주식의 취득(처분)" 2020년 행에서 자기주식 열이 DB에 양수(+426,664)로 적재, 항등식상 음수(-426,664)여야 함 — 이슈#38과 동일 메커니즘의 두 번째 확인 사례(반복 패턴, 재notify 불필요)

- **회사**: SK텔레콤 (00159023) KOSPI 시총순위 37
- **필링**: 2020FY 사업보고서, r20210317000804
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20210317000804
- **CSV**: `layer2_review/KOSPI/00159023_SK텔레콤/annual/2020/20210317000804_review.csv`

**발견 경로**: 8-scope 전체 원문 대조 중 [별도] 자본변동표(SCE, headerRows=3,
dataWidth=10) 실제 헤더를 재확인해 열 순서를 검증(자본금 / 주식발행초과금 / 자기주식 /
신종자본증권 / 주식선택권 / 기타 / 기타불입자본합계 / 이익잉여금 / 기타자본구성요소 /
자본합계)한 뒤, 모든 데이터 행에 대해 두 항등식(①기타불입자본합계=주식발행초과금+
자기주식+신종자본증권+주식선택권+기타, ②자본합계=자본금+기타불입자본합계+이익잉여금+
기타자본구성요소)을 자동 검산했다. "자기주식의 취득(처분)" 2020년 행에서 위반 발견:
```
["","","426,664","","","","(426,664)","","","(426,664)"]
```
(빈 셀은 원문에서 실제로 빈칸 — 자기주식/기타불입자본합계/자본합계 3열만 값 존재.)

**항등식으로 참값 확인**: 자기주식(426,664, 원문에 괄호 없이 양수로 표기) +
0(나머지 4열 전부 공란) = 기타불입자본합계여야 하는데, 기타불입자본합계는
"(426,664)"로 음수 표기됨. 자기주식을 음수로 바꿔 대입하면 -426,664 = -426,664로
정확히 일치. 또한 [별도] 재무상태표 자본변동표 자기주식 잔액의 연도별 롤포워드로도
교차검증: 2019.12.31 기말 자기주식 잔액 -1,696,997 → 2020.12.31 기말 잔액
-2,123,661, 그 차이가 정확히 -426,664 (당기 중 자기주식 순취득에 따른 감소) —
음수가 맞다는 것을 독립적으로 재확인.

**DART 원문 자체의 표기**: 이 행은 자기주식/기타불입자본합계/자본합계 3열에만 값이
있고 나머지는 원문에서부터 빈칸이며, 값이 있는 3열 중 기타불입자본합계·자본합계는
괄호로 음수 표기되어 있는데 자기주식 열만 괄호 없이 양수로 표기됨 — 이슈#38과
정확히 같은 메커니즘("같은 행 안에서 인접·연관 셀 일부만 부호 복원되고 특정 셀만
원문 그대로 양수 적재")의 두 번째 확인 사례. 필링 종류(분기 vs 사업보고서)와 시점
(2021Q1 vs 2020FY)이 달라 우연이 아니라 재현 가능한 패턴으로 보인다.

**참고(별건, 결함 아님)**: 같은 SCE 표의 2019년 "자기주식의 취득(처분)" 행은
`["0","0","(282,478)","0","0","17,522","300,000","0","0","300,000"]`으로,
자기주식이 원문에 명시적 괄호로 음수(-282,478) 표기되어 있음에도 같은 행의
기타불입자본합계(300,000)와 항등식이 안 맞는다(282,478을 양수로 바꿔야
-282,478+17,522=... 아니라 +282,478+17,522=300,000으로 맞음). 이 셀은 원문에
명시적 괄호가 있어 파싱은 원문 그대로 정확히 이뤄졌고, 불일치는 DART 원문 자체의
저자측 계산 오류로 보인다(우리 파서/DB의 부호복원 실패가 아님) — 결함 판정 대상이
아니므로 fail 사유에서 제외.

---

## 40. SK텔레콤 2018FY 사업보고서 (r20190401004425) — [연결] 자본변동표(SCE, con-sce
7열) "배당금지급" 행에서 이익잉여금 열만 음수로 복원되고 같은 행의 지배기업소유주지분
합계·자본합계 열은 원문 그대로 양수 적재되어 행 내부 항등식 붕괴 — ★#38/#39와 같은
근본원인(부호복원 로직의 열별 적용 누락)의 세 번째 확인 사례이나, con-scope·구독
(subtotal)열 미적용이라는 새로운 하위패턴이라 정보공유 목적으로 기록(반복 패턴,
재notify 불필요, 대기 없이 진행)

- **회사**: SK텔레콤 (00159023) KOSPI 시총순위 37
- **필링**: 2018FY 사업보고서, r20190401004425
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20190401004425
- **CSV**: `layer2_review/KOSPI/00159023_SK텔레콤/annual/2018/20190401004425_review.csv`

**발견 경로**: con-sce(연결자본변동표, headerRows=3, dataWidth=7, 열순서:
자본금/기타불입자본/이익잉여금/기타자본구성요소/지배기업소유주지분합계/비지배지분/
자본합계) 전체 33개 데이터 행에 대해 두 항등식(①자본금+기타불입자본+이익잉여금+
기타자본구성요소=지배기업소유주지분합계, ②지배기업소유주지분합계+비지배지분=
자본합계)을 DART 원문 그대로(괄호=음수) 대입해 자동 검산. "배당금지급" 행(2016·
2017·2018 세 해 모두 동일 패턴)에서 원문 자체가 **전 셀 괄호 없이 완전히 양수**로
표기됨:
```
["0","0","706,091","0","706,091","300","706,391"]   (2016)
["0","0","706,091","0","706,091","281","706,372"]   (2017)
["0","0","706,091","0","706,091","0","706,091"]      (2018)
```
그런데 CSV(DB 적재값)를 확인하면:
```
[별도 아님, 연결 3개 행 모두] 이익잉여금열만 -706,091(음수)로 적재, 나머지
(지배기업소유주지분합계·자본합계)는 원문 그대로 +706,091(양수) 유지.
2016행 비지배지분열도 -300(음수)로 적재됨(2017행은 -281로 동일 패턴).
```

**항등식으로 참값 확인**: DB 적재값 그대로 대입하면 ①식이 `0+0+(-706,091)+0=
-706,091 ≠ 706,091(지배기업소유주지분합계)`로 즉시 깨짐 — 부호를 어느 쪽으로
통일하지 않는 이상 이 행 자체가 산술적으로 성립하지 않는다(양쪽 다 음수든 양쪽 다
양수든 통일하면 성립하지만, 현재 DB는 "이익잉여금·비지배지분만 음수, 합계열은
양수"인 혼합 상태로 저장되어 있어 항등식이 무조건 깨진다).

**교차검증(sep-scope 대조)**: 같은 필링의 [별도] 자본변동표(sep-sce, 10열)의
동일 개념 "배당금지급" 행은 원문에서 이익잉여금(706,091)·자본합계(706,091) 두
열만 값이 있고 나머지는 공란인데, CSV/DB는 **두 열 모두** -706,091(음수)로
일관되게 적재되어 있다(`layer2_review 결과: rownum 48/50, 158/160,
268/270 전부 -706,091`). 즉 배당금지급이 자본을 감소시킨다는 부호 추론 자체는
파서가 정확히 수행하고 있으며, sep-scope에서는 "합계" 역할 열(자본합계)까지
빠짐없이 음수로 전파된다. con-scope에서만 유독 지배기업소유주지분합계·자본합계
(모두 con-sce 특유의 상위 합계/구독 열)에는 그 전파가 누락된다 — con-sce 7열
레이아웃에서 구독(subtotal) 역할 열만 부호복원 로직 적용 대상에서 빠졌을
가능성을 시사.

**확인 범위**: 이 필링 con-scope 4종(con-bs 66/con-is 38/con-cf 62/con-sce
230, 총 396행)과 sep-scope 4종(sep-bs 59/sep-is 32/sep-cf 45/sep-sce 330,
총 466행)을 원문 tr 순회 + 파이썬 element-wise 대조로 전부 확인 — con-sce의
"배당금지급" 3개 행(2016/2017/2018) 지배기업소유주지분합계·자본합계 열(총 6셀)을
제외한 나머지는 전부 원문과 정확히 일치. (con-sce "신종자본증권 이자" 2018행의
자본합계 열이 원문 자체에서 괄호 없이 표기된 건은 DART 원문 자체의 표기 불일치로
파서가 원문 그대로 정확히 전사했으므로 별건 — 결함 아님, #39 2019행 사례와 동일
유형.)

### 처리
- `fail --rcept 20190401004425` — 이 필링은 결함 셀 존재로 pass 불가.
- 이슈#38/#39와 근본원인 가설(부호복원 로직의 행 내 열별 적용 누락)이 동일하므로
  캠페인 규약(반복 패턴은 재notify 불필요)에 따라 **재notify 없이 로그만 기록하고
  큐 진행**. 다만 con-scope·구독열 미적용이라는 세부 범위 확장은 peer의 R162-d
  전체 백필 설계에 유용한 정보이므로 비차단(informational) 메시지로 전달함.

**확인 범위**: 이 필링의 8-scope 전체(con-bs 67/con-is 38/con-cf 66/con-sce 272/
sep-bs 60/sep-is 31/sep-cf 47/sep-sce 269, 총 810행)를 원문 tr 순회 + 파이썬
element-wise 대조로 전부 확인 — 이 자기주식 셀 1건(sep-sce, 2020년 자기주식의
취득행)을 제외한 나머지 전부 원문과 정확히 일치. R160 마침표셀 스캔 전체 anchor
0건.

### 처리
- 이슈#38과 동일 근본원인(R162/163류 SCE 부호복원 로직의 셀별 적용 누락)의 반복
  패턴이므로 재notify는 불필요([[feedback-layer2-campaign-new-issue-wait-for-peer-instruction]]
  참고) — 문서에만 기록하고 peer에게는 참고용으로만 알림. camp_run 규약상 파서
  수정 없이 `fail --note`로 처리하고 큐를 계속 진행한다.

---

## 41. 미래에셋증권 2016Q1 분기보고서 (r20160516002286) — [연결] 재무상태표
"5. 이익잉여금" 행이 통째로 결측 — ★docs/PARSING_RULES.md R152(금융업 보충표기
압축칸)와 정확히 동일한 패턴의 재발, 반복 패턴이므로 재notify 불필요

- **회사**: 미래에셋증권 (00111722) KOSPI 시총순위 38
- **필링**: 2016Q1 분기보고서, r20160516002286
- **DART 원문**: https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20160516002286
- **CSV**: `layer2_review/KOSPI/00111722_미래에셋증권/quarter/2016/20160516002286_review.csv`

**발견 경로**: con-bs(연결재무상태표) 전체 tr 순회 추출 결과가 36개 유효행인데
CSV/DB 적재값은 35개뿐 — 순서 대조로 어느 라벨이 빠졌는지 특정.

**증상**: "I. 지배기업소유주지분"(4,338,599) 산하 자본금(1,703,883)→자본잉여금
(660,085)→자본조정(-113,962)→기타포괄손익누계액(130,233)까지는 정상 적재됐는데
바로 다음 "5. 이익잉여금"(1,958,360) 행이 통째로 사라지고 "II. 비지배지분"(679)
으로 바로 건너뜀. 항등식으로 확인: 1,703,883+660,085-113,962+130,233+
**1,958,360(결측행)** = 4,338,599 = 지배기업소유주지분 총계 — 차이 0, 결측행의
참값이 이 금액임을 독립 검증.

**원인(R152와 동일)**: 이 회사는 금융업(증권사)이라 이익잉여금 밑에 감독규정상
보충 표기(대손준비금 적립액/환입(적립)예정액)를 같은 물리 `<TR>`에 병합해 인쇄한다.
원문 라벨 셀:
```
5. 이익잉여금
   (대손준비금 적립액)
   (대손준비금 환입(적립)예정액)
```
금액 셀(당기열): `1,958,360 / (19,556) / 3,315` — 3개 값이 한 칸에 붙어 있고, 주석
칸은 `26,27`(주석 26·27번, R152가 경고한 바로 그 "날조 위험" 패턴 — 첫 토큰을
그냥 취하면 26을 이익잉여금 값으로 오인할 뻔한 구조). PARSING_RULES.md R152 절이
"미래에셋증권 20150515001242"를 확인 사례 3사 중 하나로 이미 명시하고 있고, 이번
건은 **같은 회사의 다른 rcept_no**(2016Q1)에서 같은 서식이 재현된 것 — R152는
공용 함수(`extract_rows`) 수정으로 "캠페인 재적재 시 자동 해소"라 기재돼 있었으나
이 특정 rcept_no는 아직 그 재적재/백필 범위에 들어가지 않은 것으로 보임.

**확인 범위**: 이 필링 8-scope 전체(con-bs 36/con-is 38/con-cf 68/con-sce 85,
sep-bs 32/sep-is 32/sep-cf 67/sep-sce 54, DART 원문 기준)를 원문 tr 순회 + JS
element-wise 대조로 전부 확인 — con-bs의 "5. 이익잉여금" 결측 1건을 제외한 나머지
(con-is/con-cf/con-sce 전량, sep-bs/is/cf/sce 전량)는 CSV와 정확히 일치(diffCount
0). sep-BS에는 동일 보충표기 압축 패턴 자체가 없어(별도 재무제표는 대손준비금
보충표기 요건이 다름) sep는 애초에 결측 위험이 없었음.

### 처리
- R152(docs/PARSING_RULES.md, 2026-09-20)와 근본원인이 완전히 동일한 반복 패턴이므로
  [[feedback-layer2-campaign-new-issue-wait-for-peer-instruction]] 규약에 따라
  **재notify 없이 로그만 기록하고 큐 진행**. R152가 "이미 알려진 회사(미래에셋증권)"의
  "아직 재적재 안 된 필링"에서 재현된 것이므로, R152 백필/재적재 스코프에 이 rcept_no
  (그리고 같은 회사의 인접 연도 필링 전반)가 실제로 포함됐는지 peer의 R152 백필
  기록 확인이 필요하다는 점만 비차단 정보로 남긴다.
- `fail --note`로 처리(camp_run 규약상 파서 수정 없음), 큐는 계속 진행한다.
