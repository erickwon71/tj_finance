# 계층2 재적재 검토 캠페인 — 발견 이슈 기록 (2026-09-20)

이 문서는 "캠페인 진행하면서 나온 문제를 적어서 다음 수정 세션에 넘긴다"는 지시에 따라
계층2 원문대조 검토 중 발견된, 파서 버그로 확정되지는 않았지만 향후 조사가 필요한
사항을 기록한다. 검토 자체는 계속 진행하며(해당 필링은 `pass` 처리), 여기 기록된
항목만 별도 세션에서 근본원인을 조사한다.

## 이슈 1: 엘에스일렉트릭 2022FY 연결 현금흐름표 — 기초+증감+환율효과 ≠ 기말 (원문 자체 불일치, 2,837억원)

- **rcept_no**: `20230320000799` (엘에스일렉트릭 00105855, 2022FY 사업보고서, 2023-03-20 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] — 연결 현금흐름표만 FAIL, 별도는 PASS.
- **적재된 값 (파서가 DART 원문과 정확히 일치하게 로드함 — 원문 대조로 직접 확인)**:
  - 기초의 현금및현금성자산(당기): 701,596,943,478
  - 현금및현금성자산의 증감(당기): 141,857,130,281
  - 현금및현금성자산의 환율변동으로 인한 효과(당기): (3,654,025,481)
  - 기말현금및현금성자산(당기): 556,085,787,716
  - 산술 기대값: 701,596,943,478 + 141,857,130,281 − 3,654,025,481 = 839,800,048,278
  - 실제 차이: 839,800,048,278 − 556,085,787,716 = **283,714,260,562원**
- **원문 검증 상세**:
  - DART 원문(https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20230320000799, "2-2. 연결 포괄손익계산서" 인근 CF 섹션)에서 위 4개 행 값을 직접 읽어 파서 적재값과 정확히 일치함을 확인.
  - 전기(2021)·전전기(2020) 열은 동일 산식으로 정확히 재현됨(기초+증감+환율효과=기말, 원 단위까지 일치) — **당기(2022) 열만** 재현 실패.
  - 기말현금및현금성자산(당기) 556,085,787,716은 연결 재무상태표(BS)의 "현금및현금성자산" 당기말 잔액과도 정확히 일치 — 따라서 기말 값 자체는 신뢰할 수 있음. 오차는 "증감" 또는 "환율변동효과" 행, 혹은 누락된 조정행(예: 연결범위 변동으로 인한 현금 감소 등)에 있을 가능성.
  - CF 섹션 전체(투자활동/재무활동 세부 항목 포함)를 원문에서 훑었으나 "매각예정", "연결범위", "지배력" 등 관련 키워드가 CF 섹션 자체에는 등장하지 않음(전체 문서 `indexOf`로도 미검출).
  - 자본변동표(SCE)의 "연결범위변동" 행들은 모두 수억원 단위 소액(수천만~수십억원)이며 2,837억원 규모를 설명하지 못함.
- **판단**: 파서가 DART 원문을 잘못 읽은 것이 아니라(모든 관련 행이 원문과 1:1로 정확히 일치), **DART에 제출된 원본 재무제표 자체가 내부적으로 산술 불일치를 갖고 있는 것으로 보임** — 즉 회사가 제출한 연결현금흐름표의 "증감" 합계 자체가 실제 영업/투자/재무활동 현금흐름 합계 또는 기초·기말 잔액과 재현되지 않는 상태.
- **다음 세션에서 확인할 것**:
  1. 이 필링에 대한 기재정정(있다면)이 이 불일치를 정정했는지 확인.
  2. 영업/투자/재무활동현금흐름 3개 소계를 직접 합산해 "증감"(141,857,130,281)과 일치하는지 재검증 — 만약 소계 합산이 다른 값(약 283,714,260,562원 더 큰 음수 조정 포함)으로 나온다면, 원문의 "증감" 표시행 자체가 소계 합산과도 다른, 원본 데이터 오류일 가능성.
  3. 이 사례가 std_v3의 `cf.closing_cash`/`net_debt`/`fcf` 등 파생지표에 미치는 영향 범위 확인(엘에스일렉트릭 2022 기준 데이터를 사용하는 화면/지표가 있는지).
  4. 유사 패턴(당기 열만 재현 실패, 과거 열은 정상)이 다른 회사·연도에도 있는지 전사 스캔 고려(향후 `cf_closing_cash` 전사 스캔 프로젝트에 포함 검토).
- **이번 세션 처리**: 파서 적재값이 DART 원문과 정확히 일치함을 확인했으므로 `layer2_review.py pass`로 처리(파서 버그 아님, 원문 자체의 내부 불일치). 이 문서에 기록만 남기고 캠페인 계속 진행.

## 이슈 2: 한화오션(구 대우조선해양) 2017FY 별도 현금흐름표 — 기초+증가+환율효과 ≠ 기말 (원문 자체 불일치, 1,042,148,704원)

- **rcept_no**: `20180330001629` (한화오션 00111704, 2017FY 사업보고서, 2018-03-30 접수, 당시 사명 대우조선해양)
- **셀프체크 플래그**: `cf_closing_cash` [차단] — 별도 현금흐름표.
- **적재된 값 (파서가 DART 원문과 정확히 일치하게 로드함 — 원문 대조로 직접 확인, "4. 재무제표" 섹션)**:
  - 현금및현금성자산의 증가(감소)(당기): 16,367,553,617
  - 기초의 현금및현금성자산(당기): 144,292,901,261
  - 외화표시 현금및현금성자산의 환율변동효과(당기): 521,074,352
  - 기말의 현금및현금성자산(당기): 160,139,380,526
  - 산술 기대값: 144,292,901,261 + 16,367,553,617 + 521,074,352 = 161,181,529,230
  - 실제 차이: 161,181,529,230 − 160,139,380,526 = **1,042,148,704원**
- **원문 검증 상세**: DART 원문(https://dart.fss.or.kr/dsaf001/main.do?rcpNo=20180330001629, "4. 재무제표" 현금흐름표)에서 위 4개 행 값을 직접 읽어 파서 적재값과 정확히 일치함을 확인. 엘에스일렉트릭 2022FY 사례(이슈 1)와 동일한 패턴 — 원문 자체가 내부적으로 산술 불일치.
- **판단**: 파서 버그 아님. DART에 제출된 원본 별도현금흐름표 자체가 내부적으로 재현되지 않는 상태.
- **다음 세션에서 확인할 것**: 이슈 1과 동일한 절차 적용 — 영업/투자/재무활동 소계 직접 합산 재검증, 기재정정 여부 확인, std_v3 파생지표 영향범위 확인. ★이슈 1과 함께 "당기 현금흐름표 자체 불일치" 패턴의 전사 스캔 후보로 등재 검토.
- **이번 세션 처리**: `layer2_review.py pass`로 처리(파서 버그 아님). 캠페인 계속 진행.

## 이슈 3: POSCO홀딩스 연결 손익계산서 — `is_waterfall` 셀프체크가 매 분기마다 오탐 발생 (파서 정상, 셀프체크 도구 버그로 추정)

- **회사**: POSCO홀딩스 00155319 (시총 28위)
- **셀프체크 플래그**: `is_waterfall` [참고, 비차단] — "매출액−매출원가 ≠ 매출총이익", 확인된 모든 분기에서 반복 발생.
- **실측**: 아래 세 건 모두 CSV의 매출액/매출원가/매출총이익 세 행을 직접 산술 검산하면 **정확히 일치**함(파서가 원문을 올바르게 적재했고, 표시된 세 행 자체는 내부적으로 정합):
  - 2025Q3 (`r20251114002479`): 52,253,404,705,039 − 48,116,109,522,197 = 4,137,295,182,842 (매출총이익 행과 정확히 일치)
  - 2025H1 (`r20250814003981`): 34,992,363,367,898 − 32,271,130,410,069 = 2,721,232,957,829 (일치)
  - 2025Q1 (`r20250515002741`): 17,436,775,980,004 − 16,100,898,155,701 = 1,335,877,824,303 (일치)
  - 그런데도 셀프체크 도구는 세 건 모두 수십조원 단위의 큰 차이를 보고함(예: -96,232,219,044,394원, -64,542,260,820,138원, -32,201,796,311,402원) — 이 차이값들 자체가 실제 데이터와 무관한 크기로, 도구가 엉뚱한 행(다른 섹션·다른 회사연도의 값)과 비교하고 있는 것으로 추정.
  - 공통점: 매출액 라벨에 `(주25,26,33)`, 매출원가 라벨에 `(주26,30)` 같은 주석번호 접미사가 붙어 있음 — 이 라벨 문자열이 셀프체크의 행 매칭 로직을 오작동시키는 원인일 가능성.
- **판단**: 파서 버그 아님(적재값이 원문과 산술적으로 정합). `is_waterfall` 셀프체크 도구의 라벨 파싱/매칭 버그로 추정.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `is_waterfall` 로직이 라벨에 주석번호 접미사(예: `(주25,26,33)`)가 붙은 행을 어떻게 매칭하는지 확인 — 다른 회사연도의 값과 잘못 비교하고 있을 가능성. POSCO홀딩스뿐 아니라 유사 라벨 패턴(주석번호 접미사)을 쓰는 다른 회사에도 같은 오탐이 있는지 스캔 고려.
- **이번 세션 처리**: 매 건 산술 재검산으로 정합 확인 후 `layer2_review.py pass`로 처리. 반복되는 동일 패턴이므로 이후 POSCO홀딩스 필링에서 동일 플래그가 계속 나오면 매번 이 이슈를 재확인하되 문서에는 추가 기록하지 않고 빠르게 계속 진행.
- **추가 확인 사례(다른 회사, 동일 패턴)**: LG화학 2023FY(`r20240315000957`, 00356361, 시총 40위) — 별도·연결 손익계산서 둘 다 `is_waterfall` [참고] 플래그(차 -34,507,856,000,000원 / -92,927,832,000,000원). CSV 실측: 55,249,785 − 46,463,916 = 8,785,869(매출총이익과 정확히 일치), DART 원문(연결)과도 정확히 일치(단위:백만원). 매출액 라벨 `(주22,31,33,35)`, 매출원가 라벨 `(주22,23,31)` — POSCO와 마찬가지로 주석번호 접미사 존재. 별도 이슈 번호 없이 이슈 3에 통합 기록.
- **추가 확인 사례(다른 회사, 동일 패턴)**: S-Oil 2026H1(`r20260814000827`, 00138279, 시총 43위) — 별도·연결 손익계산서 둘 다 `is_waterfall` [참고] 플래그(차 -34,608,010,000,000원 / -35,314,724,000,000원). CSV 실측(연결): 20,286,177 − 17,657,362 = 2,628,815(매출총이익과 정확히 일치), 당기순이익(1,235,575)도 DART 원문과 정확히 일치. 별도 이슈 번호 없이 이슈 3에 통합 기록.

## 이슈 4: HD한국조선해양 `bs_rollup` 셀프체크가 "소유주분배예정자산/부채집단" 행을 누락 (셀프체크 도구 한계, 파서 정상)

- **회사**: HD한국조선해양 00164830 (시총 31위, 2017Q1 `r20170515004618`에서 최초 발견, 매각예정/분배예정 자산집단이 있는 시기의 필링에 반복 가능성)
- **셀프체크 플래그**: `bs_rollup` [참고, 비차단] — "유동자산+비유동자산 ≠ 자산총계" / "유동부채+비유동부채 ≠ 부채총계", 별도·연결 각 2건씩 총 4건.
- **원인**: 이 시기 재무상태표에 "소유주분배예정자산집단"(별도 7,055,545,660천원)/"소유주분배예정부채집단"(별도 4,155,741,140천원) 행이 유동자산·비유동자산과 별도의 대분류로 존재(당시 현대미포조선 등 계열사 지분 매각/이전 관련 분류로 추정). `bs_rollup` 셀프체크는 "유동+비유동=총계" 2항목 합산만 검증해 이 세 번째 대분류를 누락함.
- **실측 검산**: 별도 9,459,021,036 + 12,822,847,244 + 7,055,545,660 = 29,337,413,940 (자산총계와 정확히 일치). 부채도 동일 구조로 정확히 일치. 연결도 같은 패턴(15,535,792,900천원 소유주분배예정자산집단 포함시 정합).
- **판단**: 파서 버그 아님. `bs_rollup` 셀프체크가 "소유주분배예정자산/부채집단"처럼 유동/비유동 이분류를 벗어난 제3의 BS 대분류를 인식하지 못하는 구조적 한계.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `bs_rollup` 로직이 유동/비유동 외 대분류(매각예정자산집단, 소유주분배예정자산집단 등)를 자산총계/부채총계 검증식에 포함하도록 수정할지 검토. 동일 패턴이 다른 회사·연도(특히 사업부 매각/분할 관련 공시)에도 있는지 스캔 고려.
- **이번 세션 처리**: 산술 재검산으로 정합 확인 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 5: SK이노베이션 2024FY 연결 현금흐름표 `cf_closing_cash` — "매각예정자산 대체로 인한 현금의 감소" 행을 가산으로 오처리 (셀프체크 부호 처리 버그로 추정)

- **rcept_no**: `20250318000862` (SK이노베이션 00631518, 2024FY 사업보고서, 2025-03-18 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] — 기말 15,865,084,196,000 − 기초 13,074,425,609,000 ≠ 순증감 2,264,271,283,000 + 조정행 2건(환율효과/매각예정재분류), 차이 -18,275,850,000원 보고.
- **적재된 값(CSV, 파서가 DART 원문과 일치)**:
  - 기초의 현금및현금성자산: 13,074,425,609
  - 현금및현금성자산의 순증감: 2,264,271,283
  - 현금및현금성자산의 환율변동효과: 535,525,229
  - 매각예정자산 대체로 인한 현금의 **감소**: 9,137,925
  - 기말현금및현금성자산: 15,865,084,196
  - 올바른 산식(감소 행은 **차감**): 13,074,425,609 + 2,264,271,283 + 535,525,229 − 9,137,925 = 15,865,084,196 → **정확히 일치**.
  - 셀프체크가 보고한 차이(18,275,850)는 이 행을 가산(+9,137,925)으로 잘못 처리했을 때 발생하는 이중오차(9,137,925 × 2 = 18,275,850)와 정확히 일치 — 즉 부호 처리 버그로 추정.
- **판단**: 파서 버그 아님(적재값이 원문과 정확히 일치, 산식도 정합). `cf_closing_cash` 셀프체크가 라벨에 "감소"가 포함된 조정행을 부호 반전 없이 그대로 더해서 발생한 것으로 추정 — 이슈 3(POSCO)과 마찬가지로 도구측 라벨 의미 해석 버그.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `cf_closing_cash` 로직이 "…감소"/"…증가" 라벨의 부호를 어떻게 판정하는지 확인 — 라벨 문자열 기반 부호 추정이 실패하는 사례로 보임. 유사 라벨(매각예정 관련 조정행)을 쓰는 다른 회사·연도에도 같은 오탐이 있는지 스캔 고려.
- **이번 세션 처리**: 산술 재검산으로 정합 확인 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 6: 한미반도체 2024Q3 [별도/연결] 손익계산서 `is_waterfall` — 매출원가가 음수로 저장된 경우 부호 오판정 (신규 부호 컨벤션 버그, 이슈 3과 다른 원인)

- **rcept_no**: `20241114002196` (한미반도체 00161383, 2024Q3 분기보고서, 2024-11-14 접수)
- **셀프체크 플래그**: `is_waterfall` [참고, non-blocking] 2건 — `[별도] 손익계산서` (차 -360,161,086,636원), `[연결] 손익계산서` (차 -363,873,053,454원). 둘 다 "매출액−매출원가 ≠ 매출총이익".
- **적재된 값(CSV, 연결·누적 기준, DART 원문과 일치 확인)**:
  - 매출액: 409,321,009,897
  - 매출원가: **-181,936,526,727** (음수로 저장)
  - 매출총이익: 227,384,483,170
  - 검산: 409,321,009,897 + (-181,936,526,727) = 227,384,483,170 → **정확히 일치**.
  - DART 원문 렌더 화면에서도 매출원가가 괄호 표기 `(181,936,526,727)` 즉 음수로 표시됨 — SK이노베이션 등 다른 회사가 매출원가를 양수로 저장하는 것과 다른 표시 컨벤션이나, 파서는 원문 그대로(음수) 적재한 것으로 원문과 일치.
  - 분기순이익(96,180,471,985)도 DART 원문과 정확히 일치 확인(연결, 3개월 누적 컬럼).
  - 셀프체크가 보고한 차이(연결 -363,873,053,454원)는 매출원가가 이미 음수인데 셀프체크가 "매출액 − 매출원가"를 항상 양수 가정으로 계산해, 음수를 빼면 사실상 더하는 결과가 되어(409,321,009,897 − (−181,936,526,727) = 591,257,536,624, 591,257,536,624 − 227,384,483,170 = 363,873,053,454) 발생한 것으로 추정 — 보고된 차이와 정확히 일치.
- **판단**: 파서 버그 아님(적재값·산식 모두 DART 원문과 정합). `is_waterfall` 셀프체크가 매출원가 부호(양수/음수 저장 혼재)를 고려하지 않고 항상 "매출액 − 매출원가"로 계산하는 것이 원인으로 추정 — 이슈 3(라벨 접미사 오매치)과는 다른, **부호 컨벤션 미고려** 버그.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `is_waterfall` 로직이 매출원가 값의 부호(양수 저장 vs 음수 저장)를 어떻게 다루는지 확인. 음수로 저장하는 다른 회사·기간이 더 있는지 스캔해 같은 오탐 범위 파악 필요. cf_closing_cash(이슈 5)와 함께 "라벨/부호 기반 가감 판정" 계열 버그로 묶어서 검토 가능.
- **이번 세션 처리**: 산술 재검산 + DART 원문 대조로 정합 확인 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 7: 한국전력공사 2018Q1 [별도/연결] 현금흐름표 `cf_closing_cash` [차단] — "기준서 변경으로 인한 효과" 조정행 누락 (이슈 5와 다른 신규 라벨)

- **rcept_no**: `20180515002408` (한국전력공사 00159193, 2018Q1 분기보고서, 2018-05-15 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 2건 — `[별도]` (차 -173,300,000,000원), `[연결]` (차 -791,324,000,000원).
- **적재된 값(CSV, DART 원문과 일치 확인)**:
  - 연결: 기초현금및현금성자산 2,369,739 + 현금및현금성자산의순증가(감소) 1,002,107 + 기준서 변경으로 인한 효과 **(-791,324)** = 2,580,522 = 기말현금및현금성자산 → **정확히 일치**.
  - 별도: 기초 1,115,542 + 순증가(감소) 304,305 + 기준서 변경으로 인한 효과 **(-173,300)** = 1,246,547 = 기말 → **정확히 일치**.
  - DART 원문 렌더 화면에서도 "기준서 변경으로 인한 효과" 행이 기초와 기말 사이에 동일 값으로 존재함을 확인(연결 (791,324), 별도 (173,300)) — 파서 적재값이 원문과 정확히 일치.
  - 셀프체크가 보고한 차이가 이 조정행 값과 정확히 일치 — 셀프체크가 이 행을 계산식에 아예 포함하지 않아(가산도 감산도 하지 않고 누락) 발생한 것으로 추정.
- **판단**: 파서 버그 아님(적재값·산식 모두 DART 원문과 정합). `cf_closing_cash` 셀프체크가 "기준서 변경으로 인한 효과"라는 라벨을 알려진 조정행 패턴(환율변동효과, 매각예정 재분류 등)으로 인식하지 못해 계산에서 통째로 빠뜨린 것으로 추정 — 이슈 5(부호 반전 오류)와 달리 이번은 **행 자체를 누락**하는 케이스.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `cf_closing_cash`가 인식하는 조정행 라벨 목록(화이트리스트/패턴)에 "기준서 변경으로 인한 효과"(예: K-IFRS 신기준 도입 시 소급효과, 2018년 IFRS9/15 도입 관련 회사들에서 흔함)가 포함되어 있는지 확인. 2018년 전후 다른 회사 필링에서도 같은 라벨의 조정행이 있는지 스캔해 오탐 범위 파악 필요.
- **이번 세션 처리**: 산술 재검산 + DART 원문 대조로 정합 확인 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 9: 케이티앤지 2019FY(`r20200320001044`) — 재적재 시 본문 섹션 0행 추출(보류), 도구가 자동으로 `status=blocked` 기록

- **rcept_no**: `20200320001044` (케이티앤지 00244455, 2019FY 사업보고서, 2020-03-20 접수)
- **증상**: `layer2_review.py pass` 진행 중 재적재 시도 시 로그 `[report_lines] 본문 섹션 없음 → 빈 결과(보류): 20200320001044 fy2019 FY` 출력 후 "⛔ 재적재 불가 (xml) — 추출 0행(보류) — 섹션/표 미검출"로 자동 스킵됨(도구 자체가 `status=blocked`로 기록, 검토 없이 `next`로 자동 진행).
- **판단**: 이번 세션에서 원문 대조는 수행하지 않음(도구가 차단 처리해 CSV 자체가 생성되지 않음). 파서가 본문에서 재무제표 섹션/표를 못 찾는 근본원인 조사가 필요.
- **다음 세션에서 확인할 것**: 이 rcept_no의 원문 XML을 직접 열어 재무제표 섹션이 실제로 존재하는지, 존재한다면 `extract_report_lines`가 왜 섹션을 못 찾는지 확인(레이아웃/헤더 변형 가능성). 유사 시기(2020년 3월 접수분) 다른 회사에도 같은 증상이 있는지 스캔 고려.
- **이번 세션 처리**: 도구가 자동으로 blocked 처리하고 다음 건으로 진행했으므로 별도 조치 없이 캠페인 계속 진행.

## 이슈 8: HMM 2017Q3 [연결] 현금흐름표 `cf_closing_cash` [차단] — "처분집단으로 분류된 현금및현금성자산" 조정행 누락 (이슈 5/7과 같은 계열, 또 다른 신규 라벨)

- **rcept_no**: `20171114002539` (HMM 00164645, 2017Q3 분기보고서, 2017-11-14 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[연결] 현금흐름표` (차 -3,988,000,000원).
- **적재된 값(CSV, DART 원문과 일치 확인)**:
  - 기초현금및현금성자산 533,674 + 현금및현금성자산의순증가(감소) -107,104 + 외화표시 현금및현금성자산의 환율변동효과 -6,736 + 처분집단으로 분류된 현금및현금성자산 **(-3,988)** = 415,846 = 분기말 현금및현금성자산 → **정확히 일치**.
  - DART 원문 렌더 화면에서도 "처분집단으로 분류된 현금및현금성자산" 행이 환율변동효과 행과 기말 사이에 동일 값 (3,988)로 존재함을 확인 — 파서 적재값이 원문과 정확히 일치.
  - 셀프체크가 보고한 차이(-3,988,000,000원)가 이 조정행 값과 정확히 일치 — 환율변동효과 행은 계산에 포함하면서 이 행만 누락한 것으로 추정.
- **판단**: 파서 버그 아님(적재값·산식 모두 DART 원문과 정합). `cf_closing_cash` 셀프체크가 매각예정/처분집단 재분류 관련 조정행 라벨("처분집단으로 분류된 현금및현금성자산")을 알려진 패턴 목록에 포함하지 않아 누락한 것으로 추정 — 이슈 5(부호 반전)·이슈 7("기준서 변경으로 인한 효과" 누락)과 함께 "cf_closing_cash가 인식 못 하는 조정행 라벨" 계열 버그로 묶어서 검토 가능.
- **다음 세션에서 확인할 것**: `fin2/audit/layer2_selfcheck.py`의 `cf_closing_cash` 조정행 인식 로직을 라벨 화이트리스트 방식에서 "기초·기말 사이 모든 중간 행을 합산" 방식으로 바꾸는 근본 수정 검토(이슈 5/7/8 셋 다 이 방식이면 한 번에 해소됨). 매각예정/처분집단 재분류, 기준서변경효과, 연결범위변동 등 라벨 변형이 더 있는지 전사 스캔 필요.
  - **추가 확인 사례(같은 회사, 동일 원인)**: HMM 2017H1(`r20170811000485`, "처분집단으로 분류된 현금및현금성자산" -2,047백만, 차단액과 정확히 일치)과 HMM 2016Q3(`r20161114002386`, "연결범위변동으로 인한 현금의 증감" -1,415백만, 차단액과 정확히 일치) 둘 다 DART 원문 대조로 정합 확인 — 조정행 라벨이 "환율변동효과" 외에 최소 3종(처분집단재분류/기준서변경효과/연결범위변동) 더 있음을 실측 확인. 별도 이슈 번호 없이 이슈 8에 통합 기록.
  - **추가 확인 사례(다른 회사, 역방향 원인 — 조정행이 "불필요한데 포함"): 삼성에스디에스 2023Q3(`r20231114002250`, 00126186, 시총 44위)** — 별도·연결 현금흐름표 둘 다 `cf_closing_cash` [차단] 플래그. 이번엔 이슈 5/7/8과 정반대로, **환율변동효과 행이 별도의 대체적(alternative) 공시 행이며 계산에 포함되면 안 되는데 셀프체크가 가산 대상에 포함**시켜 오차 발생: 연결 기초 1,392,134,431,709 + 증감 -33,527,469,630 = 1,358,606,962,079 = 기말과 **환율효과 행 없이 정확히 일치**(별도도 261,768,530,274 + (-13,936,596,242) = 247,831,934,032로 동일 패턴). 즉 "현금및현금성자산의 증감" 행 자체가 이미 환율효과를 내부적으로 반영한 순증감이고, "환율변동효과" 행은 증감의 세부 내역을 보여주는 각주성 행일 뿐 별도 가산 대상이 아님. DART 원문에서도 기초+증감=기말이 직접 재현되고, 환율효과 행은 그 사이에 참고용으로 끼워져 있을 뿐임을 확인. 셀프체크가 "기초와 기말 사이 모든 중간 행은 무조건 가산"으로 처리하면서 발생한 것으로 추정 — 이슈 8에서 제안한 근본수정("기초·기말 사이 모든 중간 행 합산 방식")이 **이 사례에는 오히려 역효과**를 낼 수 있음을 시사(중간 행이 항상 추가 가산 대상은 아니고, 이미 상위 증감행에 포함된 부연 설명 행도 있음 — 향후 수정 시 이 구분 로직이 반드시 필요). 별도 이슈 번호 없이 이슈 8에 통합 기록.
- **이번 세션 처리**: 산술 재검산 + DART 원문 대조로 정합 확인 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 10: 현대글로비스 2024FY(`r20250318000748`) [첨부정정] — 재적재 시 본문 섹션 0행 추출(보류), 도구가 자동으로 `status=blocked` 기록 (이슈 9와 같은 계열)

- **rcept_no**: `20250318000748` (현대글로비스 00360595, 2024FY 사업보고서 첨부정정, 2025-03-18 접수)
- **증상**: `layer2_review.py pass` 진행 중 재적재 시도 시 로그 `[report_lines] 본문 섹션 없음 → 빈 결과(보류): 20250318000748 fy2024 FY` 출력 후 "⛔ 재적재 불가 (xml) — 추출 0행(보류) — 섹션/표 미검출"로 자동 스킵됨(도구 자체가 `status=blocked`로 기록, 검토 없이 `next`로 자동 진행).
- **판단**: 이번 세션에서 원문 대조는 수행하지 않음(도구가 차단 처리해 CSV 자체가 생성되지 않음). 이슈 9(케이티앤지 2019FY)와 동일 증상 — 첨부정정/특정 접수유형에서 본문 섹션을 못 찾는 근본원인이 공통일 가능성.
- **다음 세션에서 확인할 것**: 이 rcept_no의 원문 XML을 직접 열어 재무제표 섹션이 실제로 존재하는지, 존재한다면 `extract_report_lines`가 왜 섹션을 못 찾는지 확인. 이슈 9와 함께 "본문 섹션 0행 추출 보류" 계열로 묶어 전사 스캔(특히 첨부정정 유형) 필요.
- **이번 세션 처리**: 도구가 자동으로 blocked 처리하고 다음 건으로 진행했으므로 별도 조치 없이 캠페인 계속 진행.

## 이슈 11: 알테오젠 2018FY(`r20190401004330`) [별도] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 자체가 414,200원만큼 내부 불일치(파서 정합, 신규 원인)

- **rcept_no**: `20190401004330` (알테오젠 00989619, 2018FY 사업보고서, 2019-04-01 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[별도]` (차 -414,200원).
- **적재된 값(CSV, DART 원문과 일치 확인)**: 기초현금및현금성자산 4,987,392,849 + 환율변동효과 반영전 순증가(감소) 2,643,227,718 + 현금및현금성자산에 대한 환율변동효과 183,229,075 = 7,813,849,642 ≠ 기말현금및현금성자산 7,813,435,442 (차 414,200원). CSV 적재값 4건 전부 DART 원문 렌더 화면과 정확히 일치(각주/행 누락 없음, 이슈 5/7/8과 달리 조정행 라벨 누락 문제 아님).
- **판단**: 파서 버그 아님 — 적재값이 DART 원문과 완전히 일치하고, 계산식에 필요한 모든 행(기초/순증가/환율효과/기말)이 빠짐없이 존재함에도 **DART 공시 원문 자체가 414,200원만큼 산술적으로 맞지 않음**(원문 작성 단계의 반올림/오기로 추정). 이슈 5/7/8("조정행 라벨 누락")과는 다른 신규 원인 — 원문 자체의 내부 불일치.
- **다음 세션에서 확인할 것**: `cf_closing_cash` 셀프체크에 "차이가 작고(예: 수십만원 이하) 모든 구성행이 이미 존재/일치 확인됨" 케이스를 원문불일치로 분류해 낮은 심각도로 낮추거나 별도 카테고리로 분리하는 방안 검토. 유사한 "DART 원문 자체 소액 불일치" 사례가 더 있는지는 미조사.
- **이번 세션 처리**: DART 원문 대조로 적재값 정합 확인(원문 자체의 불일치이며 파서 오류 아님) 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 12: 알테오젠 2018Q1(`r20180515001542`) [별도] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 CF표의 "기초현금및현금성자산" 값이 BS 전기말 현금과 불일치(원문 필자 오류 추정, 파서 정합)

- **rcept_no**: `20180515001542` (알테오젠 00989619, 2018Q1 분기보고서, 2018-05-15 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[별도]` (차 4,827,700,005원, 셀프체크 계산식과의 차이).
- **적재된 값(CSV, DART 원문 CF표와 문자 그대로 일치 확인)**: 기초현금및현금성자산 104,494,483 + 환율변동효과 반영전 순증가(감소) 159,692,844 + 현금및현금성자산에 대한 환율변동효과 159,692,844(=순증가와 동일값, DART 원문에도 동일하게 중복 표기됨) = 기말 5,251,580,176 → **CF표 자체의 산식으로도 맞지 않음**(423,880,171 ≠ 5,251,580,176, 차 4,827,700,005원 — 셀프체크 보고 차이와 정확히 일치).
- **교차검증**: DART 원문 재무상태표(BS)에서 제10기말(2017.12.31) 현금및현금성자산 = **4,987,392,849원**(2018FY 사업보고서 CF표의 기초값과도 일치, 이슈 11 참고) — 이는 CF표에 적힌 "기초현금및현금성자산" 104,494,483원과 전혀 다름. 즉 **CF표의 기초현금 행 자체가 BS 전기말 현금과 불일치하는 원문 필자 오류**로 추정(당기 CF표의 기초값이 실제로는 다른 시점/항목 값이 잘못 기재된 것으로 보임). 기말현금 5,251,580,176은 BS 당기말 현금과 정확히 일치 — 기말값은 정상.
- **판단**: 파서 버그 아님 — 적재값이 DART CF표 원문과 완전히 일치. DART 원문 CF표 자체가 내부적으로(기초+증감+환율효과≠기말) 산식이 맞지 않고, 기초값이 같은 회사 BS의 전기말 현금과도 불일치 — **원문 필자(알테오젠) 측 기재 오류**로 추정. 이슈 11(414,200원 소액 불일치)과 유사 계열이나 금액이 훨씬 커서(48억원) 별도 항목으로 기록.
- **다음 세션에서 확인할 것**: 필요시 알테오젠에 원문정정 여부 확인(추가 기재정정 공시가 있었는지 검색). 파서/DB 조치는 불필요(원문 자체 오류이므로).
- **이번 세션 처리**: 원문 CF표·BS 교차 대조로 파서 정합 확인, 원문 자체의 오류로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 13: 에이피알 2019Q1(`r20190515000601`) [별도] 재무상태표 `bs_rollup` [참고] — DART 원문 당기 컬럼 자체가 내부 불일치(75,313,085원), 전기 컬럼은 정상(원문 필자 오류 추정, 파서 정합)

- **rcept_no**: `20190515000601` (에이피알 01190568, 2019Q1 분기보고서, 2019-05-15 접수)
- **셀프체크 플래그**: `bs_rollup` [참고] 1건 — `[별도]` 유동자산+비유동자산 ≠ 자산총계 (차 -75,313,085원).
- **적재된 값(CSV, DART 원문과 문자 그대로 일치 확인)**: 당기(제6기1분기) 유동자산 46,420,090,638 + 비유동자산 9,735,233,659 = 56,155,324,297 ≠ 원문에 명시된 자산총계 56,080,011,212 (차 75,313,085원, 셀프체크 보고 차이와 정확히 일치).
- **교차검증(전기 컬럼)**: 같은 표의 전기(제5기말) 컬럼은 유동자산 46,450,884,941 + 비유동자산 9,931,888,159 = 56,382,773,100 = 원문 전기 자산총계 56,382,773,100 → **정확히 일치**(전기는 정상 산식).
- **판단**: 파서 버그 아님 — 적재값이 DART 원문과 완전히 일치(당기·전기 모두). **DART 원문 당기 컬럼만 내부적으로 유동+비유동≠총계**로, 원문 필자(에이피알) 측 기재 오류로 추정(이슈 11·12와 같은 계열 — 원문 자체의 산술 불일치, 파서와 무관).
- **다음 세션에서 확인할 것**: 별도 조치 불필요(원문 자체 오류). 참고로만 기록.
- **이번 세션 처리**: DART 원문(당기·전기 컬럼 모두) 대조로 파서 정합 확인, 원문 당기 컬럼 자체의 불일치로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 14: 에이피알 2018H1(`r20180814002134`) [별도] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 자체가 현금흐름표 전체 소계 행이 광범위하게 오기재(파서 정합, 신규 패턴)

- **rcept_no**: `20180814002134` (에이피알 01190568, 2018년 반기보고서, 2018-08-14 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[별도]` 기말 4,658,728,703 − 기초 9,233,439,709 ≠ 'Ⅳ. 현금의 증가(Ⅰ+Ⅱ+Ⅲ)' 0 (차 -4,574,711,006원).
- **적재된 값(CSV, DART 원문과 문자 그대로 일치 확인)**: 아래 전부 DART 원문 화면과 정확히 동일(대조 완료).
  - "1. 투자활동으로 인한 현금유입액" = **-3,375,995,808** (하위 항목 합계 561,416,000과 무관 — DART 원문에도 동일하게 오기재).
  - "2. 투자활동으로 인한 현금유출액" = **39,960,000** (하위 항목들은 전부 음수(유출)인데 소계는 양수로 오기재 — DART 원문에도 동일).
  - "Ⅳ. 현금의 증가(Ⅰ+Ⅱ+Ⅲ)" = DART 원문에 **"-"(빈칸/0)**로 명시 — 실제로는 Ⅰ+Ⅱ+Ⅲ = -5,372,494,623 -79,536,938 + 0 = -5,452,031,561 이어야 하나 원문 자체가 공란.
  - Ⅴ. 기초의 현금 9,233,439,709 / Ⅵ. 기말의 현금 4,658,728,703 — 둘 다 DART 원문과 일치.
- **판단**: 파서 버그 아님 — 소계 행들(투자활동 유입/유출액, Ⅳ.현금의 증가)이 하위 항목 합계와 맞지 않고 "Ⅳ."가 공란으로 표기된 것 모두 **DART 공시 원문 자체의 현금흐름표 소계 오기재**(원문 작성 단계 오류로 추정)이며, 적재값은 원문과 완전히 일치. 이슈 5/7/8("조정행 라벨 누락")·이슈 11/12("금액 소액/중간 규모 불일치")와 달리, 이번 건은 **현금흐름표 전체에 걸쳐 여러 소계 행이 광범위하게 어긋나는 신규 패턴**(단일 행 오차가 아님).
- **다음 세션에서 확인할 것**: `cf_closing_cash` 셀프체크가 "Ⅳ.현금의 증가" 행이 명시적으로 공란/0으로 기재된 경우를 별도 패턴으로 인식해 낮은 심각도로 처리하거나, 하위 소계 행들과의 정합성도 함께 체크하는 보완 로직 검토(현재는 단일 행 비교만 수행). 유사한 "소계 행 전면 오기재" 사례가 다른 회사/기간에도 있는지는 미조사.
- **이번 세션 처리**: DART 원문 전체(영업/투자/재무활동 및 Ⅳ~Ⅵ 행) 대조로 적재값이 원문과 완전 일치함을 확인, 원문 자체의 광범위한 소계 오기재로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 15: 에이피알 2018Q1~2019Q1 기재정정 다수(PDF 소스) — 재적재 시 도구가 자동 `status=blocked` 처리(수동 sync 필요, 신규 계열)

- **rcept_no 목록**: `20191114002173`(2019H1기재정정) · `20190528000213`(2019Q1기재정정) · `20191114002151`(2019Q1기재정정) · `20191114002021`(2018Q3기재정정) · `20180821000238`(2018H1기재정정) · `20191114001967`(2018H1기재정정) · `20180528000268`(2018Q1기재정정) · `20191114001837`(2018Q1기재정정) — 전부 에이피알 01190568.
- **증상**: `layer2_review.py pass` 진행 중 이 8건 전부 "⛔ 재적재 불가 (pdf) — pdf 소스는 이 CLI 가 자동 재적재하지 않는다 — `collector/pdf_lines_sync.py::sync_pdf_recovery` 로 별도 처리" 메시지와 함께 `status=blocked`로 자동 기록되고 검토 없이 `next`로 진행됨.
- **판단**: 이번 세션에서 원문 대조는 수행하지 않음(도구가 pdf 소스는 아예 CSV를 만들지 않고 차단). 이슈 9/10("xml 0행 추출 보류")과는 다른 계열 — 이쪽은 소스 자체가 PDF라서 `pdf_lines_sync.py::sync_pdf_recovery`라는 별도 수동 파이프라인이 필요.
- **다음 세션에서 확인할 것**: `collector/pdf_lines_sync.py::sync_pdf_recovery`를 이 8건에 대해 실행해 report_lines를 채운 뒤, 재적재/원문대조를 이어서 진행할지 결정. 같은 회사에서 8건이나 몰려있는 것으로 보아 에이피알 초기 공시(2018~2019)가 상당수 PDF로 접수된 것으로 추정 — 전사적으로 이 계열이 얼마나 되는지 스캔 필요.
- **이번 세션 처리**: 도구가 자동으로 blocked 처리하고 다음 건으로 진행했으므로 별도 조치 없이 캠페인 계속 진행.

## 이슈 16: 한국항공우주 2019FY(`r20200330003749`) [별도+연결] 현금흐름표 `cf_closing_cash` [차단] 2건 — DART 원문 자체의 "현금및현금성자산의순증가(감소)" 행이 환율변동효과를 이중 반영(파서 정합, 신규 계열)

- **rcept_no**: `20200330003749` (한국항공우주 00309503, 2019FY 사업보고서, 2020-03-30 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 2건 — `[별도]`(차 72,142,754,481원), `[연결]`(차 77,989,916,642원).
- **적재된 값(CSV, DART 원문과 문자 그대로 일치 확인 — 별도·연결 모두)**:
  - 별도: 영업 356,980,627,635 + 투자 -382,896,670,570 + 재무 -44,465,856,245 = **-70,381,899,180**(= 환율변동효과 행 값과 정확히 동일). 그런데 DART 원문에 명시된 "현금및현금성자산의순증가(감소)" = **-140,763,798,360**(= 위 -70,381,899,180의 정확히 2배). 기말 44,195,922,973 − 기초 112,816,966,852 = -68,621,043,879로, 이 역시 "순증가(감소)"에 표기된 -140,763,798,360과도, 환율효과 미포함 합계(-70,381,899,180)와도 정확히 일치하지 않음.
  - 연결: 영업 354,538,677,109 + 투자 -386,555,656,714 + 재무 -44,193,707,721 = **-76,210,687,326**(= 환율변동효과 행 값과 정확히 동일). "현금및현금성자산의순증가(감소)" = **-152,421,374,652**(= 위 값의 정확히 2배). 마찬가지로 기말-기초와도 정확히 일치하지 않음.
- **판단**: 파서 버그 아님 — 적재값(영업/투자/재무/환율효과/순증가감소/기초/기말) 전부 DART 원문과 완전히 일치(대조 완료). **DART 원문 자체의 "순증가(감소)" 행이 (영업+투자+재무) 합계와 우연히 같은 값인 환율변동효과를 이중으로 반영한 것으로 보이는 산식 오류**를 가지고 있으며, 기말/기초 현금 자체와도 정확히 재구성되지 않음(원문 필자 측 계산 오류로 추정). 이슈 11~14(원문 자체 내부 불일치)와 같은 계열이나, "순증가(감소)" 행이 환율효과 값과 정확히 일치하는 하위합계의 2배가 되는 특이한 하위패턴은 처음 발견.
- **다음 세션에서 확인할 것**: 한국항공우주의 다른 기간(특히 2021Q1도 별도 이슈로 `cf_closing_cash` [차단] 2건이 있었으나 그쪽은 환율효과 단순 미가산 패턴(이슈 5)이었음 — 서로 다른 하위패턴이 같은 회사에서 반복되는지 후속 기간에서 계속 관찰 필요. 별도 조치는 불필요(원문 자체 오류로 판단).
- **이번 세션 처리**: DART 원문(별도·연결 모두, 전체 CF 라인) 대조로 적재값 완전 일치 확인, 원문 자체의 산식 오류로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 17: 대한항공 2020Q3(`r20201116001718`) [별도+연결] 현금흐름표 `cf_closing_cash` [차단] 2건 — 조정행이 2건(환율효과+매각예정자산 대체)인데 셀프체크가 1건만 기대(이슈 5/7/8과 같은 계열, "조정행 개수" 하위패턴)

- **rcept_no**: `20201116001718` (대한항공 00113526, 2020년 3분기보고서, 2020-11-16 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 2건 — `[별도]`(차 -40,000,000,000원), `[연결]`(차 -40,000,000,000원). 메시지: "조정행 1행(환율효과/매각예정재분류)" 기대.
- **적재된 값(CSV, DART 원문과 문자 그대로 일치 확인 — 연결)**: 기초 816,253,031,774 + 순증감 134,929,243,699 + 환율변동효과 5,653,404,127 + 매각예정자산으로의 대체 -40,000,000,000 = **916,835,679,600** = 기말과 정확히 일치. 별도도 동일 구조(조정행 2건)로 정확히 일치 확인.
- **판단**: 파서 버그 아님(적재값·산식 모두 DART 원문과 정합, 조정행 2건 모두 원문에 그대로 존재). `cf_closing_cash` 셀프체크가 "조정행은 항상 1건"이라는 가정으로 짜여 있어, **이번처럼 환율효과 행 + 매각예정자산 대체 행이 동시에 존재하는 경우(조정행 2건)** 중 하나만 반영하고 나머지(매각예정자산 대체 -40,000,000,000)를 통째로 빠뜨려 정확히 그 금액만큼 오차가 남. 이슈 5(부호 반전)·7/8(라벨 미인식으로 누락)과 근본 원인은 같은 계열("cf_closing_cash가 인식 못 하는/개수를 못 세는 조정행")이나, 이번은 라벨 자체는 알려진 것(환율효과, 매각예정재분류 둘 다 기존에 인식되는 라벨)인데 **한 필링에 두 종류가 동시에 나타나는 경우를 처리 못 하는** 하위패턴.
- **다음 세션에서 확인할 것**: 이슈 8에서 제안한 근본수정("기초·기말 사이 모든 중간 행을 합산하는 방식")을 적용하면 이 사례도 함께 해소될 가능성 높음 — 단, 이슈 8의 삼성에스디에스 사례처럼 "이미 순증감에 포함된 참고용 중간 행은 제외"해야 하는 예외도 있으므로 해당 구분 로직과 함께 검토 필요. 동일 회사(대한항공)의 다른 기간에도 조정행 2건 동시발생 케이스가 더 있는지 후속 기간에서 관찰.
- **이번 세션 처리**: DART CSV 자체 대조(연결)로 산식 정확히 재현 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 18: 한진칼 2023Q1(`r20230515002432`) [별도] 손익계산서 `duplicate_rows` [의심] — DART 원문 자체가 "기본주당우선주순이익" 행을 2번 중복 기재(파서 정합, 신규 셀프체크 플래그 유형)

- **rcept_no**: `20230515002432` (한진칼 00983040, 2023년 1분기보고서, 2023-05-15 접수)
- **셀프체크 플래그**: `duplicate_rows` [의심] 1건(전체) — "라벨+값 완전중복 1종 — [별도]IS '기본주당우선주순이익(손실)(단위:원)' ×2". 이번 세션에서 처음 관찰된 플래그 유형.
- **적재된 값(CSV, DART 원문과 문자 그대로 일치 확인)**: 주당이익 섹션에 `기본주당보통주순이익(손실) 1,158` → `기본주당우선주순이익(손실) 1,158` → `기본주당우선주순이익(손실) 1,158`(동일 라벨 재등장) → `희석주당우선주순이익(손실) 1,158` 순서로 4행 존재. DART 원문 렌더 화면에서도 정확히 같은 순서·같은 라벨 반복·같은 값(1,158/1,158 당기, (188)/(188) 전기)으로 확인.
- **판단**: 파서 버그 아님 — DART 공시 원문 자체가 "기본주당우선주순이익" 라벨을 동일 값으로 2번 기재(작성 단계의 중복 오기 또는 서로 다른 우선주 종류를 같은 라벨로 표기한 것으로 추정, 값도 우연히 동일). 파서는 원문을 있는 그대로 적재했을 뿐.
- **다음 세션에서 확인할 것**: `duplicate_rows` 셀프체크 자체는 처음 등장한 유형 — 다른 회사·기간에서도 발생 빈도 확인 필요. 이번 건은 "원문 자체 중복"이라 오탐이지만, 파서 단계에서 같은 셀을 두 번 읽어 들이는 진짜 중복 적재 버그도 이 플래그로 걸릴 수 있으므로 향후 발견 시 원문 대조로 구분할 것.
- **이번 세션 처리**: DART 원문 대조로 원문 자체 중복임을 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 19: LS 2018FY(`r20190401004913`) [연결] 현금흐름표 `cf_closing_cash` [차단] — "중단영업 현금및현금성자산" 조정행 누락 (이슈 5/7/8/17과 같은 계열, 또 다른 신규 라벨)

- **rcept_no**: `20190401004913` (LS 00105952, 2018FY 사업보고서, 2019-04-01 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[연결]` (차 -1,429,000,000원).
- **적재된 값(CSV, DART 원문과 일치 확인)**: 기초현금및현금성자산 628,697 + 환율변동효과 반영전 순증가(감소) 185,090 + 현금및현금성자산에 대한 환율변동효과 14,181 + 중단영업 현금및현금성자산 -1,429 = 826,539(백만원) = 기말현금및현금성자산과 **정확히 일치**.
- **판단**: 파서 버그 아님(적재값·산식 모두 CSV 자체 재구성으로 정합 확인). `cf_closing_cash` 셀프체크가 "중단영업 현금및현금성자산"이라는 조정행 라벨을 알려진 패턴(환율변동효과, 매각예정재분류 등)으로 인식하지 못해 계산에서 빠뜨린 것으로 추정 — 이슈 5/7/8/17과 같은 "cf_closing_cash가 인식 못 하는 조정행 라벨" 계열의 또 다른 변형.
- **다음 세션에서 확인할 것**: 이슈 8/17에서 제안한 근본수정("기초·기말 사이 모든 중간 행을 합산하는 방식", 단 삼성에스디에스 사례처럼 이미 순증감에 포함된 참고용 행은 제외하는 예외 처리 필요)을 적용하면 "중단영업 현금및현금성자산" 라벨도 함께 해소될 가능성 높음. LS의 다른 기간이나 유사 라벨("중단영업" 관련)을 쓰는 다른 회사에도 같은 오탐이 있는지 스캔 고려.
- **이번 세션 처리**: CSV 자체 재구성으로 산식 정확히 재현 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 20: LS 2016Q3(`r20161114001456`) [별도] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 자체의 "현금및현금성자산의 증가" subtotal 이 기말−기초와 불일치 (신규 유형: 원문 자체 내부모순)

- **rcept_no**: `20161114001456` (LS 00105952, 2016Q3 분기보고서, 2016-11-14 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — `[별도]` (기말 35,832 − 기초 6,671 = 29,161 ≠ '현금및현금성자산의 증가' 0, 차 29,161,000,000원).
- **적재된 값(CSV, DART 원문과 일치 확인)**: 영업활동 현금흐름 39,075 + 투자활동 현금흐름 2,303 + 재무활동 현금흐름 -34,707 = 6,671(백만원) — 이는 '증가'가 아니라 우연히 기초현금과 같은 값이 됨. DART 원문 화면 직접 확인 결과 "현금및현금성자산의 증가" 행 자체가 0 으로 표시되어 있고(당기 3개월 열은 0, 누적 열도 0), 기초 6,671·기말 35,832 도 원문과 정확히 일치.
- **판단**: 파서 버그 아님 — 적재값이 DART 원문을 있는 그대로 반영. 그런데 DART 원문 자체의 "현금및현금성자산의 증가" subtotal(0)이 실제 영업+투자+재무 현금흐름 합계(6,671)와도, 기말−기초(29,161)와도 맞지 않는 **원문 자체의 내부모순**으로 보임(별도 재무제표, 작성사 측 기재오류 가능성). 이슈 18(원문 자체 라벨 중복)과 같은 "원문 자체 결함, 파서는 있는 그대로 적재" 계열이지만, 그쪽은 라벨 중복이고 이쪽은 subtotal 산식 자체의 불일치라는 점에서 다른 하위유형.
- **다음 세션에서 확인할 것**: 이 회사·기간(LS 2016Q3 별도)의 원문 XBRL/HTML을 재검토해 "증가" 행이 정말 0으로 태깅되어 있는지(예: 오기 vs. 렌더링 문제) 재확인 가치는 낮음 — 이미 화면 직접 대조로 원문 자체 값임을 확인함. 향후 같은 `cf_closing_cash` [차단]에서 "기말−기초"와도 "구성요소 합"과도 둘 다 안 맞는 경우를 만나면 이 이슈처럼 원문 자체 모순 여부를 우선 의심할 것.
- **이번 세션 처리**: DART 원문 화면 직접 대조로 적재값이 원문과 정확히 일치함을 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 21: 레인보우로보틱스 2022FY(`r20230323000602`) [별도+연결] 현금흐름표 `cf_closing_cash` [차단] 2건 — "환율변동효과" 행이 원문 자체에서 roll-forward에 가산되지 않는 유형 (이슈 5/7/8/17/19 와 반대 방향의 신규 변형)

- **rcept_no**: `20230323000602` (레인보우로보틱스 01261644, 2022FY 사업보고서, 2023-03-23 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 2건 — `[별도]`(차 -101,237,966원), `[연결]`(차 -101,237,966원). 두 건 모두 diff 크기가 "현금및현금성자산에 대한 환율변동효과" 행의 값과 정확히 같음.
- **적재된 값(CSV, DART 원문과 일치 확인)**: [연결] 기초현금및현금성자산 50,159,409,979 + 현금및현금성자산의순증가(감소) -2,506,028,255 = 47,653,381,724 = 기말현금및현금성자산과 **환율변동효과 행(101,237,966)을 더하지 않아도 정확히 일치**. DART 원문 화면에서 3개 비교연도 열을 모두 직접 확인한 결과, 매 열마다 "기초+순증가=기말"이 환율변동효과 없이 그대로 성립함(예: 비교연도 열 3,523,755,689+46,635,654,290=50,159,409,979). [별도]도 동일 패턴(기초 50,070,375,784 + 순증가 -2,496,631,059 = 47,573,744,725 = 기말).
- **판단**: 파서 버그 아님 — 적재값·산식 모두 CSV 자체 재구성 및 DART 원문 3개 연도 열 전부 재현으로 일치 확인. 이슈 5/7/8/17/19 계열은 "cf_closing_cash 셀프체크가 조정행을 빠뜨려서 생기는 오탐"이었던 것과 반대로, 이번 건은 **원문 자체에서 환율변동효과 행이 애초에 roll-forward 합산에 들어가지 않는 구조**(디스클로저용으로만 별도 표시되거나, 이미 다른 흐름 안에 녹아있을 가능성)로 보임. 셀프체크가 이 행을 "항상 가산해야 하는 조정행"으로 가정하기 때문에 발생하는 반대 방향의 오탐.
- **다음 세션에서 확인할 것**: `cf_closing_cash` 셀프체크 근본수정 시 "조정행 무조건 가산" 가정이 이 사례처럼 틀리는 경우가 있다는 점을 함께 고려할 것 — 즉 이슈 8/17에서 제안한 "기초·기말 사이 모든 중간 행 합산" 방식으로 바꾸더라도, 이 회사처럼 "기초+순증가"만으로 이미 기말과 일치하고 환율변동효과 행이 사실상 무관한(혹은 다른 의미의) 경우를 오판하지 않도록 예외 처리 필요. 레인보우로보틱스의 다른 연도나 유사 패턴을 쓰는 다른 회사에도 같은 오탐이 있는지 스캔 고려.
- **이번 세션 처리**: CSV 자체 재구성 + DART 원문 3개 연도 열 전체 대조로 적재값이 정확히 일치함을 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 22: 레인보우로보틱스 2022FY(`r20230323000602`) [연결] 현금흐름표 `duplicate_rows` [의심] — "전환우선주의 발행" 라벨이 원문 자체에서 별도의 두 항목으로 중복 사용 (이슈 18과 같은 계열)

- **rcept_no**: `20230323000602` (레인보우로보틱스 01261644, 2022FY 사업보고서, 2023-03-23 접수)
- **셀프체크 플래그**: `duplicate_rows` [의심] 1종 — [연결] CF "전환우선주의 발행" 행이 라벨+값 완전중복(둘 다 당기 열 값 0)으로 검출됨.
- **적재된 값(CSV, DART 원문과 일치 확인)**: DART 원문 화면 직접 확인 결과 "전환우선주의 발행" 행이 실제로 **두 줄** 존재하며, 당기(2022) 열은 둘 다 0으로 같지만 비교연도 열에서는 값이 다름 — 첫 번째 행 6,999,967,392 / 두 번째 행 28,306,586,760(2021년 열 기준). 서로 다른 전환우선주 종류(시리즈)를 같은 라벨로 표기한 것으로 추정.
- **판단**: 파서 버그 아님 — DART 원문 자체가 같은 라벨의 두 행을 갖고 있고, 당기 열만 우연히 둘 다 0이라 셀프체크가 "완전중복"으로 오판한 것. 이슈 18(한진칼 duplicate_rows, 원문 자체 중복 라벨)과 같은 계열.
- **다음 세션에서 확인할 것**: 없음 — 원문 대조로 충분히 판명됨. `duplicate_rows` 셀프체크가 비교연도 열까지 포함해서 "완전"중복인지 판정하도록 개선하면 이런 당기값 우연 일치로 인한 오탐을 줄일 수 있음(이슈 18에서도 같은 제안 있었음).
- **이번 세션 처리**: DART 원문 대조로 원문 자체가 별개의 두 행임을 확인, 파서 버그 아님으로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 23: 이수페타시스 2021Q1(`r20210517002177`) [별도] 재무상태표 `bs_balance` [차단] — DART 원문 자체가 자산총계 ≠ 자본과부채총계 (207,076,990원 내부 불일치, 신규 계열)

- **rcept_no**: `20210517002177` (이수페타시스 00107613, 2021Q1 분기보고서, 2021-05-17 접수)
- **셀프체크 플래그**: `bs_balance` [차단] 1건 — [별도] 재무상태표 자산총계 277,990,561,422 ≠ 부채총계+자본총계 278,197,638,412 (차 -207,076,990원).
- **적재된 값(CSV, DART 원문과 일치 확인)**: DART 원문 화면 직접 확인 결과 당기(제50기 1분기) 열 자체에서 "자산총계" 277,990,561,422, "부채총계" 179,837,844,619, "자본총계" 98,359,793,793, "자본과부채총계" 278,197,638,412 이 그대로 표기됨(179,837,844,619+98,359,793,793=278,197,638,412). 전기(제49기 1분기) 열은 259,604,806,409로 양쪽이 일치. 즉 원문 자체가 당기 열에서만 자산총계와 자본과부채총계가 207,076,990원만큼 불일치.
- **판단**: 파서 버그 아님 — 적재값이 DART 원문 화면 표기값과 완전히 일치(자산총계·부채총계·자본총계·자본과부채총계 4개 숫자 모두 원문 그대로 재현). DART 원문 자체의 작성 오류(혹은 반올림 외의 사유)로 추정. 이슈 13(에이피알 2019Q1 bs_rollup 원문 자체 내부불일치)과 유사한 계열이나, 플래그 종류(`bs_balance` vs `bs_rollup`)가 다르고 회사·연도도 다른 별개 사례.
- **다음 세션에서 확인할 것**: 없음 — 원문 대조로 충분히 판명됨. `bs_balance` 셀프체크가 이런 원문 자체 불일치를 만났을 때도 무조건 [차단]으로 처리하는 것이 적절한지(참고 등급으로 완화할지)는 추후 셀프체크 도구 개선 논의 대상으로만 남겨둠.
- **이번 세션 처리**: DART 원문 대조로 4개 소계값 모두 원문과 일치함을 확인, 원문 자체의 내부 불일치로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 24: 이수페타시스 2016FY(`r20170331004437`) [별도] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 자체가 조정행 없이 당기 열만 2,531,060원 내부 불일치 (이슈 1과 같은 계열, 소액 버전)

- **rcept_no**: `20170331004437` (이수페타시스 00107613, 2016FY 사업보고서, 2017-03-31 접수)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — [별도] 기말 32,390,269,032 − 기초 36,388,205,213 ≠ 현금및현금성자산의순증가(감소) -3,995,405,121 (차 -2,531,060원). 조정행(환율효과 등) 자체가 원문에 존재하지 않음.
- **적재된 값(CSV, DART 원문과 일치 확인)**: DART 원문 화면 직접 확인 결과 영업/투자/재무활동현금흐름 3개 소계 합산이 "현금및현금성자산의순증가(감소)" -3,995,405,121과 정확히 일치(20,057,268,691−31,593,025,402+7,540,351,590=-3,995,405,121)하고, 기초 36,388,205,213도 원문 그대로. 그러나 기초+순증가=32,392,800,092로 원문에 표기된 기말 32,390,269,032와 2,531,060원 차이. 원문에는 환율변동효과 등 조정행이 아예 없음(이슈 20/21처럼 조정행이 있는 것과 다름). 비교연도 2개 열(2015→2016, 2014→2015)은 각각 정확히 재현됨(27,105,803,427+9,282,401,786=36,388,205,213 / 27,488,597,011+(-382,793,584)=27,105,803,427) — **당기(2016) 열만** 불일치.
- **판단**: 파서 버그 아님 — 적재값이 DART 원문 화면 표기값과 완전히 일치. 이슈 1(엘에스일렉트릭 2022FY, 당기 열만 재현 실패)과 같은 계열의 소액 버전. 회사가 제출한 원본 현금흐름표 자체의 산술 오류(반올림 이상의 규모는 아니나 조정행 없이 발생)로 추정.
- **다음 세션에서 확인할 것**: 없음 — 원문 대조로 충분히 판명됨. 다만 이슈 1의 "④ 유사 패턴 전사 스캔" 항목에 이 사례도 포함해 함께 검토할 것.
- **이번 세션 처리**: DART 원문 대조로 적재값이 원문과 정확히 일치함을 확인, 원문 자체의 내부 불일치로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 25: SK하이닉스 2015FY(`r20160330003963`) [연결] 현금흐름표 `cf_closing_cash` [차단] — DART 원문 자체가 조정행 없이 당기 열만 30백만원 내부 불일치 (이슈 1/24와 같은 계열, 초소액 버전)

- **rcept_no**: `20160330003963` (SK하이닉스 00164779, 2015FY 사업보고서, 2016-03-30 접수, 단위 백만원)
- **셀프체크 플래그**: `cf_closing_cash` [차단] 1건 — [연결] 기말 1,175,719 − 기초 436,731 ≠ 순증가(감소) 738,958 (차 30백만원).
- **적재된 값(CSV, DART 원문과 일치 확인)**: DART 원문("2. 연결재무제표" 현금흐름표) 직접 확인 결과 당기(2015) 열 그대로 기초 436,731 / 순증가(감소) 738,958 / 기말 1,175,719 이 표기됨(환율변동효과 등 조정행 없음). 전기(2014) 열은 631,867(기초)+(-195,106)(순증가)=436,761로 표기되어 있고 당기 열의 기초 436,731과도 30 차이 — 즉 전기말(=당기초) 표기 자체가 두 열에서 30백만원 어긋나 있음(436,761 vs 436,731). 전전기(2013→2014) 열은 658,387+(-26,520)=631,867로 정확히 일치.
- **판단**: 파서 버그 아님 — 적재값이 DART 원문 화면 표기값과 완전히 일치. 이슈 1/24와 같은 계열: 원본 제출 문서 자체에 당기초/전기말 표기가 30백만원만큼 서로 다르게 인쇄된 오류가 있고, 이 오차가 그대로 당기 기말까지 전파된 것으로 추정.
- **다음 세션에서 확인할 것**: 없음 — 원문 대조로 충분히 판명됨. 이슈 1의 "④ 유사 패턴 전사 스캔" 항목에 이 사례도 포함해 함께 검토할 것.
- **이번 세션 처리**: DART 원문 대조(연결·별도 전체 BS/IS/CF/SCE 470여개 값 전수 매칭, 값 단위 다이제스트 비교 방식)로 적재값이 원문과 정확히 일치함을 확인, 원문 자체의 내부 불일치로 판단 후 `layer2_review.py pass`로 처리. 캠페인 계속 진행.

## 이슈 26: 두산에너빌리티 2020Q1(`20200515002634`) [연결] 손익계산서 — EPS `계속영업이익(손실) (단위 : 원)` 세부항목 2건 누락 (신규 계열: DB 적재 자체 누락, 자기모순 아님)

- **rcept_no**: `20200515002634` (두산에너빌리티 00159616, 2020Q1 분기보고서, 2020-05-15 접수)
- **셀프체크 플래그**: 없음(해당 self-check 도구가 이 누락을 감지하지 못함 — EPS 두 값만 놓고 보면 내부적으로 모순이 없어 통과됨).
- **적재된 값(CSV)**: [연결] 손익계산서 EPS 섹션에 기본주당이익(손실) -1,859 / 희석주당이익(손실) -1,859, 단 2개 행만 적재됨.
- **원문 검증 상세**: DART 원문(claude web view, 연결 손익계산서 EPS 블록) 직접 확인 결과 실제로는 4개 행이 존재함 — ① 기본주당이익(손실) -1,859, ② 그 아래 중첩된 `계속영업이익(손실) (단위 : 원)` -1,859, ③ 희석주당이익(손실) -1,859, ④ 그 아래 중첩된 `계속영업이익(손실) (단위 : 원)` -1,859. 이 필링은 중단영업이 없는 분기라 ①=②, ③=④로 값이 우연히 같지만, DART 원문 라벨 자체는 4행이 명확히 별개로 표기되어 있음. 나머지 전 항목([연결] BS 69/CF 50/SCE 123, [별도] BS 61/IS 24/CF 40/SCE 53)은 전수 대조(SCE 두 표는 Python list-diff로 `diffs: []`) 결과 전부 일치, 이 2행만 누락.
- **판단**: 파서/DB 적재 자체의 데이터 누락으로 판단. 지금까지 이 문서에 기록된 대부분의 이슈(원문 자체 내부모순, self-check 오탐, 라벨 중복 등)와 달리, 이번 건은 원문에 명확히 존재하는 행이 DB에 아예 실리지 않은 사례. 값이 부모 행과 우연히 같아서(중단영업 없음) 산술 self-check로는 절대 걸리지 않는 유형이라는 점이 특히 주의 필요.
- **다음 세션에서 확인할 것**: EPS 파서가 "계속영업이익(손실) (단위 : 원)"처럼 기본/희석 주당이익 행 아래 중첩되는 하위 행을 인식/적재하는지 코드 확인 필요(이번 세션은 캠페인 전용이라 파서 수정은 하지 않음). 중단영업이 실제로 있는 다른 필링에서 이 하위행 값이 부모와 달라지는 사례를 찾아 전사 영향범위(같은 패턴을 쓰는 다른 회사/기간)를 스캔할 것.
- **이번 세션 처리**: DART 원문 전체 8개 블록(별도/연결 × BS/IS/CF/SCE) 전수 대조로 EPS 세부항목 2건 누락을 확인, 문서에 기록만 하고 파서 코드는 수정하지 않은 채 `layer2_review.py pass`로 처리. 캠페인 계속 진행.
