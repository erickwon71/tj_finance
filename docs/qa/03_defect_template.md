# 결함 보고 템플릿 (DEF)

> 결함 1건당 `results/defects/DEF-<n>.md` 1파일. 개발자가 **그대로 재현**할 수 있도록 빠짐없이 채운다.
> 아래 블록을 복사해 사용. 스크린샷은 `results/shots/`에 저장하고 상대경로로 링크.

---

```markdown
# DEF-<n> — <한 줄 요약>

- **심각도**: S1 치명 / S2 중대 / S3 경미 / S4 개선제안
  - S1 = 크래시·전수 값오류 / S2 = mid-gap 앱버그·UI↔DB 값 불일치·기능 불능
  - S3 = 라벨·표기·UX / S4 = 개선제안
- **분류**: 앱버그 / 데이터결함 / 매뉴얼불일치
- **상태**: 신규 / 확인 / 수정 / 재검증 / 종료
- **발견 체크리스트 ID**: (예: DS-3, CV-8, SC-5)
- **발견일 / 담당자**:

## 위치
- **페이지**: (url_path, 예: `/company`)
- **탭 / 위젯**: (예: ②지표 탭 · "최근 N개" 슬라이더)

## 재현 조건
- **기업**: 기업명 / 종목코드 / corp_code / 시장
- **토글 상태**: 연결·별도 = ? / 연간·분기 = ?
- **기간 구간**: (슬라이더/기간 라디오 값)
- **기타 입력값**: (필터·검색어·선택 지표 등)

## 재현 단계
1.
2.
3.

## 기대결과
- (매뉴얼 근거 인용: `docs/user_manual_app.md` §___ — "...")

## 실제결과
- (관측된 화면/값/오류)

## 증거
- **스크린샷**: `results/shots/____.png`
- **UI값 ↔ DB값 대조** (해당 시):

  | 기간 | 항목 | UI 표시값 | DB 값(std_financials_v2) | 단위환산 후 | 차이 |
  |---|---|---|---|---|---|
  |   |   |   |   |   |   |

- **콘솔/서버 예외**(있으면 전문):
```
<stack trace>
```

## 영향 범위
- (해당 기업 한정 / 특정 조합 / 특정 업종 / 전수 추정)
- (전수 추정 시 근거: sweep CSV에서 동일 패턴 N건)

## 비고
-
```

---

## 심각도 판정 가이드
| 심각도 | 기준 | 예시 |
|---|---|---|
| **S1** | 앱 크래시, 다수 기업 값 오류, 데이터 유실 표시 | 특정 탭 진입 시 전 기업 예외, DB값과 전면 불일치 |
| **S2** | 핵심 기능 불능 또는 값 신뢰성 훼손 | DB엔 있는데 UI가 비는 mid-gap(앱버그), CSV 단위 오류, 다운로드 실패, 임베드/모달 미표시 |
| **S3** | 표기·라벨·UX 문제(값은 정확) | 각주 누락, 단위 라벨 오타, 정렬 기본값 상이 |
| **S4** | 개선 제안 | 안내 문구 개선, 접근성 |

## 등록 규칙
- 동일 원인·동일 증상이 여러 기업에서 반복되면 **1건으로 묶고** 영향 범위에 대표사례+건수를 기재(중복 남발 금지).
- `results/sweep_all_companies.csv` / `mid_gaps.csv` / `value_diffs.csv`의 행이 근거면 파일·행을 인용.
