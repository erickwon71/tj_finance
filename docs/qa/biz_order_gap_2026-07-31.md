# `order_backlog`·`biz_metrics` 공백 원인 규명 (2026-07-31)

도구 = `scripts/audit_biz_order_gap.py` · 표본 **300 filing**(seed 20260731, 분·반기 포함)
문서 census(2026-07-30)가 남긴 미규명 항목 — "`사업의내용→order_backlog` 62/62(100%) 공백,
`biz_metrics` 87.1% 공백"의 원인을 원문에서 갈랐다.

## 0. 결론

**공백의 대부분은 추출 결함이 아니라 원문 부재다.** "공백"이라는 한 단어가 덮고 있던 두 사실을
추출기 내부 단계(`find_order_subsections` → `map_order_table`)를 그대로 호출해 분해했다.

## 1. 수주상황 → `order_backlog`

| 상태 | filing | 비율 | 성격 |
|---|---|---|---|
| 행 있음 | 55 | 18.3% | 정상 |
| 헤딩 있음 · 표 없음 | 70 | 23.3% | **원문 부재** — '해당사항 없음' |
| '수주' 글자만(서술문·주석) | 103 | 34.3% | **원문 부재** — 절 자체가 없다 |
| 헤딩·표 있는데 0행 | 72 | 24.0% | 아래 분해 |

0행 72건의 분해:

| 이유 | filing | 비율 | 판정 |
|---|---|---|---|
| 창이 무관한 표를 잡음(러닝헤더 재출현) | 46 | 15.3% | 무해 — 0행이라 오염 없음 |
| **진행률형**(계약잔액 없이 진행률%) | 13 | 4.3% | **설계상 보류**(모듈 docstring 유형 3) |
| 표가 비어 있음(전부 '-') | 8 | 2.7% | 원문 부재 |
| 잔고 열은 있는데 0행 | 5 | 1.7% | 아래 확인 |

마지막 5건을 원문으로 열어 보니(`--dump-grid`) **잔고 열의 값이 전부 '-'** 였다:

```
[20250313000769] '다. 수주상황 내역'  단위 m3/천원
  품목        | 수주일자  | 납기        | 수주총액(수량|금액)      | 기납품액(수량|금액)      | 수주잔고(수량|금액)
  레미콘(제품) | 2024년도 | 1/1~12/31 | 396,995 | 40,385,176 | 396,995 | 40,385,176 | - | -
```

수주잔고가 실제로 0 인 회사(수주=납품 완료)다. `map_order_table` 은 backlog 가 없으면 행을
만들지 않으므로 0행이 맞다. ⇒ **추출 결함 0건.**

### 남은 것은 설계 결정 두 가지 (결함 아님)

1. **진행률형 4.3%** — `수주총액 × (1 − 진행률/100)` 로 파생 가능하나 진행률 반올림·개산계약
   변경 때문에 1차 범위에서 뺐다(모듈 docstring). 되살리려면 신뢰도 표기와 함께.
2. **잔고 0 인 표 1.7%** — 지금은 행을 안 만든다. `수주총액`·`기납품액`은 실데이터이므로
   backlog=0 행으로 남기는 편이 정보량이 많다. 용량은 무시할 수준.

## 2. 생산능력·가동률 → `biz_metrics`

| 상태 | filing | 비율 | 성격 |
|---|---|---|---|
| 행 있음 | 166 | 55.3% | 정상 |
| 헤딩·표 있는데 0행 | 33 | 11.0% | **추출 개선 여지** |
| 헤딩은 있는데 표 없음(기재생략 등) | 27 | 9.0% | 원문 부재 |
| 글자만 있음(서술문) | 28 | 9.3% | 원문 부재 |
| 관련 표기가 원문에 없음 | 46 | 15.3% | 원문 부재 |

원문 확인 사례:
- `20171113000466` — '가. 생산능력 및 생산실적' 헤딩은 있으나 본문이 **"당사 방산사업 보안
  특성상 생산능력 및 실적의 공개가 불가하여 기재를 생략합니다."** → 표가 없다(원문 부재).
- `20220816002581` — 헤딩 뒤 첫 표가 `[자산항목 : 토지]` (담보 표). 창 오검출 → 0행.
- `20220516000618` — `['사업부문','구분','2022년 1분기']` 표에서 0행. 기간 열이 1개뿐인 서식.

⇒ census 의 "87.1% 공백"은 재현되지 않는다(이 표본에서는 55.3% 가 행을 만든다). census 는
'사업의 내용' **섹션 귀속** 기준이었고 이 도구는 문서 전체(`root.iter()`)를 보므로 스코프가
다르다. 두 수치는 서로 모순이 아니라 **다른 것을 센 것**이다.

## 3. 다음에 손댈 것 (우선순위)

| 항목 | 규모(표본 기준) | 성격 |
|---|---|---|
| 가동률 0행 33건의 서식 유형 분류 | 11.0% | 추출 개선 여지 — 서식별로 나눠야 판단 가능 |
| 수주 진행률형 되살리기 | 4.3% | 설계 결정(신뢰도 표기 필요) |
| 수주 잔고 0 행 남기기 | 1.7% | 간단 |
| 수주 창 오검출 46건 | 15.3% | 0행이라 무해하나 창 경계 규칙을 조이면 스캔 비용 감소 |

**착수 전 필요한 것 없음** — 전부 추출기 안에서 끝나고 스키마 변경이 없다.
