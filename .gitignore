# --- Python / venv ---
.venv/
.venv_tj_finance/
__pycache__/
*.pyc

# --- Local data & runtime ---
raw_report/
raw_report
manual_review/
tmp/
taxonomy_cache/
scratchpad/
.local_scratch/
logs/
*.log

# --- OS / editor junk ---
.DS_Store
*~
*.un~

# --- Secrets & local settings ---
.env
.claude/settings.local.json

# --- Large regenerable snapshots (kept in ~/tj_finance_archive) ---
# Scoped instead of a blanket *.json so legitimate config/fixtures are not hidden.
fin2/tests/*.json
docs/qa/results/**/*.json

# --- Regenerable coverage / output dumps (kept in ~/tj_finance_archive) ---
download_coverage.txt
rev.txt
period_completeness.txt
scripts/probe_gateb_reader_concept_gap_2026-08-15_results.csv

# --- scratch_* 세션 작업파일(census/조사 원시 덤프, 재생성 가능·용량 큼) ---
scratch_*

# --- DB backup dumps (large binary, local-only safety net) ---
db_backups/

# 계층2 재적재 검토 CSV (사람이 원문과 대조하는 작업 파일 — 로컬 전용)
layer2_review/

# 행 결측 센서스 누적 결과 — 로컬 작업 산출물(재개용 진행기록). 집계는
# `python scripts/census_row_coverage.py --report` 로 다시 만들 수 있다.
docs/qa/row_coverage_census.jsonl

# 2단 열 서식 스캔·R154 백필 누적 결과 — 위와 같은 성격(재개용 진행기록, 로컬 산출물).
# 집계는 각 스크립트의 `--report` 로 다시 만들 수 있다. ★단, R154 백필의 **적재 목록**은
# 캠페인 세션이 재검토에 쓰므로 docs/qa/r154_backfill_applied_2026-09-21.txt 로 커밋했다.
docs/qa/dual_column_scan.jsonl
docs/qa/r154_backfill.jsonl
docs/qa/decimal_cell_scan.jsonl

# --- Per-worktree Claude role file (verification campaign, 2026-09-24) ---
CLAUDE.local.md
