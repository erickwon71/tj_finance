# TJ Finance 시각화 앱 — 콘텐츠(재무 항목·평가지표) 인벤토리 저장

## Context
사용자가 이번에는 앱의 "기능"이 아니라 **표시되는 재무 숫자 항목과 평가 지표 자체가 전문투자자 관점에서 충분·경쟁력이 있는지**를 검토해 달라고 요청했다. 이를 위해 코드베이스에서 지표 레지스트리(`app/registry/metrics.py`, `extended.py`, `dividend.py`)를 직접 읽어 현재 커버리지를 확인하던 중, 사용자가 "여기까지만 하고, 지금까지 확인한 내용을 markdown 문서로 저장해달라"고 지시했다. 즉 이번 요청의 범위는 **추가 조사 없이, 이번 세션에서 이미 읽은 파일 내용을 정리해 markdown으로 저장하는 것**으로 한정한다.

## 지금까지 확인한 내용 (저장할 대상)
- `app/registry/metrics.py`: 핵심 지표 34종 전체 목록 (재무데이터 25종 — 금액 컬럼, 수익성 9종, 성장성 4종, 안정성 10종). id/한글명/카테고리/단위/소스 컬럼까지 확인 완료.
- `app/registry/extended.py`: 확장 재무항목 카탈로그(자유조합 차트 전용, 기본 재무제표 탭에는 노출 안 됨) — BS 42종, CF 32종, IS 17종, NOTE 2종. 영업권·리스부채·이연법인세·계약자산부채·EPS 기본/희석·OCI·총포괄손익 등 포함.
- `app/registry/dividend.py`: 배당 카탈로그 4종 (DPS, 배당성향, 배당수익률, 배당총액).
- 아직 읽지 않아 이번 문서에는 포함하지 않는 것(추후 필요시 별도 조사): `analyzer/comparator.py`, `analyzer/buffett_engine.py`, `analyzer/valuation_engine.py`, `analyzer/ratio_engine.py`, `app/format.py` 등.

## 실행 계획
1. `/Users/taejin/Project/tj_finance_user/` 아래에 새 markdown 문서 작성 (예: `콘텐츠_지표_인벤토리_2026-07-15.md`).
2. 문서 구성:
   - 목적/범위 명시: "기능 평가가 아닌 콘텐츠(표시 항목·지표) 커버리지 확인용 1차 인벤토리이며, 이번 조사는 사용자 요청으로 조기 종료됨. 전문투자자 관점 충분성 판단은 후속 조사 필요."
   - 표 1: 핵심 지표 레지스트리 34종 (카테고리별로 그룹화: 재무데이터/수익성/성장성/안정성)
   - 표 2: 확장 재무항목 카탈로그 (BS/CF/IS/NOTE), 자유조합 차트에서만 노출되고 기본 재무제표 탭에는 없다는 점 명시
   - 표 3: 배당 카탈로그 4종
   - 짧은 메모: "다음에 이어서 확인이 필요한 부분" 목록 (comparator/buffett/valuation/ratio 엔진, format.py 등 미조사 항목)
3. 코드 수정 없음, 앱 실행 없음 — 순수 문서 작성.

## 검증
- 별도 검증 불필요 (읽기 전용 조사 내용을 정리하는 문서 작업).
