# R162-d (행 내부 항등식 + 이중증거) — 보류·인계 (2026-09-25 밤)

## ✅후속 세션 결론 (2026-09-25, camp_err_review)

아래 "다음 세션 할 일" 1~3 을 완료하고 **설계를 확정·구현·테스트·문서화**했다.
상세 = `docs/PARSING_RULES.md` R162-d. 요약:

- **표본 원문대조 8건**(카테고리별 A/B/D-자본거래/D-손익결과 대표 + 라이브 재추출
  검증 5건) — **확인된 오탐 0건**. 최초 "오탐"으로 봤던 이니텍 `20171114002176`
  는 SCE 원문표만 보고 판단한 실수였고, 별도 basis 자체의 IS 문에 이미 같은
  금액이 음수로 있어(같은-basis 앵커) 실제로는 참이었다.
- **증거 조건 강화**: (a) 같은 행 배제를 "행 전체"가 아니라 "지금 풀려는 항등식의
  구성원"으로 좁혔다(중첩계층에서 안쪽 합계가 이미 확정돼 있으면 바깥 항등식의
  유효한 증거인데, 예전 구현은 이걸 순환참조로 잘못 배제해 많은 참(true positive)
  후보를 "교차-basis 뿐이라 의심스러움" 범주로 잘못 분류했었다). (b) 그래도
  같은-basis 가 아닌 증거(교차-basis SCE·반대-basis BS/IS/CF·주석뿐)는 자본거래/
  이월잔액 행에만 인정 — 손익결과 항목은 basis 간 실제로 부호가 갈릴 수 있다는
  구조적 우려(확인된 사고는 없지만 방어적으로 유지).
- **구현·테스트**: `fin2/extract/sce_sign_repair.py::repair_sce_row_identity()`,
  `fin2/tests/test_r162d_row_identity.py`(5건), 전체 회귀 1,395건 통과.
- **재추출은 SD+NAS 분할 샤딩**으로 진행(아래 원안 지시대로) — 진행 상황은
  커밋 로그·`vq.py batch` 상태 참고.

이하는 이 결론에 도달하기까지의 원래 인계 기록이다.

## 상태
- 사용자 결정(2026-09-25): R162-d 는 **이중증거 셀만** 적용.
- 구현 WIP: `docs/qa/r162d_row_identity_WIP_2026-09-25.diff` (`sce_sign_repair.repair_sce_row_identity` + `report_lines` 배선).
  **커밋하지 않았다.** 코드는 되돌려 두었다.

## 규칙(WIP)
1. `col_label` 계층에서 그룹마다 '합계' 열이 하나면 Σ(형제 열) = 그 합계 열. 최상위는 지배기업 합계 + 비지배 = 자본 합계.
2. 행마다 깨진 항등식을 모두 닫는 **양수 셀 최소 부분집합이 유일**해야 한다(≤3, 성립하던 항등식을 깨지 않음).
3. 뒤집는 셀 **모두** 두 번째 증거가 있어야 한다: 같은 문서의 다른 행·표·주석에 그 금액이 음수로 있거나, BS/IS 앵커가 음수.

## 측정
- sign_flip 미해결 SCE 이슈(71필링) 재추출: 해소 70건. 그중 24건은 원래 DB=원문(오등록 추정)이라 순증은 약 46건.
- 전수 후보: DB 스캔(증거 무시) 8,753필링 → DB 증거(report_lines 전 statement + note_lines 음수) 적용 시
  **6,930필링 / 17,788셀**(1셀 3,146 · 2셀 1,617 · 3셀 633 · 4셀 569 · 5셀+ 965필링).
- 2026-09-22 R162-d 연구(3,903셀/2,398필링)의 **약 4.5배**다. "같은 금액이 다른 곳에 음수" 증거가 작은/둥근 금액의 우연 일치로
  너무 느슨할 가능성이 크다. **원문 표본 검증 없이 적용하지 않았다.**

## 다음 세션 할 일
1. 17,788셀에서 층화 표본(셀 수·금액 크기별)을 뽑아 원문과 대조 → 오탐률 측정.
2. 증거 조건 강화 후보: 금액 하한, 둥근 금액 제외, 증거 위치를 BS/IS/CF 본문·다른 SCE 행으로 한정(주석 제외) 등.
3. 확정 후 재추출 전후 비교 → 배치 reload.
   ★재추출은 **SD(`/Volumes/dart_data/raw_report`)+NAS 분할 샤딩**으로 돌린다. 이번에 NAS 만 8샤드로 돌렸더니
   3.5시간에 13%밖에 못 갔다(SMB 병목) — 메모리 feedback-bulk-read-use-sdcard 규약.
   ★DB 행에 수리 루틴을 다시 돌리는 스캔은 적재 시점 행 집합과 달라 멱등이 아니다 → 후보 추리기 용도로만 쓰고,
   규모와 정확도는 재추출 비교로만 잰다.
